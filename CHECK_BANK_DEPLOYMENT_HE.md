# פריסת שדרוג זיהוי השיקים — 15.09.2026

## עדכון המשך: הודעות, תמונת השיק וקצבת ילדים

אם השדרוג הקודם כבר עלה, העדכון הנוכחי **אינו דורש SQL או מיגרציה חדשה**.
שמור ודחוף את הקוד, המתן להצלחת בדיקות GitHub לאותו commit, ואז הרץ מתיקיית המאגר:

```powershell
.\deploy_all_fast.bat --preflight-only
.\deploy_all_fast.bat
```

רענן את האתרים לאחר ההעלאה. עדכוני אותה הפקדה מוצגים בכרטיס הודעה אחד עם
״עדכונים קודמים להפקדה״ בפירוט. החזרה, היעלמות ופירעון נשארים אירועים נפרדים.
כפתור ״צפייה בחזית השיק״ מופיע כשנמצאה תמונה לשיק המסוים בתנועת הבנק המשויכת,
באותו חשבון ובתוך תקופת השמירה הקיימת של התמונות. הוא משתמש באותו מציג מאובטח של כרטיסיית הבנק.

קצבת הילדים בחשבון הביתי מופיעה בפירוט התזרים תחת ״הכנסות צפויות״, לפי סכום
הזיכוי הסופי האחרון עם התיאור ״קצבת ילדים״. המועד המשוער הוא ה־20 בחודש, גם אם
בחודש הקודם הזיכוי הוקדם. זיכוי באותו חודש מחליף את האומדן; תנועה ממתינה אינה מספיקה.
הסכום אינו קבוע בקוד. אם אין מקור בנקאי מזוהה, לא נוצרת הכנסה משוערת.
לאחר סנכרון בנק יישמר גם מקור הקצבה בקריאות חלקיות, באותו מנגנון שמשמר את מקורות המשכנתאות.

הוראות המיגרציה להלן נוגעות לשדרוג הקודם בלבד, אם הוא עדיין לא הוחל.

השדרוג כולל קוד לשני האתרים ומיגרציה חדשה אחת:
`supabase/migrations/20260915090000_check_confirmed_automation.sql`.
המיגרציות הקודמות נשארות ללא שינוי. המיגרציה החדשה מאפשרת אישור אוטומטי של
התאמה יחידה לפי מספר שיק וסכום השיק הבודד, ומוסיפה היסטוריית פעולות שנשמרת בשרת.
גם שיק שסומן “הופקד” ידנית יכול לקבל אישור אוטומטי כשהראיות מתאימות.
היא אינה משנה את מצב השיקים בזמן ההתקנה. בסנכרון בנק מלא חדש ייבדקו גם התאמות קיימות;
התאמה ודאית תפסיק לדרוש אישור. התיעוד מתחיל מהסנכרון הבא ואינו משחזר אירועים שלא נשמרו בעבר.

השדרוג כולל גם חיבור זיהוי ארבע הספרות מהבנק למנגנון התאמת חיובי האשראי בשני האתרים.
חלק זה משתמש בשדות שכבר נוספו במיגרציה `20260914183000_bank_credit_settlement_identity.sql`
ואינו דורש מיגרציה נוספת מעבר למיגרציית השיקים החדשה שמצוינת למעלה.
הזיהוי מקבל עדיפות לפי החברה, החשבון ומחזור החיוב; ספרות סותרות, כרטיס לא מוכר
או כמה כרטיסים עם אותן ספרות לא מאפשרים לעקוף את הספק באמצעות התאמת סכום.
בכאל, כל עוד אין ספרות מזוהות מהבנק, ההתאמה הקיימת לפי חברה, מועד וסכום ממשיכה לפעול.

## 1. שמירת הקוד ובדיקת GitHub

שמור את השינויים ב־commit ודחוף ל־GitHub באמצעות תהליך העבודה הרגיל שלך.
המתן להצלחת **כל** בדיקות GitHub עבור אותו commit. ה־BAT המהיר דורש קוד שמור,
ללא שינויים מקומיים נוספים, ואישור בדיקות עבור ה־commit המדויק.
קובץ האבחון הפרטי מהבנק מוחרג מ־Git ואין לצרף אותו ל־commit.

## 2. בדיקה מקדימה ב־PowerShell

פתח PowerShell בתיקיית המאגר הראשית, ולא בתוך תיקיית אחד האתרים:

```powershell
Set-Location -LiteralPath 'C:\Users\יעקב\Downloads\pro\netunim'
supabase --version
supabase migration list --linked --project-ref bupoidcurcxuypfrjqio --workdir .
supabase db push --linked --project-ref bupoidcurcxuypfrjqio --workdir . --skip-vault --dry-run
```

אם ה־CLI מבקש התחברות, הרץ `supabase login` והשלם את ההתחברות, ואז חזור לבדיקה.
הפקודות משתמשות במפורש בפרויקט הקיים `bupoidcurcxuypfrjqio` ובתיקיית המיגרציות
הראשית. גרסת ה־CLI שנבדקה מקומית היא `2.118.0-beta.1`.

במצב הצפוי, שבו המיגרציה הקודמת כבר הוחלה, ה־dry-run אמור להציג רק את
`20260915090000_check_confirmed_automation.sql` כממתינה. אם הרשימה כוללת
מיגרציות ישנות שכבר הרצת, או מופיעה שגיאה, עצור בשלב הזה ובדוק את הפלט.
אין להריץ `migration repair` כדי לעקוף פער: סימון גרסה כהוחלה אינו מוכיח שה־SQL שלה הוחל.

## 3. החלת המיגרציה

אחרי שהבדיקה המקדימה הציגה את המיגרציה החדשה בלבד:

```powershell
supabase db push --linked --project-ref bupoidcurcxuypfrjqio --workdir . --skip-vault
```

אשר את ההחלה כש־CLI מציג את הקובץ החדש. המשך רק לאחר הצלחה.
לאחר מכן הרץ:

```powershell
supabase migration list --linked --project-ref bupoidcurcxuypfrjqio --workdir .
python tools/supabase_deploy_gate.py
```

הגרסה `20260915090000` צריכה להופיע גם מקומית וגם בשרת. הבדיקה האחרונה קוראת
את היסטוריית המיגרציות, תוכן ה־SQL ומבנה מסד הנתונים בפועל. פלט ההצלחה הוא:

```text
PASS: live Production matches this code, including reviewed migrations. Static deployment is allowed.
```

אין צורך לערוך ידנית את קובץ קבלת הפריסה לאחר כל מיגרציה. המנגנון גוזר את המבנה
הצפוי מהמיגרציות החדשות ומשווה אותו לשרת. לצורך השוואה זו משתמשים בכלי PostgreSQL
המקומיים שכבר משמשים לבדיקות המאגר; הוא אינו מריץ מיגרציות נוספות בשרת.

## 4. העלאת שני האתרים

```powershell
.\deploy_all_fast.bat --preflight-only
```

לאחר הצלחה, הרץ:

```powershell
.\deploy_all_fast.bat
```

ה־BAT בודק שוב את GitHub, את הנכסים הציבוריים ואת התאמת מסד הנתונים לפני ההעלאה.
אם מופיעה שגיאת SQL חסר/שונה, אין להחיל שוב קבצים ישנים באופן אוטומטי; בדוק קודם
איזו גרסה חסרה. אם מופיעה שגיאת התחברות, יש לתקן התחברות או קישוריות ולחזור על הבדיקה.

## מדוע עדיף CLI על הדבקה ב־SQL Editor

במאגר הזה נבדקים גם השינויים במסד וגם **תוכן המיגרציה שנשמר בהיסטוריה**.
הדבקת גוף המיגרציה בלבד ב־SQL Editor אינה רושמת בהכרח את הגרסה ואת תוכן ה־SQL
בהיסטוריית המיגרציות, ולכן עלולה להחזיר את חסימת הפריסה שכבר נתקלת בה.
מסלול `db push` מחיל את הקובץ ורושם אותו באותה פעולת פריסה; זה המסלול המתועד כאן.
אין להריץ את אותו קובץ גם ב־SQL Editor וגם ב־CLI.

[תיעוד Supabase: db push ורישום מיגרציות](https://supabase.com/docs/reference/cli/installing-and-updating#supabase-db-push)

## בדיקה קצרה לאחר ההעלאה

1. רענן את שני האתרים והפעל סנכרון בנק מלא כרגיל.
2. פתח בכרטיסיית השיקים את “הודעות ופעולות אוטומטיות בבנק”. התאמה ודאית לפי מספר וסכום תופיע שם ללא אזהרה וללא בקשת אישור. ניתן לפתוח כל אירוע ולבדוק את פרטיו, ולבטל שיוך נוכחי שגוי.
3. תנועה ממתינה תסומן כזיהוי ראשוני. אישור אוטומטי דורש פרטי שיק מובנים; אסמכתת הפקדה בלבד אינה הוכחת מספר שיק. גם לאחר המעבר לתנועה סופית התאמה ודאית תאושר אוטומטית. התאמה חלופית לפי סכום ומועד ממשיכה לדרוש אישור ידני לראיות הסופיות.
4. הסימון “נפרע” דורש שלושה ימי עסקים בנקאיים נוספים וסנכרון מלא חדש, כשהספירה מתחילה לא לפני זיהוי התנועה הסופית. פירעון תקין מתועד באזור ההודעות ללא אזהרה. זהו חישוב מהנתונים ולא אישור פירעון שהתקבל מהבנק.
5. שיק שנגרע נשאר ברשימה עם הסבר. “חזר” מחייב תנועת החזרה מזוהה; היעלמות בלבד מוצגת כהיעלמות.
6. החזרה והיעלמות ממשיכות להופיע באזהרות, גם כשהזיהוי ודאי. “ראיתי” מסיר את ההתראה עבור אותו אירוע, ולא מוחק את השיק או את פרטי המעקב. שינוי מהותי יוצר אירוע חדש.
7. ההודעות מופרדות לפי חשבון ביתי/עסקי ומוצגות בעמודים. ההיסטוריה נשמרת גם לאחר אישור אירוע או מעבר של השיק ל“נסגרו”; רענונים ללא שינוי אינם מוסיפים הודעות כפולות.
8. בחיובי האשראי, תנועה סופית עם ארבע ספרות שמזהות כרטיס יחיד באותה חברה ובאותו חשבון תסגור את מחזור החיוב המתאים בחישוב התזרימי. סכום חיוב הבנק הוא הקובע גם אם אומדן חברת האשראי שונה. תנועה ממתינה או חסרה אינה מספיקה; ספק בזהות משאיר את החיוב לבירור במנגנון הקיים.

המנגנון משאיר הכרעה ידנית כאשר כמה שיקים ללא מספר יכולים להסביר את אותו סכום.
לשיפור הדיוק, השלם מספרי שיקים ברישומים הקיימים. השלמת מספר חסר שתואם לפרטי הבנק
שומרת על המעקב; שינוי שסותר זיהוי קיים מעביר את השיק לטיפול ידני.

גם שיק שסומן “הופקד” ידנית יקושר אוטומטית לתנועת בנק מתאימה, יישאר במעקב ויוכל
לעבור לפירעון אוטומטי אחרי אישור ההתאמה, אוטומטי או ידני, והתנאים הרגילים. סימון ידני אינו אישור
לשיוך בנקאי מסוים. אם המעקב כבר כבוי בשיק קיים, ניתן להפעיל בעריכת השיק את
“זיהוי הפקדה ומעקב אוטומטי בבנק”. התיקון אינו מפעיל מחדש מעקב כבוי באופן גורף,
כדי לשמור על החלטות קודמות שלך לבטל התאמה או לעבוד ידנית.
