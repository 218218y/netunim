import {esc} from '../../core/values.js';
import {money} from '../../core/money.js';
import {dateFmt} from '../../core/dates.js';

// Dependencies are supplied by the composition root; this module has no startup side effects.
export function createDomainsCashView({model, ui, cashBalance, kpi, syncBulkUi, bulkControls, bulkHeader, bulkCell}){
function renderCash(){const rows=[...model.state.cash].sort((a,b)=>(b.date||'').localeCompare(a.date||''));document.getElementById('content').innerHTML=`<div class="grid kpis cash-kpis">${kpi('יתרת מזומן',cashBalance(),'#e8f4f2','#147d73','מחושב מכל התנועות')}${kpi('כניסות',model.state.cash.filter(x=>x.amount>0).reduce((a,x)=>a+x.amount,0),'#eaf5ee','#39835a','סה״כ תנועות חיוביות')}${kpi('יציאות / התאמות',Math.abs(model.state.cash.filter(x=>x.amount<0).reduce((a,x)=>a+x.amount,0)),'#fff0de','#d88422','סה״כ תנועות שליליות')}</div><section class="section"><div class="section-head"><div><h3>תנועות מזומן</h3></div><div class="bulk-actions">${bulkControls('cash')}<button class="btn primary" data-action="open-cash-modal">+ תנועה חדשה</button></div></div><div class="table-scroll"><table class="mobile-card-table cash-table"><thead><tr>${bulkHeader('cash')}<th>תאריך</th><th>סוג</th><th>תיאור</th><th>סכום</th><th>הערה</th><th></th></tr></thead><tbody>${rows.map(r=>`<tr data-bulk-collection="cash" data-bulk-id="${esc(r.id)}" class="${esc(ui.bulkSelected.has(r.id)?'bulk-selected-row':'')}">${bulkCell('cash',r.id)}<td data-label="תאריך">${dateFmt(r.date)}</td><td data-label="סוג">${esc(r.type)}</td><td data-label="תיאור"><b>${esc(r.description)}</b></td><td data-label="סכום" class="amount cash-amount ${esc(r.amount<0?'negative':'positive')}">${money(r.amount)}</td><td data-label="הערה" class="muted">${esc(r.note)||'—'}</td><td data-label="פעולות"><button class="iconbtn" data-action="open-cash-modal-2" data-click-arg0="${esc(r.id)}">עריכה</button></td></tr>`).join('')}</tbody></table></div></section>`;syncBulkUi('cash')}

return { renderCash };
}
