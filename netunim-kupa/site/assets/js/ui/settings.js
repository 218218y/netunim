import {esc} from '../core/values.js';
import {formatNullableMoney} from '../core/money.js';
import {formatCloudSyncTime, latestCloudUpdatedAt} from '../core/dates.js';
import {SUPA_EMAIL_KEY} from '../state/constants.js';
import {normalizeCashflowSettings} from '../shared/cashflow.js';

// Dependencies are supplied by the composition root; this module has no startup side effects.
export function createUiSettings({model, session, checksSession, files, supaProjectRef, supaConfigured, bankCurrentBalance, saveState}){
function renderSettings(){
  const b=session.serverInfo.backups||[],cloudActive=session.connectionMode==='supabase',cloudReady=supaConfigured(),email=localStorage.getItem(SUPA_EMAIL_KEY)||'',sourceUpdatedAt=session.serverInfo.lastSavedAt||null,kupaUpdatedAt=cloudActive?sourceUpdatedAt:null,checksUpdatedAt=cloudActive?(checksSession.sharedChecksUpdatedAt||null):null,lastCloudUpdatedAt=latestCloudUpdatedAt(kupaUpdatedAt,checksUpdatedAt),cashflowSettings=normalizeCashflowSettings(model.state.cashflowSettings);
  document.getElementById('content').innerHTML=`
  <div class="grid two">
    <section class="section"><div class="section-head"><div><h3>כרטיסי אשראי</h3></div></div><div class="section-body"><table class="settings-table mobile-card-table"><thead><tr><th>כרטיס</th><th>חשבון</th><th>יום חיוב</th><th>פעיל</th></tr></thead><tbody>${model.state.cards.map((c,i)=>`<tr><td data-label="כרטיס"><input value="${esc(c.name)}" data-change="update-card" data-change-arg0="${esc(i)}"></td><td data-label="חשבון"><select data-change="update-card-2" data-change-arg0="${esc(i)}"><option ${c.account==='עסקי'?'selected':''}>עסקי</option><option ${c.account==='ביתי'?'selected':''}>ביתי</option></select></td><td data-label="יום חיוב"><input type="number" min="1" max="28" step="1" inputmode="numeric" value="${esc(c.chargeDay)}" data-change="update-card-3" data-change-arg0="${esc(i)}"></td><td data-label="פעיל"><select data-change="update-card-4" data-change-arg0="${esc(i)}"><option ${c.active?'selected':''}>כן</option><option ${!c.active?'selected':''}>לא</option></select></td></tr>`).join('')}</tbody></table></div></section>
    <section class="section"><div class="section-head"><div><h3>התראות עו״ש תזרימי</h3></div></div><div class="section-body"><table class="settings-table mobile-card-table"><thead><tr><th>חשבון</th><th>סכום מינימום תזרימי</th></tr></thead><tbody><tr><td data-label="חשבון"><b>עסקי</b></td><td data-label="מינימום"><input type="number" min="0" step="1" inputmode="numeric" placeholder="למשל 5000" value="${esc(cashflowSettings.businessMinimum??'')}" data-change="update-cashflow-minimum" data-change-arg0="business"></td></tr><tr><td data-label="חשבון"><b>ביתי</b></td><td data-label="מינימום"><input type="number" min="0" step="1" inputmode="numeric" placeholder="למשל 3000" value="${esc(cashflowSettings.homeMinimum??'')}" data-change="update-cashflow-minimum" data-change-arg0="home"></td></tr></tbody></table><div class="soft-note" style="margin-top:10px">שדה ריק = התראה רק אם התחזית עוברת למינוס. סכום שהוגדר מפעיל התראה כשהעו״ש התזרימי מגיע אליו או יורד ממנו. ההגדרה משותפת גם לניהול הזמנות.</div></div></section>
    <section class="section"><div class="section-head"><div><h3>גיבוי ומקור נתונים</h3></div></div><div class="section-body">
      <div class="notice"><b>${cloudActive?'Supabase מחובר':'אחסון מקומי מחובר'}.</b></div>
      <div class="statline" style="margin-top:12px"><span class="stat-pill">קופה: ${session.dbRevision>0?'r'+esc(session.dbRevision):'טרם נטען'}</span><span class="stat-pill">צ'קים: ${checksSession.sharedChecksRevision>0?'r'+esc(checksSession.sharedChecksRevision):'טרם נטען'}</span><span class="stat-pill">${cloudActive?'עדכון ענן אחרון':'עדכון מקור אחרון'}: ${formatCloudSyncTime(cloudActive?lastCloudUpdatedAt:sourceUpdatedAt)}</span><span class="stat-pill">Schema ${session.serverInfo.schemaVersion||6}</span></div><div class="soft-note" style="margin-top:12px"><b>גיבוי מקומי:</b> ${files.backupsDirHandle?`פעיל · ${esc(files.backupRootDirHandle?.name||'התיקייה המחוברת')}`:'לא מחובר'}</div>
      <div class="backup-actions" style="margin-top:16px"><button class="btn primary" data-action="manual-backup">גיבוי עכשיו</button><button class="btn" data-action="download-json-backup">ייצא JSON</button><button class="btn" data-action="document">ייבא JSON</button><input id="restoreInput" type="file" accept="application/json,.json" hidden data-change="restore-backup"><button class="btn" data-action="switch-folder">בחר תיקיית נתונים מקומית</button><button class="btn" data-action="choose-backup-folder">חבר תיקיית גיבוי</button><button class="btn" data-action="export-c-s-v">צקים CSV</button><button class="btn" data-action="export-c-s-v-2">אשראי CSV</button></div>
      ${files.backupsDirHandle?`<div style="margin-top:18px"><b>גיבויים מקומיים אחרונים:</b><div class="muted" style="margin-top:7px;line-height:1.9">${b.length?b.slice(0,10).map(x=>`${esc(x.name)} · ${Math.max(1,Math.round(x.size/1024))} KB`).join('<br>'):'עדיין אין גיבויים'}</div></div>`:''}
    </div></section>
  </div>
  <section class="section cloud-section" style="margin-top:16px"><div class="section-head"><div><h3>סנכרון ענן Supabase</h3></div><span class="badge ${esc(cloudActive?'green':cloudReady?'blue':'red')}">${cloudActive?'מחובר':cloudReady?'מוכן להגדרה':'חסר config'}</span></div><div class="section-body">
    ${cloudActive?`
      <div class="cloud-status-grid"><div><span>חשבון</span><b>${esc(email)||'מחובר'}</b></div><div><span>פרויקט</span><b style="direction:ltr">${esc(supaProjectRef())}</b></div><div><span>קופה בענן</span><b>${session.dbRevision>0?'r'+esc(session.dbRevision):'טרם נטען'} · ${formatCloudSyncTime(kupaUpdatedAt)}</b></div><div><span>צ'קים בענן</span><b>${checksSession.sharedChecksRevision>0?'r'+esc(checksSession.sharedChecksRevision):'טרם נטען'} · ${formatCloudSyncTime(checksUpdatedAt)}</b></div></div>
      <div class="backup-actions" style="margin-top:16px"><button class="btn primary" data-action="load-supabase-state">רענן מהענן</button><button class="btn" data-action="download-json-backup">הורד גיבוי מקומי</button><button class="btn danger" data-action="logout-supabase">התנתק מהמחשב הזה</button></div>
      <div class="notice ${esc(session.cloudConflictPending?'danger':'')}" style="margin-top:14px"><b>${session.cloudConflictPending?'יש שינוי מקומי שממתין לטיפול.':'סנכרון פעיל.'}</b></div>
      <div class="backup-actions" style="margin-top:10px"><button class="btn" data-action="cloud-poll">בדוק וסנכרן עכשיו</button>${session.cloudConflictPending?`<button class="btn orange" data-action="download-json-backup">ייצא קודם את השינוי המקומי</button><button class="btn danger" data-action="discard-cloud-pending-and-load-remote">וותר על המקומי וטען ענן</button>`:''}</div>
    `:`
      <div class="notice"><b>מומלץ למעבר לכמה מחשבים.</b> הענן הוא מקור נתונים מרכזי; אין העתקת תיקיות ואין “מי שמר אחרון ניצח”. הקופה המקומית נשארת זמינה כגיבוי/מצב חלופי.</div>
      <div class="backup-actions" style="margin-top:16px"><button class="btn primary" ${cloudReady?'':'disabled'} data-action="enable-cloud-from-current-state">הפעל ענן והעלה את הקופה הנוכחית</button><button class="btn" ${cloudReady?'':'disabled'} data-action="open-supabase-login-modal">פתח קופה קיימת מהענן</button></div>
      <div class="soft-note" style="margin-top:14px">הפרויקט שמוגדר כרגע: <code>${esc(supaProjectRef())}</code>. בהתקנה חדשה יש להריץ את <code>supabase/setup.sql</code> וגם את <code>netunim-orders/supabase/shared/setup.sql</code>. במעבר מהמערכת הקיימת יש להריץ את <code>netunim-orders/supabase/shared/cutover.sql</code> בזמן חלון ההקפאה, ורק לאחר הצלחת האימות להעלות את שתי גרסאות האתר החדשות. קובץ <code>supabase/config.js</code> מכיל רק Project URL ו־Publishable Key — לא מפתח סודי.</div>
    `}
  </div></section>
  <section class="section" style="margin-top:16px"><div class="section-head"><div><h3>נתוני מערכת</h3></div></div><div class="section-body"><div class="statline"><span class="stat-pill">${esc(model.state.checks.length)} צקים</span><span class="stat-pill">${esc(model.state.credits.length)} עסקאות אשראי</span><span class="stat-pill">${esc(model.state.cash.length)} תנועות מזומן</span><span class="stat-pill">${esc((model.state.rights||[]).length)} תנועות מעשר</span><span class="stat-pill">${esc((model.state.notes||[]).length)} פתקים</span><span class="stat-pill">${esc(model.state.expenses.length)} הוצאות</span><span class="stat-pill">עו״ש ממקור הבנק: ${formatNullableMoney(bankCurrentBalance())}</span></div></div></section>`}

function updateCard(i,k,v){const old=model.state.cards[i][k];model.state.cards[i][k]=v;if(k==='name'&&old!==v){model.state.credits.forEach(cr=>{if(cr.card===old)cr.card=v})}saveState('ההגדרה נשמרה')}
function updateCashflowMinimum(account,value){const raw=String(value??'').trim(),parsed=raw===''?null:Number(raw);if(parsed!==null&&!Number.isFinite(parsed))return;const settings=normalizeCashflowSettings(model.state.cashflowSettings);if(account==='home')settings.homeMinimum=parsed;else settings.businessMinimum=parsed;model.state.cashflowSettings=normalizeCashflowSettings(settings);saveState('סף ההתראה התזרימי נשמר')}

return { renderSettings, updateCard, updateCashflowMinimum };
}
