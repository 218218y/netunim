import {esc} from '../../core/values.js';
import {$} from '../../state/constants.js';
import {googleCalendarConfig} from '../../calendar/config.js';
import {addDaysKey,calendarPrefetchRangeFor,calendarRangeContains,calendarRangeFor,localDateKey,moveFocusDate,normalizeFocusDate,normalizeViewMode,parseDateKey} from '../../calendar/view.js';

const HEBREW_WEEKDAYS=['א׳','ב׳','ג׳','ד׳','ה׳','ו׳','שבת'];
const DEFAULT_EVENT_COLOR='#8f6d55';

export function createDomainsCalendarController({ui,tab,calendarUi,calendarSession,calendarStorage,calendarAuth,calendarApi,calendarJournal,mountViewLayout,modal,closeModal,toast,confirmDialog,dateEditorMarkup}){
let started=false,reconnectTimer=null;
function consumeOAuthReturn(){if(!globalThis.location?.href||!globalThis.history?.replaceState)return{status:'',code:''};let url;try{url=new URL(globalThis.location.href)}catch{return{status:'',code:''}}const status=String(url.searchParams.get('calendar_oauth')||''),code=String(url.searchParams.get('calendar_oauth_code')||'');if(!status)return{status:'',code:''};url.searchParams.delete('calendar_oauth');url.searchParams.delete('calendar_oauth_code');globalThis.history.replaceState(globalThis.history.state,'',url.pathname+url.search+url.hash);return{status,code}}
function oauthReturnError(code){if(code==='access_denied')return 'החיבור ל-Google בוטל או לא אושר';if(code==='state_expired')return 'בקשת החיבור ל-Google פגה. יש לנסות להתחבר שוב.';if(code==='backend_not_configured')return 'שירות Google Calendar בשרת עדיין לא הוגדר';return 'החיבור ל-Google לא הושלם. יש לנסות שוב.'}
const oauthReturn=consumeOAuthReturn();
if(oauthReturn.status==='connected'){const previous=calendarStorage.connectionPreference();calendarStorage.saveConnectionPreference({autoConnect:true,accountId:previous.accountId});ui.currentView='calendar'}else if(oauthReturn.status==='error'){const previous=calendarStorage.connectionPreference();calendarStorage.saveConnectionPreference({autoConnect:false,accountId:previous.accountId});calendarSession.lastError=oauthReturnError(oauthReturn.code);ui.currentView='calendar'}
const savedConnection=calendarStorage.connectionPreference();
calendarSession.autoConnect=savedConnection.autoConnect;
calendarSession.accountId=savedConnection.accountId||calendarSession.accountId;
async function hydrateLegacyConnectionPreference(){if(savedConnection.known)return;try{const accountId=String(await calendarStorage.getMeta('accountId')||'').trim();if(!accountId||calendarStorage.connectionPreference().known)return;calendarStorage.saveConnectionPreference({autoConnect:true,accountId});calendarSession.autoConnect=true;calendarSession.accountId=accountId}catch(error){console.warn('calendar connection preference migration',error)}}
function oauthReturnUrl(){if(!globalThis.location?.href)return '';const url=new URL(globalThis.location.href);url.hash='';url.searchParams.delete('calendar_oauth');url.searchParams.delete('calendar_oauth_code');return url.toString()}
const monthFormatter=new Intl.DateTimeFormat('he-IL',{month:'long',year:'numeric'});
const shortDateTimeFormatter=new Intl.DateTimeFormat('he-IL',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});
const longDateFormatter=new Intl.DateTimeFormat('he-IL',{weekday:'long',day:'numeric',month:'long',year:'numeric'});
const shortPeriodFormatter=new Intl.DateTimeFormat('he-IL',{day:'numeric',month:'long'});
const fullPeriodFormatter=new Intl.DateTimeFormat('he-IL',{day:'numeric',month:'long',year:'numeric'});

function pad(value){return String(value).padStart(2,'0')}
function currentViewMode(){calendarUi.viewMode=normalizeViewMode(calendarUi.viewMode);return calendarUi.viewMode}
function currentFocusKey(){calendarUi.focusDate=normalizeFocusDate(calendarUi.focusDate);return calendarUi.focusDate}
function calendarRange(){return calendarRangeFor(currentFocusKey(),currentViewMode())}
function prefetchRange(){return calendarPrefetchRangeFor(currentFocusKey())}
function loadedRange(){return{rangeStart:String(calendarUi.cacheRangeStart||''),rangeEnd:String(calendarUi.cacheRangeEnd||'')}}
function loadedRangeCovers(range){return calendarRangeContains(loadedRange(),range)}
function cacheIsFresh(){const fetched=Date.parse(String(calendarUi.cacheFetchedAt||''));return Number.isFinite(fetched)&&Date.now()-fetched<Math.max(30_000,Number(googleCalendarConfig.pollIntervalMs||60_000))}
function changePeriod(delta){calendarUi.focusDate=moveFocusDate(currentFocusKey(),currentViewMode(),delta);renderCalendar({alignFocus:true})}
function goToday(){const previousRange=calendarRange().key;calendarUi.focusDate=localDateKey(new Date());if(ui.currentView==='calendar'&&calendarRange().key===previousRange){alignFocusInScroller();return}renderCalendar({alignFocus:true})}
function setViewMode(mode){const next=normalizeViewMode(mode);if(next===currentViewMode())return;calendarUi.viewMode=next;renderCalendar({alignFocus:true})}
function eventKey(calendarId,eventId){return `${encodeURIComponent(String(calendarId||''))}::${encodeURIComponent(String(eventId||''))}`}
function safeColor(value){const color=String(value||'');return /^#[0-9a-f]{6}$/i.test(color)?color:DEFAULT_EVENT_COLOR}
function eventDateKey(event){if(event?.start?.date)return String(event.start.date);const date=new Date(event?.start?.dateTime||'');return Number.isNaN(date.getTime())?'':localDateKey(date)}
function eventOccursOnDay(event,dayKey){const dayStart=parseDateKey(dayKey),dayEnd=new Date(dayStart);dayEnd.setDate(dayEnd.getDate()+1);if(event?.start?.date){const start=parseDateKey(event.start.date),exclusiveEnd=parseDateKey(event?.end?.date||addDaysKey(event.start.date,1));return start<dayEnd&&exclusiveEnd>dayStart}const start=new Date(event?.start?.dateTime||''),end=new Date(event?.end?.dateTime||event?.start?.dateTime||'');if(Number.isNaN(start.getTime())||Number.isNaN(end.getTime()))return false;return start<dayEnd&&end>dayStart}
function eventTimeLabel(event,dayKey=''){if(event?.start?.date)return '';const date=new Date(event?.start?.dateTime||'');if(Number.isNaN(date.getTime()))return '';if(dayKey&&eventDateKey(event)!==dayKey)return '↳';return `${pad(date.getHours())}:${pad(date.getMinutes())}`}
function eventSortValue(event){return `${event?.start?.date?'0':'1'}|${event?.start?.date||event?.start?.dateTime||''}|${event?.summary||''}`}
function writableCalendar(calendar){return ['owner','writer','writerWithoutPrivateAccess'].includes(String(calendar?.accessRole||''))}
function editableEvent(event){const role=String(event?._calendarAccessRole||'');return !!event&&writableCalendar({accessRole:role})&&!(role==='writerWithoutPrivateAccess'&&event.visibility==='private')&&!event.locked&&(event.eventType==null||event.eventType==='default')&&!(Array.isArray(event.attendees)&&event.attendees.length)&&event.status!=='cancelled'&&!event._pendingDelete}
function eventReadOnlyReason(event){if(event?.eventType==='birthday')return 'אירוע יום הולדת מוצג מקריאה ישירה מ-Google ואינו נערך במסך הזה.';if(Array.isArray(event?.attendees)&&event.attendees.length)return 'לאירוע יש מוזמנים. כדי לא לשנות התראות או הזמנות בלי כוונה, עריכה שלו נשארת ב-Google Calendar.';if(event?.locked)return 'Google סימן את האירוע כנעול לעריכה.';if(event?._calendarAccessRole==='writerWithoutPrivateAccess'&&event?.visibility==='private')return 'זהו אירוע פרטי ביומן שבו Google אינה מעניקה הרשאה לעריכת אירועים פרטיים.';if(!writableCalendar({accessRole:event?._calendarAccessRole}))return 'ליומן הזה יש הרשאת קריאה בלבד.';if(event?.eventType&&event.eventType!=='default')return 'סוג האירוע הזה מנוהל על-ידי Google ומוצג כאן לקריאה בלבד.';return 'האירוע מוצג לקריאה בלבד.'}
function primaryWritableCalendar(){return (calendarUi.calendars||[]).find(calendar=>calendar.primary&&writableCalendar(calendar))||(calendarUi.calendars||[]).find(writableCalendar)||null}

function decorateEvent(event,calendar){return{...event,_calendarId:event._calendarId||calendar?.id||'',_calendarSummary:event._calendarSummary||calendar?.summary||calendar?.id||'',_calendarColor:event._calendarColor||calendar?.backgroundColor||'',_calendarAccessRole:event._calendarAccessRole||calendar?.accessRole||''}}
function applyPending(snapshot,pending){const calendars=snapshot?.calendars||calendarUi.calendars||[],calendarMap=new Map(calendars.map(calendar=>[calendar.id,calendar])),events=new Map();for(const source of snapshot?.events||[]){const event=decorateEvent(source,calendarMap.get(source._calendarId));events.set(eventKey(event._calendarId,event.id),event)}for(const operation of pending||[]){const key=eventKey(operation.calendarId,operation.eventId),calendar=calendarMap.get(operation.calendarId)||{id:operation.calendarId,summary:operation.calendarId,accessRole:'writer'};if(operation.type==='insert'){events.set(key,decorateEvent({...operation.body,id:operation.eventId,_pending:true,_pendingSeq:operation.seq},calendar));continue}if(operation.type==='patch'){const prior=events.get(key)||decorateEvent({id:operation.eventId,summary:'שינוי ממתין',start:operation.body?.start,end:operation.body?.end},calendar);events.set(key,{...prior,...operation.body,_pending:true,_pendingSeq:operation.seq});continue}if(operation.type==='delete'){const prior=events.get(key);if(prior)events.set(key,{...prior,_pending:true,_pendingDelete:true,_pendingSeq:operation.seq})}}return[...events.values()].sort((a,b)=>eventSortValue(a).localeCompare(eventSortValue(b),'en'))}

function rebuildEventMap(){calendarUi.eventMap=new Map();for(const event of calendarUi.displayEvents||[])calendarUi.eventMap.set(eventKey(event._calendarId,event.id),event)}
function loadSnapshotIntoUi(snapshot,pending){calendarUi.calendars=snapshot?.calendars||[];calendarUi.events=snapshot?.events||[];calendarUi.pending=pending||[];calendarUi.displayEvents=applyPending(snapshot,pending);if(snapshot?.rangeStart&&snapshot?.rangeEnd){calendarUi.cacheRangeStart=String(snapshot.rangeStart);calendarUi.cacheRangeEnd=String(snapshot.rangeEnd)}if(snapshot?.fetchedAt)calendarUi.cacheFetchedAt=snapshot.fetchedAt;rebuildEventMap()}
function lastSyncText(){if(!calendarSession.lastSyncAt)return '';const date=new Date(calendarSession.lastSyncAt);return Number.isNaN(date.getTime())?'':shortDateTimeFormatter.format(date)}
function statusDescriptor(){const pending=(calendarUi.pending||[]).length;if(calendarSession.authResumePromise)return{className:'pending',text:`מתחבר מחדש ל-Google${pending?` · ${pending} ממתינים`:''}`};if(calendarSession.syncing)return{className:'pending',text:`מסנכרן מול Google${pending?` · ${pending} ממתינים`:''}`};if(!navigator.onLine)return{className:'offline',text:`אופליין${pending?` · ${pending} שינויים שמורים וממתינים`:''}`};if(calendarSession.lastError)return{className:'error',text:`${calendarSession.lastError}${pending?` · ${pending} ממתינים`:''}`};if(calendarAuth.hasUsableToken())return{className:'synced',text:`מחובר ל-Google${pending?` · ${pending} ממתינים`:lastSyncText()?` · עודכן ${lastSyncText()}`:''}`};if(pending)return{className:'offline',text:`${pending} שינויים שמורים מקומית · התחבר ל-Google כדי לשלוח`};return{className:'offline',text:calendarAuth.configured()?'לא מחובר ל-Google':'נדרשת הגדרת Google OAuth'}}
function updateCalendarStatus(){const el=$('#calendarStatus');if(!el)return;const descriptor=statusDescriptor();el.className=`calendar-status ${descriptor.className}`;el.textContent=descriptor.text;const authButton=$('#calendarAuthButton');if(authButton){authButton.textContent=calendarSession.authResumePromise?'מתחבר…':calendarAuth.hasUsableToken()?'נתק Google':'התחבר ל-Google';authButton.disabled=!!calendarSession.authResumePromise}const newButton=$('#calendarNewButton');if(newButton)newButton.disabled=!primaryWritableCalendar()||(calendarAuth.hasUsableToken()&&!calendarSession.accountVerified)}

function periodTitle(range){
  if(range.mode==='month')return monthFormatter.format(range.month);
  if(range.mode==='day')return longDateFormatter.format(parseDateKey(range.focusDate));
  const last=parseDateKey(addDaysKey(range.endKey,-1));
  return `${shortPeriodFormatter.format(range.gridStart)} – ${fullPeriodFormatter.format(last)}`;
}
function viewModeMarkup(){const mode=currentViewMode();return `<div class="calendar-view-modes" role="group" aria-label="תצוגת יומן"><button class="btn small calendar-view-mode${mode==='month'?' active':''}" data-action="calendar-set-view" data-click-arg0="month" aria-pressed="${mode==='month'}">חודש</button><button class="btn small calendar-view-mode${mode==='week'?' active':''}" data-action="calendar-set-view" data-click-arg0="week" aria-pressed="${mode==='week'}">שבוע</button><button class="btn small calendar-view-mode${mode==='day'?' active':''}" data-action="calendar-set-view" data-click-arg0="day" aria-pressed="${mode==='day'}">יום</button></div>`}
function toolbarMarkup(){const range=calendarRange();return `<section class="calendar-toolbar"><div class="calendar-nav-actions"><button class="btn small" data-action="calendar-prev-period" aria-label="התקופה הקודמת">‹</button><button class="btn small" data-action="calendar-today">היום</button><button class="btn small" data-action="calendar-next-period" aria-label="התקופה הבאה">›</button></div>${viewModeMarkup()}<div class="calendar-title-block"><h1>יומן</h1><div class="calendar-period-title">${esc(periodTitle(range))}</div></div><div class="calendar-toolbar-spacer"></div><div id="calendarStatus" class="calendar-status offline">טוען…</div><button class="btn small" data-action="calendar-refresh">רענן</button><button id="calendarAuthButton" class="btn small" data-action="calendar-auth">${calendarAuth.hasUsableToken()?'נתק Google':'התחבר ל-Google'}</button><button id="calendarNewButton" class="btn primary" data-action="calendar-new-event" ${primaryWritableCalendar()?'':'disabled'}>+ תור חדש</button></section>`}
function eventMarkup(event,dayKey=''){const key=eventKey(event._calendarId,event.id),pending=event._pending?' pending':'',deleting=event._pendingDelete?' deleting':'',readonly=editableEvent(event)?'':' readonly',color=safeColor(event._calendarColor),time=eventTimeLabel(event,dayKey),summary=event.summary||'(ללא כותרת)',title=event._pendingDelete?`${summary} — ממתין למחיקה`:summary;return `<button class="calendar-event${pending}${deleting}${readonly}" style="--event-color:${esc(color)}" data-action="calendar-open-event" data-click-arg0="${esc(key)}" title="${esc(title)}"><span class="calendar-event-dot"></span>${time?`<span class="calendar-event-time">${esc(time)}</span>`:''}<span class="calendar-event-title">${esc(summary)}</span>${event._pending?`<span class="calendar-event-pending" aria-label="${event._pendingDelete?'ממתין למחיקה':'ממתין לסנכרון'}">${event._pendingDelete?'×':'●'}</span>`:''}</button>`}
function eventsForDay(dayKey){return (calendarUi.displayEvents||[]).filter(event=>eventOccursOnDay(event,dayKey))}
function dayMarkup(date,range,{maxEvents=6,showWeekday=false}={}){const key=localDateKey(date),today=key===localDateKey(new Date()),outside=range.mode==='month'&&date.getMonth()!==range.month.getMonth(),events=eventsForDay(key),shown=Number.isFinite(maxEvents)?events.slice(0,maxEvents):events,extra=Math.max(0,events.length-shown.length),weekday=HEBREW_WEEKDAYS[date.getDay()];return `<section class="calendar-day${today?' today':''}${outside?' outside':''}${range.mode!=='month'?` ${range.mode}-day`:''}" data-calendar-day="${esc(key)}">${showWeekday?`<div class="calendar-day-heading">${esc(weekday)}</div>`:''}<button class="calendar-day-number" data-action="calendar-new-day" data-click-arg0="${esc(key)}" title="תור חדש בתאריך זה">${date.getDate()}</button><div class="calendar-day-events">${shown.map(event=>eventMarkup(event,key)).join('')}${extra?`<div class="calendar-more">+${extra} נוספים</div>`:''}</div></section>`}
function cachedNoteMarkup(){return calendarUi.cacheFetchedAt&&(!navigator.onLine||!calendarAuth.hasUsableToken())?`<div class="calendar-cache-note">מוצגת תצוגה שמורה מ-${esc(shortDateTimeFormatter.format(new Date(calendarUi.cacheFetchedAt)))}</div>`:''}
function monthBodyMarkup(range){const days=[];for(let index=0;index<range.days;index++){const date=new Date(range.gridStart);date.setDate(date.getDate()+index);days.push(dayMarkup(date,range))}return `<div class="calendar-weekdays">${HEBREW_WEEKDAYS.map(day=>`<div>${esc(day)}</div>`).join('')}</div><div class="calendar-grid calendar-grid-month">${days.join('')}</div>`}
function weekBodyMarkup(range){const headers=[],days=[];for(let index=0;index<range.days;index++){const date=new Date(range.gridStart);date.setDate(date.getDate()+index);const key=localDateKey(date),today=key===localDateKey(new Date());headers.push(`<div class="calendar-weekday-date${today?' today':''}"><span>${esc(HEBREW_WEEKDAYS[date.getDay()])}</span><b>${date.getDate()}</b></div>`);days.push(dayMarkup(date,range,{maxEvents:12}))}return `<div class="calendar-weekdays calendar-weekdays-dated">${headers.join('')}</div><div class="calendar-grid calendar-grid-week">${days.join('')}</div>`}
function dayBodyMarkup(range){const date=parseDateKey(range.focusDate),key=range.focusDate;return `<div class="calendar-day-view-heading"><span>${esc(longDateFormatter.format(date))}</span><button class="btn small" data-action="calendar-new-day" data-click-arg0="${esc(key)}">+ תור ביום זה</button></div><div class="calendar-grid calendar-grid-day">${dayMarkup(date,range,{maxEvents:Infinity})}</div>`}
function bodyMarkup(){const range=calendarRange(),content=range.mode==='month'?monthBodyMarkup(range):range.mode==='week'?weekBodyMarkup(range):dayBodyMarkup(range);return `<div class="calendar-board mode-${esc(range.mode)}">${cachedNoteMarkup()}${content}</div>`}
function calendarScroller(){return $('#main')?.querySelector('.calendar-view .view-scroll')||null}
function alignFocusInScroller(){const scroller=calendarScroller();if(!scroller)return;const focus=currentFocusKey(),target=scroller.querySelector(`[data-calendar-day="${focus}"]`);if(!target)return;requestAnimationFrame(()=>{if(!target.isConnected||!scroller.isConnected)return;const scrollerRect=scroller.getBoundingClientRect(),targetRect=target.getBoundingClientRect(),maxTop=Math.max(0,scroller.scrollHeight-scroller.clientHeight),centerOffset=Math.max(0,(scroller.clientHeight-targetRect.height)/2),desired=scroller.scrollTop+(targetRect.top-scrollerRect.top)-centerOffset;scroller.scrollTop=Math.max(0,Math.min(maxTop,desired))})}
function renderCalendarBody({alignFocus=false}={}){if(ui.currentView!=='calendar')return;const body=$('#calendarBody'),scroller=calendarScroller(),scrollTop=scroller?.scrollTop||0,scrollLeft=scroller?.scrollLeft||0;if(body)body.innerHTML=bodyMarkup();updateCalendarStatus();if(alignFocus)alignFocusInScroller();else if(scroller)requestAnimationFrame(()=>{if(scroller.isConnected){scroller.scrollTop=scrollTop;scroller.scrollLeft=scrollLeft}})}

async function renderCalendar({alignFocus=true}={}){currentViewMode();currentFocusKey();const range=calendarRange(),memoryCovered=loadedRangeCovers(range),initialBody=memoryCovered?bodyMarkup():'<div class="calendar-loading">טוען יומן…</div>';$('#main').innerHTML=`<div class="calendar-view">${toolbarMarkup()}<div id="calendarBody" class="calendar-body calendar-body-${range.mode}">${initialBody}</div></div>`;mountViewLayout({sourceSelector:'.calendar-view',headCount:1,className:'calendar-view'});updateCalendarStatus();if(memoryCovered){if(alignFocus)alignFocusInScroller()}else try{const [snapshot,pending]=await Promise.all([calendarStorage.getRangeCacheCovering(range.startKey,range.endKey,calendarSession.accountId),calendarStorage.listOperations()]);if(ui.currentView!=='calendar'||calendarRange().key!==range.key)return;if(snapshot)loadSnapshotIntoUi(snapshot,pending);else{calendarUi.cacheFetchedAt=null;calendarUi.cacheRangeStart='';calendarUi.cacheRangeEnd='';calendarUi.events=[];calendarUi.pending=pending;calendarUi.displayEvents=applyPending({calendars:calendarUi.calendars||[],events:[]},pending);rebuildEventMap()}renderCalendarBody({alignFocus})}catch(error){calendarSession.lastError='טעינת המטמון המקומי נכשלה';console.error(error);renderCalendarBody({alignFocus})}if(calendarAuth.hasUsableToken()&&navigator.onLine)void syncNow({quiet:true,force:false})}

function newGoogleEventId(){const bytes=new Uint8Array(16);crypto.getRandomValues(bytes);return [...bytes].map(value=>value.toString(16).padStart(2,'0')).join('')}
function localInputParts(value){const date=new Date(value||'');if(Number.isNaN(date.getTime()))return{date:localDateKey(new Date()),time:'09:00'};return{date:localDateKey(date),time:`${pad(date.getHours())}:${pad(date.getMinutes())}`}}
function allDayDisplayEnd(event){const end=String(event?.end?.date||event?.start?.date||'');return end?addDaysKey(end,-1):String(event?.start?.date||'')}
function writableCalendarOptions(selectedId){return (calendarUi.calendars||[]).filter(writableCalendar).map(calendar=>`<option value="${esc(calendar.id)}" ${calendar.id===selectedId?'selected':''}>${esc(calendar.summary||calendar.id)}</option>`).join('')}
function editorDateDefaults(dayKey){const date=dayKey||localDateKey(new Date());return{startDate:date,endDate:date,startTime:'09:00',endTime:'10:00',allDay:false}}

function openCalendarEvent(key){const event=calendarUi.eventMap?.get(key);if(!event)return toast('האירוע כבר אינו בתצוגה. רענן את היומן.');if(!editableEvent(event)){const start=event.start?.date||event.start?.dateTime||'',when=start?shortDateTimeFormatter.format(new Date(event.start?.dateTime||`${start}T00:00:00`)):'';modal(event.summary||'אירוע',`<div class="calendar-readonly-details"><div><b>יומן:</b> ${esc(event._calendarSummary||'')}</div>${when?`<div><b>מועד:</b> ${esc(when)}</div>`:''}${event.location?`<div><b>מיקום:</b> ${esc(event.location)}</div>`:''}${event.description?`<div class="calendar-description">${esc(event.description)}</div>`:''}<div class="notice">${esc(eventReadOnlyReason(event))}</div></div>`,`<button class="btn" data-action="close-modal">סגור</button>`);return}openCalendarEditor({event,key})}
function openCalendarEditor({event=null,key='',dayKey=''}={}){const writable=primaryWritableCalendar();if(!event&&!writable)return toast('יש להתחבר ל-Google לפחות פעם אחת כדי לבחור יומן לכתיבה.');let fields=editorDateDefaults(dayKey),calendarId=event?._calendarId||writable?.id||'';if(event){const allDay=!!event.start?.date;if(allDay)fields={allDay:true,startDate:event.start.date,endDate:allDayDisplayEnd(event),startTime:'',endTime:''};else{const start=localInputParts(event.start?.dateTime),end=localInputParts(event.end?.dateTime);fields={allDay:false,startDate:start.date,endDate:end.date,startTime:start.time,endTime:end.time}}}const calendarField=event?`<input value="${esc(event._calendarSummary||calendarId)}" disabled>`:`<select id="calendarCalendarId">${writableCalendarOptions(calendarId)}</select>`;modal(event?'עריכת תור':'תור חדש',`<div class="form-grid calendar-event-form"><div class="field full"><label>כותרת</label><input id="calendarSummary" value="${esc(event?.summary||'')}" autocomplete="off"></div><div class="field full"><label>יומן</label>${calendarField}</div><div class="field full calendar-all-day-field"><label><input id="calendarAllDay" type="checkbox" ${fields.allDay?'checked':''} data-change="calendar-toggle-all-day"> אירוע לכל היום</label></div><div class="field"><label>מתאריך</label>${dateEditorMarkup('calendarStartDate',fields.startDate,{label:'מתאריך'})}</div><div class="field"><label>עד תאריך</label>${dateEditorMarkup('calendarEndDate',fields.endDate,{label:'עד תאריך'})}</div><div id="calendarTimeFields" class="calendar-time-fields ${fields.allDay?'hidden':''}"><div class="field"><label>שעת התחלה</label><input id="calendarStartTime" type="time" value="${esc(fields.startTime)}"></div><div class="field"><label>שעת סיום</label><input id="calendarEndTime" type="time" value="${esc(fields.endTime)}"></div></div><div class="field full"><label>מיקום</label><input id="calendarLocation" value="${esc(event?.location||'')}"></div><div class="field full"><label>פרטים</label><textarea id="calendarDescription" class="large-textarea">${esc(event?.description||'')}</textarea></div></div>`,`<button class="btn primary" data-action="calendar-save-event" data-click-arg0="${esc(key)}">שמור</button>${event?`<button class="btn danger" data-action="calendar-delete-event" data-click-arg0="${esc(key)}">מחק</button>`:''}<button class="btn" data-action="close-modal">ביטול</button>`)}
function toggleCalendarAllDay(element){const fields=$('#calendarTimeFields');if(fields)fields.classList.toggle('hidden',!!element.checked)}
function readEditorPayload(existing=null){const summary=$('#calendarSummary')?.value.trim()||'';if(!summary)throw new Error('יש להזין כותרת לתור');const startDate=$('#calendarStartDate')?.value||'',endDate=$('#calendarEndDate')?.value||startDate;if(!startDate||!endDate)throw new Error('יש להזין תאריך התחלה וסיום');if(endDate<startDate)throw new Error('תאריך הסיום לא יכול להיות לפני תאריך ההתחלה');const allDay=!!$('#calendarAllDay')?.checked;let start,end;if(allDay){start={date:startDate};end={date:addDaysKey(endDate,1)}}else{const startTime=$('#calendarStartTime')?.value||'',endTime=$('#calendarEndTime')?.value||'';if(!startTime||!endTime)throw new Error('יש להזין שעת התחלה וסיום');const startDateTime=new Date(`${startDate}T${startTime}:00`),endDateTime=new Date(`${endDate}T${endTime}:00`);if(Number.isNaN(startDateTime.getTime())||Number.isNaN(endDateTime.getTime())||endDateTime<=startDateTime)throw new Error('שעת הסיום חייבת להיות אחרי שעת ההתחלה');start={dateTime:startDateTime.toISOString()};end={dateTime:endDateTime.toISOString()}}return{summary,description:$('#calendarDescription')?.value.trim()||'',location:$('#calendarLocation')?.value.trim()||'',start,end}}
async function refreshPendingOverlay(){const pending=await calendarStorage.listOperations();calendarUi.pending=pending;const range=calendarRange(),snapshot=await calendarStorage.getRangeCacheCovering(range.startKey,range.endKey,calendarSession.accountId);loadSnapshotIntoUi(snapshot||{calendars:calendarUi.calendars||[],events:calendarUi.events||[]},pending);renderCalendarBody()}
async function saveCalendarEvent(key=''){try{if(calendarAuth.hasUsableToken()&&!calendarSession.accountVerified)throw new Error('יש להמתין לסיום אימות חשבון Google לפני שמירת שינוי ביומן');const existing=key?calendarUi.eventMap?.get(key):null,body=readEditorPayload(existing);let operation;if(existing){if(!editableEvent(existing))throw new Error(eventReadOnlyReason(existing));operation={type:'patch',calendarId:existing._calendarId,eventId:existing.id,body,baseEtag:existing.etag||''}}else{const calendarId=$('#calendarCalendarId')?.value||primaryWritableCalendar()?.id||'';if(!calendarId)throw new Error('לא נמצא יומן עם הרשאת כתיבה');const eventId=newGoogleEventId();operation={type:'insert',calendarId,eventId,body:{id:eventId,...body}}}await calendarStorage.addOperation(operation);closeModal();await refreshPendingOverlay();toast(navigator.onLine&&calendarAuth.hasUsableToken()?'השינוי נשמר מקומית ונשלח ל-Google':'השינוי נשמר בבטחה וממתין לסנכרון');if(navigator.onLine&&calendarAuth.hasUsableToken())void syncNow({quiet:true})}catch(error){toast(error?.message||'שמירת התור נכשלה')}}
async function deleteCalendarEvent(key){if(calendarAuth.hasUsableToken()&&!calendarSession.accountVerified)return toast('יש להמתין לסיום אימות חשבון Google לפני מחיקה מהיומן');const event=calendarUi.eventMap?.get(key);if(!event||!editableEvent(event))return toast(eventReadOnlyReason(event));if(!await confirmDialog('מחיקת תור',`למחוק את התור „${event.summary||'ללא כותרת'}”?`,{confirmText:'מחק תור'}))return;try{await calendarStorage.addOperation({type:'delete',calendarId:event._calendarId,eventId:event.id});closeModal();await refreshPendingOverlay();toast(navigator.onLine&&calendarAuth.hasUsableToken()?'המחיקה נשמרה ונשלחת ל-Google':'המחיקה נשמרה וממתינה לסנכרון');if(navigator.onLine&&calendarAuth.hasUsableToken())void syncNow({quiet:true})}catch(error){toast(error?.message||'שמירת המחיקה נכשלה')}}

async function ensureAccount(calendars,pending){const primary=calendars.find(calendar=>calendar.primary)||calendars.find(calendar=>calendar.accessRole==='owner')||calendars[0],accountId=String(primary?.id||'');if(!accountId)throw new Error('Google לא החזיר יומן פעיל לחשבון');const expected=String(calendarSession.expectedAccountId||'');if(expected&&expected!==accountId){calendarAuth.clearToken();const error=new Error('Google חיברה חשבון אחר מהחשבון שנשמר ליומן הזה. החיבור האוטומטי נעצר כדי לא לערבב בין יומנים; לחץ על התחבר ל-Google ובחר את החשבון הרצוי.');error.code='calendar_account_mismatch';throw error}const stored=String(await calendarStorage.getMeta('accountId')||''),changed=!!stored&&stored!==accountId;if(changed){if(pending.length)throw new Error('קיימים שינויים ממתינים מחשבון Google אחר. התחבר לחשבון הקודם כדי לשלוח אותם לפני החלפת חשבון.');await calendarStorage.clearRangeCache();calendarUi.events=[];calendarUi.displayEvents=[];calendarUi.pending=[];calendarUi.eventMap=new Map();calendarUi.cacheFetchedAt=null;calendarUi.cacheRangeStart='';calendarUi.cacheRangeEnd=''}await calendarStorage.putMeta('accountId',accountId);calendarStorage.saveConnectionPreference({autoConnect:true,accountId});calendarSession.accountId=accountId;calendarSession.autoConnect=true;calendarSession.expectedAccountId='';calendarSession.accountVerified=true;return accountId}
async function syncNow({quiet=false,force=true}={}){if(calendarSession.syncPromise)return calendarSession.syncPromise;const range=prefetchRange();if(!force&&loadedRangeCovers(range)&&cacheIsFresh())return false;let syncedSnapshot=null,syncSucceeded=false;calendarSession.syncPromise=(async()=>{calendarSession.syncing=true;calendarSession.lastError='';updateCalendarStatus();try{if(!navigator.onLine)throw new Error('אין חיבור לרשת');if(!calendarAuth.hasUsableToken())throw calendarAuth.authRequiredError();const calendars=await calendarApi.listCalendars();let pending=await calendarStorage.listOperations();await ensureAccount(calendars,pending);calendarUi.calendars=calendars;await calendarJournal.flushPending();pending=await calendarStorage.listOperations();const events=await calendarApi.fetchEvents(calendars,range);const snapshot={key:range.key,accountId:calendarSession.accountId,rangeStart:range.startKey,rangeEnd:range.endKey,fetchedAt:new Date().toISOString(),calendars,events};await calendarStorage.putRangeCache(snapshot);syncedSnapshot=snapshot;syncSucceeded=true;calendarSession.lastSyncAt=snapshot.fetchedAt;calendarSession.lastError='';if(ui.currentView==='calendar'&&calendarRangeContains(snapshot,calendarRange())){loadSnapshotIntoUi(snapshot,pending);renderCalendarBody()}if(!quiet)toast('יומן Google עודכן')}catch(error){calendarSession.lastError=error?.code==='calendar_auth_required'?'נדרשת התחברות ל-Google':error?.message||'סנכרון היומן נכשל';try{calendarUi.pending=await calendarStorage.listOperations()}catch(e){console.error(e)}if(!quiet&&error?.message)toast(error.message);if(ui.currentView==='calendar')renderCalendarBody();if(error?.status===401&&calendarSession.autoConnect&&navigator.onLine)queueMicrotask(()=>void resumeKnownConnectionSilently());console.error('calendar sync',error)}finally{calendarSession.syncing=false;calendarSession.syncPromise=null;if(ui.currentView==='calendar')updateCalendarStatus();if(syncSucceeded&&ui.currentView==='calendar'&&calendarAuth.hasUsableToken()&&navigator.onLine&&!calendarRangeContains(syncedSnapshot,prefetchRange()))queueMicrotask(()=>void syncNow({quiet:true,force:false}))}})();return calendarSession.syncPromise}
async function calendarAuthAction(){try{if(calendarSession.authResumePromise){await calendarSession.authResumePromise;if(calendarAuth.hasUsableToken())return}if(calendarAuth.hasUsableToken()){await calendarAuth.disconnect();calendarStorage.saveConnectionPreference({autoConnect:false,accountId:calendarSession.accountId});calendarSession.autoConnect=false;calendarSession.expectedAccountId='';calendarSession.lastError='';updateCalendarStatus();toast('החיבור ל-Google נותק. שינויים ממתינים נשארים שמורים מקומית.');return}calendarSession.expectedAccountId='';calendarSession.lastError='';updateCalendarStatus();await calendarAuth.beginConnect({returnUrl:oauthReturnUrl()})}catch(error){calendarSession.expectedAccountId='';calendarSession.lastError=error?.message||'התחברות ל-Google נכשלה';updateCalendarStatus();toast(calendarSession.lastError)}}
async function refreshCalendar(){if(calendarSession.authResumePromise){await calendarSession.authResumePromise;if(calendarAuth.hasUsableToken())return syncNow({quiet:false})}if(!calendarAuth.hasUsableToken()&&calendarSession.autoConnect&&navigator.onLine){await resumeKnownConnectionSilently();if(calendarAuth.hasUsableToken())return syncNow({quiet:false})}if(!calendarAuth.hasUsableToken())return calendarAuthAction();return syncNow({quiet:false})}
function newEvent(dayKey=''){openCalendarEditor({dayKey:dayKey||localDateKey(new Date())})}
function beginRememberedReconnect({syncRegardlessOfView=false}={}){if((!tab.primaryTab&&ui.currentView!=='calendar')||!calendarSession.autoConnect||calendarSession.authResumePromise||calendarAuth.hasUsableToken()||!navigator.onLine||!calendarAuth.ready())return null;const resume=calendarAuth.restore().then(()=>{clearTimeout(reconnectTimer);calendarSession.lastError='';if(navigator.onLine&&((syncRegardlessOfView&&tab.primaryTab)||ui.currentView==='calendar'))return syncNow({quiet:true})}).catch(error=>{calendarSession.expectedAccountId='';if(['calendar_not_connected','calendar_reconnect_required'].includes(String(error?.code||''))){calendarStorage.saveConnectionPreference({autoConnect:false,accountId:calendarSession.accountId});calendarSession.autoConnect=false;if(error?.code==='calendar_reconnect_required')calendarSession.lastError=error.message;return}if(error?.code==='calendar_cloud_auth_required'){if(ui.currentView==='calendar')calendarSession.lastError=error.message;return}if(error?.code==='calendar_data_api_unavailable'&&tab.primaryTab){clearTimeout(reconnectTimer);reconnectTimer=setTimeout(()=>void resumeKnownConnectionSilently(),Math.max(1000,Number(error.retryAfterMs||15_000)))}calendarSession.lastError=error?.message||'החיבור האוטומטי ל-Google נכשל';if(ui.currentView==='calendar')console.error('calendar auto reconnect',error)}).finally(()=>{if(calendarSession.authResumePromise===resume)calendarSession.authResumePromise=null;if(ui.currentView==='calendar')updateCalendarStatus()});calendarSession.authResumePromise=resume;if(ui.currentView==='calendar')updateCalendarStatus();return resume}
function resumeKnownConnectionSilently(){return beginRememberedReconnect({syncRegardlessOfView:true})}
function start(){if(started)return;started=true;void (async()=>{await hydrateLegacyConnectionPreference();if(!calendarAuth.configured()||(!tab.primaryTab&&ui.currentView!=='calendar'))return;try{await calendarAuth.prepare();await resumeKnownConnectionSilently()}catch(error){console.warn('calendar oauth preload',error)}})();window.addEventListener('online',()=>{if(!tab.primaryTab&&ui.currentView!=='calendar')return;if(calendarAuth.hasUsableToken()){if(ui.currentView==='calendar')void syncNow({quiet:true});return}void resumeKnownConnectionSilently()});window.addEventListener('offline',()=>{if(ui.currentView==='calendar'){calendarSession.lastError='';updateCalendarStatus()}});document.addEventListener('visibilitychange',()=>{if(document.visibilityState!=='visible'||!navigator.onLine||(!tab.primaryTab&&ui.currentView!=='calendar'))return;if(calendarAuth.hasUsableToken()){if(ui.currentView==='calendar')void syncNow({quiet:true});return}void resumeKnownConnectionSilently()});calendarSession.pollTimer=setInterval(()=>{if(document.visibilityState!=='visible'||ui.currentView!=='calendar'||!navigator.onLine||calendarSession.syncing)return;if(calendarAuth.hasUsableToken())void syncNow({quiet:true});else void resumeKnownConnectionSilently()},Math.max(30_000,Number(googleCalendarConfig.pollIntervalMs||60_000)))}

return {start,renderCalendar,changePeriod,goToday,setViewMode,refreshCalendar,calendarAuthAction,newEvent,openCalendarEvent,toggleCalendarAllDay,saveCalendarEvent,deleteCalendarEvent,syncNow};
}
