import {esc, uid} from '../../core/values.js';
import {num} from '../../core/money.js';
import {incomingRemaining} from './model.js';
import {$} from '../../state/constants.js';

// Dependencies are supplied by the composition root; this module has no startup side effects.
export function createDomainsInventoryEditor({model, modal, inventoryStats, inventoryLocationDatalist, inventoryCategoryDatalist, toast, scheduleSave, closeModal, ensureInventoryCategoryOrder, renderWarehouse}){
function openInventoryItemModal(id=null){const i=id?model.state.inventoryItems.find(x=>x.id===id):null,stats=i?inventoryStats(i.id):null,canArchive=!!i&&Math.abs(stats.onHand)<1e-9&&Math.abs(stats.incoming)<1e-9&&Math.abs(stats.reserved)<1e-9;modal(i?'עריכת פריט מלאי':'פריט מלאי חדש',`<div class="form-grid"><div class="field full"><label>שם פריט</label><input id="invName" value="${esc(i?.name||'')}"></div><div class="field"><label>קטגוריה</label><input id="invCategory" list="inventoryCategoryList" placeholder="בחר קטגוריה קיימת או כתוב חדשה" value="${esc(i?.category||'')}">${inventoryCategoryDatalist()}</div><div class="field"><label>מיקום ברירת מחדל</label><input id="invLocation" list="warehouseLocationList" placeholder="מחסן גדול / מקלט / מחסן קטן" value="${esc(i?.defaultLocation||'')}">${inventoryLocationDatalist()}</div><div class="field full"><label>הערה</label><textarea id="invNote">${esc(i?.note||'')}</textarea></div>${!i?'<div class="field"><label>כמות שכבר קיימת במחסן</label><input id="invOpening" type="number" min="0" step="1" value="0"></div>':`<div class="field full"><div class="inventory-delete-note ${esc(canArchive?'':'badtext')}">${canArchive?'ניתן להסיר את הפריט מהקטלוג: אין במלאי, אין בדרך ואין שמירה פעילה.':'לא ניתן להסיר כרגע: יש לפריט מלאי, הזמנה בדרך או שמירה פעילה.'}</div></div>`}</div>`,`<button class="btn primary" data-action="save-inventory-item" data-click-arg0="${esc(id||'')}">שמור</button>${i?`<button class="btn danger" ${canArchive?'':'disabled'} data-action="archive-inventory-item" data-click-arg0="${esc(i.id)}">הסר פריט מהקטלוג</button>`:''}<button class="btn" data-action="close-modal">ביטול</button>`)}

function saveInventoryItem(id=''){const name=$('#invName').value.trim(),category=$('#invCategory').value.trim();if(!name)return toast('יש להזין שם פריט');if(!category)return toast('יש לבחור או להזין קטגוריה');let i=id?model.state.inventoryItems.find(x=>x.id===id):null;const opening=i?0:Number($('#invOpening')?.value||0);if(!i&&(!Number.isInteger(opening)||opening<0))return toast('כמות פתיחה חייבת להיות מספר שלם ולא שלילי');if(i){i.name=name;i.category=category;i.defaultLocation=$('#invLocation').value.trim();i.note=$('#invNote').value.trim();i.updatedAt=new Date().toISOString()}else{i={id:uid('INV'),name,category,defaultLocation:$('#invLocation').value.trim(),note:$('#invNote').value.trim(),active:true,createdAt:new Date().toISOString()};model.state.inventoryItems.push(i);if(opening)model.state.inventoryEvents.push({id:uid('INEV'),itemId:i.id,type:'opening',quantity:opening,customerName:'',location:i.defaultLocation,note:'יתרת פתיחה בעת יצירת הפריט',createdAt:new Date().toISOString()})}ensureInventoryCategoryOrder(category);closeModal();scheduleSave('פריט המלאי נשמר');renderWarehouse()}

function archiveInventoryItem(id){const i=model.state.inventoryItems.find(x=>x.id===id);if(!i)return;const st=inventoryStats(id);if(Math.abs(st.onHand)>=1e-9||Math.abs(st.incoming)>=1e-9||Math.abs(st.reserved)>=1e-9)return toast('אי אפשר להסיר פריט שיש עליו מלאי, מלאי בדרך או שמירה פעילה');if(!confirm(`להסיר את “${i.name}” מרשימת המלאי הפעילה? יומן התנועות ההיסטורי יישמר.`))return;i.active=false;i.archivedAt=new Date().toISOString();i.updatedAt=i.archivedAt;closeModal();scheduleSave('הפריט הוסר מהקטלוג הפעיל וההיסטוריה נשמרה');renderWarehouse()}

function openStockAdjustmentModal(itemId){const item=model.state.inventoryItems.find(i=>i.id===itemId);if(!item)return;const s=inventoryStats(itemId);modal('התאמת ספירת מלאי',`<div class="form-grid"><div class="field full"><div class="notice"><b>${esc(item.name)}</b> · כרגע במחסן ${num(s.onHand)}. רשום הפרש בלבד: לדוגמה 2+ אם נמצאו שתי יחידות, או ‎-1 אם חסרה יחידה.</div></div><div class="field"><label>הפרש כמות</label><input id="adjQty" type="number" step="1" value="0"></div><div class="field"><label>מיקום</label><input id="adjLocation" list="warehouseLocationList" value="${esc(item.defaultLocation||'')}">${inventoryLocationDatalist()}</div><div class="field full"><label>סיבת ההתאמה</label><input id="adjNote" placeholder="ספירה פיזית / תיקון טעות / סיבה אחרת"></div></div>`,`<button class="btn primary" data-action="save-stock-adjustment" data-click-arg0="${esc(itemId)}">שמור התאמה</button><button class="btn" data-action="close-modal">ביטול</button>`)}

function saveStockAdjustment(itemId){const q=Number($('#adjQty').value||0),note=$('#adjNote').value.trim();if(!Number.isInteger(q)||q===0)return toast('יש להזין הפרש שלם שאינו אפס');if(!note)return toast('יש להזין סיבה להתאמת המלאי');model.state.inventoryEvents.push({id:uid('INEV'),itemId,type:'adjust',quantity:q,customerName:'',location:$('#adjLocation').value.trim(),note,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});closeModal();scheduleSave('התאמת המלאי נשמרה ביומן');renderWarehouse()}

function openInventoryEventModal(itemId,type,eventId=null){const item=model.state.inventoryItems.find(i=>i.id===itemId),e=eventId?model.state.inventoryEvents.find(x=>x.id===eventId):null;if(!item)return;const isReserve=type==='reserve';modal(type==='order'?'הזמנת מלאי':type==='receive'?'קליטה למחסן':'שמירת פריט ללקוח',`<div class="form-grid"><div class="field full"><div class="notice"><b>${esc(item.name)}</b> · במחסן ${num(inventoryStats(itemId).onHand)} · פנוי ${num(inventoryStats(itemId).available)} · בדרך ${num(inventoryStats(itemId).incoming)}</div></div><div class="field"><label>כמות</label><input id="evQty" type="number" min="1" step="1" value="${esc(Number(e?.quantity||1))}"></div><div class="field"><label>מיקום</label><input id="evLocation" list="warehouseLocationList" value="${esc(e?.location||item.defaultLocation||'')}">${inventoryLocationDatalist()}</div>${isReserve?`<div class="field full"><label>שם לקוח</label><input id="evCustomer" value="${esc(e?.customerName||'')}"></div>`:''}<div class="field full"><label>הערה</label><input id="evNote" value="${esc(e?.note||'')}"></div></div>`,`<button class="btn primary" data-action="save-inventory-event" data-click-arg0="${esc(itemId)}" data-click-arg1="${esc(type)}" data-click-arg2="${esc(eventId||'')}">שמור</button><button class="btn" data-action="close-modal">ביטול</button>`)}

function saveInventoryEvent(itemId,type,eventId=''){const q=Number($('#evQty').value||0);if(!Number.isInteger(q)||q<=0)return toast('יש להזין כמות שלמה וחיובית');const stats=inventoryStats(itemId),customer=type==='reserve'?$('#evCustomer').value.trim():'';if(type==='reserve'&&!customer)return toast('יש להזין שם לקוח');if(type==='reserve'&&q>stats.projected&&!confirm(`הכמות גדולה מהמלאי הפנוי הצפוי (${num(stats.projected)}). לשמור בכל זאת?`))return;let e=eventId?model.state.inventoryEvents.find(x=>x.id===eventId):null;if(e?.type==='order'&&Number(e.receivedQuantity||0)>q)return toast(`כבר נקלטו ${num(e.receivedQuantity)} יחידות; אי אפשר להקטין את ההזמנה מתחת לכמות שנקלטה`);const row={...(e||{}),id:e?.id||uid('INEV'),itemId,type,quantity:q,customerName:customer,location:$('#evLocation').value.trim(),note:$('#evNote').value.trim(),createdAt:e?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};if(e)Object.assign(e,row);else model.state.inventoryEvents.push(row);closeModal();scheduleSave('תנועת המלאי נשמרה');renderWarehouse()}

function editInventoryEvent(id){const e=model.state.inventoryEvents.find(x=>x.id===id);if(!e)return;openInventoryEventModal(e.itemId,e.type,e.id)}

function openStockReceive(itemId){const pending=model.state.inventoryEvents.filter(e=>e.itemId===itemId&&incomingRemaining(e)>0);if(pending.length===1)return receiveIncoming(pending[0].id);if(pending.length>1){const item=model.state.inventoryItems.find(i=>i.id===itemId);return modal('בחירת הזמנה לקליטה',`<div class="notice"><b>${esc(item?.name||'פריט')}</b> · יש כמה הזמנות פתוחות בדרך. בחר מאיזו הזמנה לקלוט כדי שהכמות תרד נכון מ״בדרך״.</div><div class="simple-list" style="margin-top:12px">${pending.map(e=>`<div class="list-row"><div><b>${num(incomingRemaining(e))} יחידות בדרך</b><div class="source">${e.note?esc(e.note):'ללא הערה'}${Number(e.receivedQuantity||0)>0?` · כבר נקלטו ${num(e.receivedQuantity)}`:''}</div></div><button class="btn small primary" data-action="receive-incoming" data-click-arg0="${esc(e.id)}">בחר</button></div>`).join('')}</div>`,`<button class="btn" data-action="close-modal">ביטול</button>`)}openInventoryEventModal(itemId,'receive')}

function receiveIncoming(id){const e=model.state.inventoryEvents.find(x=>x.id===id),remaining=incomingRemaining(e);if(!e||e.type!=='order'||remaining<=0)return;const item=model.state.inventoryItems.find(i=>i.id===e.itemId);modal('קליטת מלאי שהגיע',`<div class="form-grid"><div class="field full"><div class="notice"><b>${esc(item?.name||'פריט')}</b> · עד ${num(remaining)} יחידות יועברו מ״בדרך״ ל״במחסן״.</div></div><div class="field"><label>כמות שנקלטה</label><input id="receiveQty" type="number" min="1" max="${esc(remaining)}" step="1" value="${esc(remaining)}"></div><div class="field"><label>מיקום במחסן</label><input id="receiveLocation" list="warehouseLocationList" value="${esc(e.location||item?.defaultLocation||'')}">${inventoryLocationDatalist()}</div><div class="field full"><label>הערה</label><input id="receiveNote" value="${esc(e.note||'')}"></div></div>`,`<button class="btn primary" data-action="confirm-receive" data-click-arg0="${esc(id)}">הגיע למחסן</button><button class="btn" data-action="close-modal">ביטול</button>`)}

function confirmReceive(id){const e=model.state.inventoryEvents.find(x=>x.id===id),remaining=incomingRemaining(e);if(!e||e.type!=='order'||remaining<=0)return;const q=Number($('#receiveQty')?.value||0);if(!Number.isInteger(q)||q<=0)return toast('יש להזין כמות שלמה לקליטה');if(q>remaining)return toast(`אפשר לקלוט עד ${num(remaining)} יחידות מההזמנה הזאת`);const now=new Date().toISOString(),location=$('#receiveLocation').value.trim(),note=$('#receiveNote').value.trim(),receivedBefore=Math.max(0,Number(e.receivedQuantity||0)),receivedAfter=receivedBefore+q;e.receivedQuantity=receivedAfter;e.receivedLocation=location;e.updatedAt=now;if(receivedAfter>=Number(e.quantity||0))e.receivedAt=now;model.state.inventoryEvents.push({id:uid('INEV'),itemId:e.itemId,type:'receive',quantity:q,customerName:'',location,note:note||`קליטה מהזמנת מלאי ${e.id}`,sourceOrderId:e.id,createdAt:now,updatedAt:now});closeModal();scheduleSave(e.receivedAt?'המלאי נקלט וההזמנה הוסרה מ״בדרך״':'המלאי נקלט והיתרה בדרך עודכנה');renderWarehouse()}

function pickupReservation(id){const e=model.state.inventoryEvents.find(x=>x.id===id);if(!e||e.type!=='reserve'||e.pickedAt||e.releasedAt)return;const s=inventoryStats(e.itemId),q=Number(e.quantity||0);if(s.onHand<q)return alert(`אין מספיק מלאי פיזי במחסן לאיסוף. במחסן ${num(s.onHand)}, שמור ללקוח ${num(q)}. יש לקלוט קודם את המלאי שהגיע.`);if(!confirm(`לסמן ש-${e.customerName} אסף ${num(q)} יחידות? הכמות תרד מהמלאי.`))return;const now=new Date().toISOString();e.pickedAt=now;e.updatedAt=now;scheduleSave('האיסוף סומן והמלאי הופחת');renderWarehouse()}

function releaseReservation(id){const e=model.state.inventoryEvents.find(x=>x.id===id);if(!e||e.type!=='reserve'||e.pickedAt||e.releasedAt||!confirm('לבטל את השמירה ולהחזיר את הכמות למלאי הפנוי?'))return;const now=new Date().toISOString();e.releasedAt=now;e.updatedAt=now;scheduleSave('השמירה בוטלה ונשמרה ביומן');renderWarehouse()}

function cancelIncoming(id){const e=model.state.inventoryEvents.find(x=>x.id===id);if(!e||incomingRemaining(e)<=0||!confirm('לבטל את יתרת הזמנת המלאי שבדרך? הכמות שכבר נקלטה תישאר במחסן והביטול יישמר ביומן.'))return;const now=new Date().toISOString();e.cancelledAt=now;e.updatedAt=now;scheduleSave('הזמנת המלאי בוטלה ונשמרה ביומן');renderWarehouse()}

return { openInventoryItemModal, saveInventoryItem, archiveInventoryItem, openStockAdjustmentModal, saveStockAdjustment, openInventoryEventModal, saveInventoryEvent, editInventoryEvent, openStockReceive, receiveIncoming, confirmReceive, pickupReservation, releaseReservation, cancelIncoming };
}
