import {esc} from '../../core/values.js';
import {num} from '../../core/money.js';
import {incomingRemaining} from '../inventory/model.js';
import {warehouseStatusLabel} from './model.js';
import {$} from '../../state/constants.js';

// Dependencies are supplied by the composition root; this module has no startup side effects.
export function createDomainsWarehouseView({warehouseUi, model, mountViewLayout, inventoryTotals, renderStockGrid, renderWarehouseLocations, warehouseBulkControls, syncWarehouseBulkUi, inventoryEventView}){
function renderWarehouse({resultsOnly=false}={}){const content=warehouseUi.warehouseTab==='stock'?renderStockGrid():warehouseUi.warehouseTab==='incoming'?renderIncomingList():warehouseUi.warehouseTab==='reservations'?renderReservationList():warehouseUi.warehouseTab==='orders'?renderWarehouseOrders():warehouseUi.warehouseTab==='locations'?renderWarehouseLocations():renderInventoryHistory();if(resultsOnly){const host=$('#warehouseSearchResults');if(host)host.innerHTML=content;if(warehouseUi.warehouseTab==='stock')syncWarehouseBulkUi();return}const totals=inventoryTotals();$('#main').innerHTML=`<div class="module-toolbar warehouse-toolbar"><input class="grow" placeholder="חיפוש פריט, לקוח, מיקום או הזמנה…" value="${esc(warehouseUi.warehouseSearch)}" data-input="warehouse-search"><div class="module-tabs"><button class="chip-filter ${esc(warehouseUi.warehouseTab==='stock'?'active':'')}" data-action="set-warehouse-tab">מלאי</button><button class="chip-filter ${esc(warehouseUi.warehouseTab==='incoming'?'active':'')}" data-action="set-warehouse-tab-2">בדרך</button><button class="chip-filter ${esc(warehouseUi.warehouseTab==='reservations'?'active':'')}" data-action="set-warehouse-tab-3">שמור ללקוחות</button><button class="chip-filter ${esc(warehouseUi.warehouseTab==='orders'?'active':'')}" data-action="set-warehouse-tab-4">הזמנות מזרונים</button><button class="chip-filter ${esc(warehouseUi.warehouseTab==='locations'?'active':'')}" data-action="set-warehouse-tab-5">מחסן</button><button class="chip-filter ${esc(warehouseUi.warehouseTab==='history'?'active':'')}" data-action="set-warehouse-tab-6">יומן תנועות</button></div><div class="module-toolbar-actions"><button class="btn" data-action="open-inventory-item-modal-2">+ פריט חדש</button>${warehouseBulkControls()}<button class="btn primary" data-action="open-warehouse-order-modal">+ הזמנת לקוח</button></div></div><div id="warehouseSearchResults">${content}</div><section class="module-bottom-summary"><div class="kpis module-kpis"><div class="kpi"><div class="label">במחסן</div><div class="value">${num(totals.onHand)}</div><div class="sub">יחידות פיזיות</div></div><div class="kpi"><div class="label">שמור ללקוחות</div><div class="value warntext">${num(totals.reserved)}</div></div><div class="kpi"><div class="label">פנוי עכשיו</div><div class="value ${esc(totals.available<0?'badtext':'goodtext')}">${num(totals.available)}</div></div><div class="kpi"><div class="label">בדרך</div><div class="value">${num(totals.incoming)}</div></div></div></section>`;mountViewLayout({headCount:1,className:'warehouse-view-shell',scrollKey:`warehouse:${warehouseUi.warehouseTab}`});if(warehouseUi.warehouseTab==='stock')syncWarehouseBulkUi()}

function renderIncomingList(){const q=(warehouseUi.warehouseSearch||'').trim();const rows=model.state.inventoryEvents.filter(e=>incomingRemaining(e)>0).map(e=>({...e,remaining:incomingRemaining(e),item:model.state.inventoryItems.find(i=>i.id===e.itemId)})).filter(x=>!q||`${x.item?.name||''} ${x.note||''} ${x.location||''}`.includes(q));return `<div class="panel"><div class="simple-list">${rows.map(x=>`<div class="list-row"><div><b>${esc(x.item?.name||'פריט לא ידוע')}</b><div class="source">${num(x.remaining)} יחידות בדרך${Number(x.receivedQuantity||0)>0?` · נקלטו ${num(x.receivedQuantity)} מתוך ${num(x.quantity)}`:''}${x.note?` · ${esc(x.note)}`:''}</div></div><div class="list-actions"><span class="badge yellow">בדרך</span><button class="btn small primary" data-action="receive-incoming" data-click-arg0="${esc(x.id)}">הגיע למחסן</button><button class="btn small" data-action="edit-inventory-event" data-click-arg0="${esc(x.id)}">עריכה</button><button class="btn small danger" data-action="cancel-incoming" data-click-arg0="${esc(x.id)}">בטל יתרה</button></div></div>`).join('')||'<div class="empty">אין כרגע מלאי בדרך.</div>'}</div></div>`}

function renderReservationList(){const q=(warehouseUi.warehouseSearch||'').trim();const rows=model.state.inventoryEvents.filter(e=>e.type==='reserve'&&!e.pickedAt&&!e.releasedAt).map(e=>({...e,item:model.state.inventoryItems.find(i=>i.id===e.itemId)})).filter(x=>!q||`${x.customerName||''} ${x.item?.name||''} ${x.note||''} ${x.location||''}`.includes(q));return `<div class="panel"><div class="simple-list">${rows.map(x=>`<div class="list-row"><div><b>${esc(x.customerName||'לקוח')}</b><div>${esc(x.item?.name||'פריט')} · ${num(x.quantity)} יחידות</div><div class="source">${x.location?`מיקום: ${esc(x.location)}`:''}${x.note?` · ${esc(x.note)}`:''}</div></div><div class="list-actions"><span class="badge yellow">שמור</span><button class="btn small primary" data-action="pickup-reservation" data-click-arg0="${esc(x.id)}">נאסף</button><button class="btn small" data-action="release-reservation" data-click-arg0="${esc(x.id)}">בטל שמירה</button></div></div>`).join('')||'<div class="empty">אין כרגע פריטים שמורים ללקוחות.</div>'}</div></div>`}

function renderInventoryHistory(){const q=(warehouseUi.warehouseSearch||'').trim();const rows=(model.state.inventoryEvents||[]).map(inventoryEventView).filter(x=>!q||`${x.item?.name||''} ${x.e.customerName||''} ${x.e.location||''} ${x.e.note||''} ${x.label}`.includes(q)).sort((a,b)=>String(b.when).localeCompare(String(a.when)));return `<div class="panel"><div class="inventory-history">${rows.map(x=>`<div class="history-row" data-inventory-event-id="${esc(x.e.id)}"><div class="history-main"><b>${esc(x.item?.name||'פריט לא ידוע')}</b><div class="source">${x.e.customerName?`${esc(x.e.customerName)} · `:''}${x.e.location?`${esc(x.e.location)} · `:''}${x.e.note?esc(x.e.note):''}</div></div><span class="badge ${esc(x.cls)}">${esc(x.label)}</span><b class="history-effect ${esc(String(x.effect).startsWith('-')?'badtext':String(x.effect).startsWith('+')?'goodtext':'')}">${esc(x.effect)}</b><span class="source history-date">${esc(x.when?String(x.when).slice(0,10):'')}</span></div>`).join('')||'<div class="empty">אין תנועות מלאי.</div>'}</div></div>`}

function renderWarehouseOrders(){const q=(warehouseUi.warehouseSearch||'').trim();const rows=model.state.warehouseOrders.filter(o=>!q||`${o.customerName||''} ${o.phone||''} ${o.details||''} ${o.location||''} ${o.note||''}`.includes(q)).sort((a,b)=>Number(a.status==='picked')-Number(b.status==='picked'));return `<div class="warehouse-order-list">${rows.map(o=>{const st=warehouseStatusLabel(o);return `<article data-warehouse-order-id="${esc(o.id)}" class="warehouse-order-row ${esc(o.status==='picked'?'closed':'')}"><div class="warehouse-order-main"><div class="service-head"><div><div class="service-title">${esc(o.customerName)}</div><div class="service-meta">${o.phone?esc(o.phone):'הזמנת מחסן'}${o.source?.row?` · אקסל ${esc(o.source.row)}`:''}</div></div><span class="badge ${esc(st[1])}">${st[0]}</span></div>${o.location?`<div class="warehouse-order-location">מיקום: ${esc(o.location)}</div>`:''}</div><div class="warehouse-order-details">${esc(o.details||'')}</div><div class="warehouse-order-actions"><div class="warehouse-status-actions warehouse-flow"><button class="workflow-btn ${esc(o.status==='to_order'?'active':'')}" data-action="set-warehouse-order-status" data-click-arg0="${esc(o.id)}">להזמין</button><button class="workflow-btn ${esc(['ordered','direct'].includes(o.status)?'active':'')}" data-action="set-warehouse-order-status-2" data-click-arg0="${esc(o.id)}">הוזמן</button><button class="workflow-btn ${esc(o.status==='arrived'?'active':'')}" data-action="set-warehouse-order-status-3" data-click-arg0="${esc(o.id)}">הגיע</button><button class="workflow-btn ${esc(o.status==='picked'?'active':'')}" data-action="set-warehouse-order-status-4" data-click-arg0="${esc(o.id)}">נאסף</button></div><button class="btn small warehouse-order-edit" data-action="open-warehouse-order-modal-2" data-click-arg0="${esc(o.id)}">עריכה</button></div></article>`}).join('')||'<div class="empty module-empty">אין הזמנות מזרונים.</div>'}</div>`}

return { renderWarehouse, renderIncomingList, renderReservationList, renderInventoryHistory, renderWarehouseOrders };
}
