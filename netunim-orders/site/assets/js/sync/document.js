import {clone} from '../core/values.js';
import {CLOUD_BASE_KEY} from '../state/constants.js';
import {CLOUD_WRITE_POLICY,contentionDelay,normalizeCloudError,runBusyCloudWriteWithPolicy} from '../shared/cloud-sync.js';

function revisionConflict(res){return !res?.r?.ok&&normalizeCloudError(res).kind==='revision_conflict'}
function saveBusy(res){return !res?.r?.ok&&normalizeCloudError(res).kind==='busy'}
function contentionBackoff(attempt=0){return new Promise(resolve=>setTimeout(resolve,contentionDelay(attempt)))}

// Dependencies are supplied by the composition root; this module has no startup side effects.
export function createSyncDocument({model, files, session, ui, tab, normalizeState, localSnapshot, markCloudPending, getCloudPending, clearCloudPending, toast, setCloud, prepareCloudState, writeStateToFolder, readCloud, rpcSave, merge3, applyOrderCloudState, cloudPendingExists, setSave, cloudEnabled, loadCloudPendingState, sameOrderCloudData, cloudHasLocalWork, render, readCloudMeta, refreshKupaReadout, pollSharedChecks, refreshCloudTimestamp}){
async function saveCloudSnapshot(snapshot,startGeneration,pendingRecord=null){let base=clone(pendingRecord?.baseState||session.lastCloudState||snapshot),expected=Number(pendingRecord?.baseRevision??session.cloudRevision??0),res=null,serverSnapshot=clone(snapshot),mergedRemote=false;for(let conflictAttempt=0;conflictAttempt<CLOUD_WRITE_POLICY.conflictAttempts;conflictAttempt++){res=await runBusyCloudWriteWithPolicy(()=>rpcSave(serverSnapshot,expected));if(saveBusy(res))throw new Error('save_busy');if(!revisionConflict(res))break;await contentionBackoff(conflictAttempt);const remote=await readCloud();if(!remote)throw new Error('מסמך הענן לא נמצא בזמן פתרון התנגשות');const merged=merge3(base,serverSnapshot,remote.state||{});if(merged.conflicts.length){session.cloudConflictBlocked=true;markCloudPending(snapshot,'',{conflict:{kind:'entity-conflict',items:merged.conflicts}});setCloud('ענן: התנגשות','error');toast('יש התנגשות בענן באותה רשומה. הנתונים המקומיים נשמרו ולא נדרסו.');return false}base=prepareCloudState(remote.state||{});serverSnapshot=prepareCloudState(merged.state);expected=Number(remote.revision||0);mergedRemote=true}if(!res?.r?.ok)throw new Error(res?.j?.message||res?.txt||'שמירה לענן נכשלה');session.cloudRevision=Number(res.row?.revision||expected+1);session.cloudUpdatedAt=res.row?.updated_at||session.cloudUpdatedAt;session.lastCloudState=clone(serverSnapshot);localStorage.setItem(CLOUD_BASE_KEY,JSON.stringify(session.lastCloudState));session.cloudConflictBlocked=false;const pendingNow=await getCloudPending();if(Number(pendingNow?.generation||0)===Number(startGeneration)){if(mergedRemote)applyOrderCloudState(serverSnapshot);const cleared=await clearCloudPending(startGeneration);if(!cleared){const latest=await getCloudPending();if(Number(latest?.generation||0)>Number(startGeneration))session.cloudSaveRequested=true;else{setCloud('ענן: השינוי אושר; ניקוי מקומי ממתין להתאוששות','error');return false}}}else{if(mergedRemote){const sharedChecks=clone(model.state.checks||[]),rebased=merge3(snapshot,prepareCloudState(model.state),serverSnapshot,{preferLocalConflicts:true});model.state=normalizeState(rebased.state);model.state.checks=sharedChecks}localSnapshot();markCloudPending(prepareCloudState(model.state),'',{baseRevision:session.cloudRevision,baseState:serverSnapshot,conflict:null});session.cloudSaveRequested=true}localSnapshot();try{if(files.dirHandle)await writeStateToFolder()}catch(localError){console.error('local backup/mirror',localError)}return true}

async function requestCloudSave(message='השינויים סונכרנו'){if(!tab.primaryTab)return false;if(message)session.cloudSaveMessage=message;session.cloudSaveRequested=true;if(!cloudEnabled())return false;if(session.cloudConflictBlocked){setCloud('ענן: התנגשות','error');return false}if(!navigator.onLine){markCloudPending(prepareCloudState(),message);try{await session.ordersOutboxCommitPromise}catch(e){console.error('orders outbox offline commit',e)}setCloud(session.cloudDurabilityDegraded?'ענן: שמירה מקומית במצב מוגבל':'ענן: אופליין',session.cloudDurabilityDegraded?'error':'offline');return false}if(session.cloudSavePromise)return session.cloudSavePromise;session.cloudSavePromise=(async()=>{let allOk=true;while(session.cloudSaveRequested&&cloudEnabled()&&navigator.onLine&&!session.cloudConflictBlocked){session.cloudSaveRequested=false;const msg=session.cloudSaveMessage||'השינויים סונכרנו';session.cloudSaveMessage='';let pending=await getCloudPending();if(!pending){if(!markCloudPending(prepareCloudState(),msg))setSave('מקומי: שגיאת pending','error');pending=await getCloudPending()}if(!pending)throw new Error('orders_outbox_persistence_failed');const startGeneration=Number(pending.generation),snapshot=prepareCloudState(pending.snapshot);session.cloudBusy=true;setCloud(session.cloudDurabilityDegraded?'ענן: מצב התאוששות':'ענן: מסנכרן…',session.cloudDurabilityDegraded?'error':'');try{const ok=await saveCloudSnapshot(snapshot,startGeneration,pending);if(!ok){allOk=false;break}const newer=await getCloudPending();if(newer&&Number(newer.generation)>startGeneration)session.cloudSaveRequested=true;if(!session.cloudSaveRequested&&!newer){setCloud('ענן: מסונכרן','synced');if(msg)toast(msg)}else setCloud('ענן: מסנכרן…')}catch(e){console.error('cloud save',e);const current=await getCloudPending(),normalized=normalizeCloudError(e),attempts=Number(current?.retry?.attempts||0)+1,nextAttemptAt=normalized.retryAfterMs?new Date(Date.now()+normalized.retryAfterMs).toISOString():null;markCloudPending(prepareCloudState(model.state),msg,{retry:{attempts,lastErrorCode:normalized.code||normalized.kind,lastAttemptAt:new Date().toISOString(),nextAttemptAt}});setCloud(navigator.onLine?'ענן: ממתין לסנכרון':'ענן: אופליין',navigator.onLine?'':'offline');allOk=false;break}finally{session.cloudBusy=false}}return allOk&&!cloudPendingExists()})().finally(()=>{session.cloudSavePromise=null;if(session.cloudSaveRequested&&navigator.onLine&&!session.cloudConflictBlocked)setTimeout(()=>requestCloudSave(session.cloudSaveMessage||'השינויים סונכרנו'),0)});return session.cloudSavePromise}


async function restorePendingAgainstCloud(row){const durable=await getCloudPending(),pendingRaw=durable?.snapshot||loadCloudPendingState()||((session.cloudSaveRequested||!!(session.lastCloudState&&!sameOrderCloudData(model.state,session.lastCloudState)))?clone(model.state):null);if(!pendingRaw)return false;const pending=normalizeState(clone(pendingRaw)),remote=normalizeState(clone(row.state||{})),base=durable?.baseState?normalizeState(clone(durable.baseState)):session.lastCloudState?normalizeState(clone(session.lastCloudState)):null;model.state=pending;session.cloudRevision=Number(row.revision||0);session.cloudUpdatedAt=row.updated_at||session.cloudUpdatedAt;if(!base){session.cloudConflictBlocked=true;setCloud('ענן: נדרש שחזור','error');localSnapshot();toast('נמצאו שינויים מקומיים שלא סונכרנו, אך חסרה גרסת הבסיס. הנתונים המקומיים נשמרו ולא נדרסו.');return true}const merged=merge3(base,pending,remote);if(merged.conflicts.length){session.cloudConflictBlocked=true;setCloud('ענן: התנגשות','error');localSnapshot();toast('נמצאו שינויים מקומיים ועדכון ענן באותה רשומה. שום נתון לא נדרס.');return true}model.state=normalizeState(merged.state);session.lastCloudState=clone(remote);localStorage.setItem(CLOUD_BASE_KEY,JSON.stringify(session.lastCloudState));localSnapshot();markCloudPending(prepareCloudState(model.state));session.localGeneration=Math.max(session.localGeneration,1);session.cloudSaveRequested=true;await requestCloudSave('שינויים מקומיים שוחזרו וסונכרנו');return true}

async function cloudPoll(){if(!tab.primaryTab)return;if(!cloudEnabled()||session.cloudBusy||!navigator.onLine)return;if(cloudHasLocalWork()){await requestCloudSave('סונכרנו שינויים מקומיים ועדכון מרחוק');await pollSharedChecks();return}try{session.cloudBusy=true;const meta=await readCloudMeta();if(!meta)return;const metaRevision=Number(meta.revision||0);if(metaRevision<=session.cloudRevision){session.cloudUpdatedAt=meta.updated_at||session.cloudUpdatedAt;refreshCloudTimestamp();return}const row=await readCloud();if(!row)return;if(Number(row.revision||0)<=session.cloudRevision)return;applyOrderCloudState(row.state);session.cloudRevision=Number(row.revision);session.cloudUpdatedAt=row.updated_at||meta.updated_at||session.cloudUpdatedAt;session.lastCloudState=prepareCloudState(model.state);localStorage.setItem(CLOUD_BASE_KEY,JSON.stringify(session.lastCloudState));localSnapshot();try{if(files.dirHandle)await writeStateToFolder()}catch(localError){console.error('local backup/mirror',localError)}setCloud('ענן: מסונכרן','synced');render();toast('התקבל עדכון מהענן')}catch(e){console.error(e)}finally{session.cloudBusy=false;await pollSharedChecks();await refreshKupaReadout({renderIfChanged:true})}}

function startPolling(){clearTimeout(session.cloudPollTimer);session.cloudPollingEnabled=true;const schedule=()=>{if(!session.cloudPollingEnabled)return;session.cloudPollTimer=setTimeout(async()=>{try{await cloudPoll()}finally{schedule()}},12_000+Math.floor(Math.random()*2_000))};schedule()}

return { saveCloudSnapshot, requestCloudSave, restorePendingAgainstCloud, cloudPoll, startPolling };
}
