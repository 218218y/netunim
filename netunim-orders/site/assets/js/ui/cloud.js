import {esc, clone} from '../core/values.js';
import {CLOUD_EMAIL_KEY, $, CLOUD_AUTO_KEY, CLOUD_BASE_KEY} from '../state/constants.js';

// Dependencies are supplied by the composition root; this module has no startup side effects.
export function createUiCloud({model, files, tab, session, checksSession, ui, modal, supaConfigured, toast, closeModal, authPassword, localSnapshot, markCloudPending, clearCloudPending, setCloud, showSecondaryTabGuard, prepareCloudState, render, writeStateToFolder, loadSession, readCloud, applyOrderCloudState, refreshKupaReadout, syncSharedChecksFromCloud, requestCloudSave, restorePendingAgainstCloud, startPolling, saveSession, renderSettings}){
function loginModal(mode='open'){if(!supaConfigured())return alert('הגדרת Supabase חסרה. בדוק supabase/config.js');const email=localStorage.getItem(CLOUD_EMAIL_KEY)||'';modal(mode==='upload'?'הפעלת ענן נפרד לניהול הזמנות':'פתיחת ניהול הזמנות מהענן',`<div class="form-grid"><div class="field full"><div class="notice">ניהול ההזמנות נשמר בטבלאות <b>order_management_*</b>. טאב הצ'קים קורא וכותב ישירות למאגר הצ'קים המשותף <b>shared_checks_documents/main</b>. בנוסף, כרטיס „מאזן קופה נטו” קורא בנפרד את <b>kupa_documents/main</b> לצורך חישוב בלבד; ניהול ההזמנות אינו יוצר מסלול כתיבה חדש לנתוני עו״ש, אשראי, הוצאות או מזומן.</div></div><div class="field full"><label>אימייל Supabase Auth</label><input id="cEmail" type="email" value="${esc(email)}"></div><div class="field full"><label>סיסמה</label><input id="cPassword" type="password"></div></div>`,`<button class="btn primary" data-action="finish-cloud-login" data-click-arg0="${esc(mode)}">התחבר</button><button class="btn" data-action="close-modal">ביטול</button>`)}

async function finishCloudLogin(mode){const email=$('#cEmail').value.trim(),pass=$('#cPassword').value;if(!email||!pass)return toast('יש להזין אימייל וסיסמה');try{await authPassword(email,pass);closeModal();if(mode==='upload')await enableCloud(true);else await openCloud()}catch(e){console.error(e);toast('התחברות נכשלה: '+e.message)} }

async function enableCloud(afterLogin=false){if(!tab.primaryTab)return showSecondaryTabGuard();if(!supaConfigured())return alert('הגדרת Supabase חסרה');if(!loadSession()&&!afterLogin)return loginModal('upload');try{setCloud('ענן: בודק…');const existing=await readCloud();localStorage.setItem(CLOUD_AUTO_KEY,'1');if(existing){if(!(await restorePendingAgainstCloud(existing))){session.cloudConflictBlocked=false;applyOrderCloudState(existing.state);session.cloudRevision=Number(existing.revision||0);session.cloudUpdatedAt=existing.updated_at||session.cloudUpdatedAt;session.lastCloudState=prepareCloudState(model.state);localStorage.setItem(CLOUD_BASE_KEY,JSON.stringify(session.lastCloudState));clearCloudPending();localSnapshot();try{if(files.dirHandle)await writeStateToFolder()}catch(localError){console.error('local backup/mirror',localError)}setCloud('ענן: מסונכרן','synced');render();toast('כבר היה מסמך ניהול הזמנות בענן — נטענה גרסת הענן ולא נדרסה.')}}else{session.cloudRevision=0;session.lastCloudState=clone(prepareCloudState());localStorage.setItem(CLOUD_BASE_KEY,JSON.stringify(session.lastCloudState));markCloudPending();session.localGeneration=Math.max(session.localGeneration,1);await requestCloudSave('נתוני ניהול ההזמנות הועלו לענן הנפרד')}await syncSharedChecksFromCloud({quiet:true,required:true});await refreshKupaReadout({force:true});startPolling()}catch(e){console.error(e);toast('לא ניתן להפעיל ענן: '+e.message);setCloud('ענן: שגיאה','error')}}

async function openCloud({renderAfter=true}={}){if(!tab.primaryTab)return false;if(!loadSession())return loginModal('open');try{setCloud('ענן: פותח…');const row=await readCloud();if(!row){setCloud('ענן: אין מסמך','error');return toast('אין עדיין מסמך ניהול הזמנות בענן. השתמש ב"הפעל ענן והעלה".')};localStorage.setItem(CLOUD_AUTO_KEY,'1');const restored=await restorePendingAgainstCloud(row);if(!restored){session.cloudConflictBlocked=false;applyOrderCloudState(row.state);session.cloudRevision=Number(row.revision||0);session.cloudUpdatedAt=row.updated_at||session.cloudUpdatedAt;session.lastCloudState=prepareCloudState(model.state);localStorage.setItem(CLOUD_BASE_KEY,JSON.stringify(session.lastCloudState));clearCloudPending();localSnapshot();try{if(files.dirHandle)await writeStateToFolder()}catch(localError){console.error('local backup/mirror',localError)}setCloud('ענן: מסונכרן','synced')}await syncSharedChecksFromCloud({quiet:true,required:true});closeModal();if(renderAfter)render();startPolling();if(!session.cloudConflictBlocked)toast(restored?'ניהול ההזמנות שוחזר וסונכרן':'ניהול ההזמנות נטען מהענן')}catch(e){console.error(e);toast('פתיחת הענן נכשלה: '+e.message);setCloud('ענן: שגיאה','error')}}

function logoutCloud(){saveSession(null);localStorage.removeItem(CLOUD_AUTO_KEY);session.cloudRevision=0;session.cloudUpdatedAt=null;checksSession.checksCloudRevision=0;checksSession.checksCloudUpdatedAt=null;session.cloudConflictBlocked=false;session.cloudSaveRequested=false;clearInterval(session.cloudPollTimer);setCloud('ענן: לא פעיל');renderSettings();toast('נותקת מהענן; העותק המקומי ונתוני השחזור נשמרו')}

return { loginModal, finishCloudLogin, enableCloud, openCloud, logoutCloud };
}
