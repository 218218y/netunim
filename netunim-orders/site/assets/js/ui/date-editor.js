import {esc} from '../core/values.js';
import {checkDateParts} from '../core/dates.js';

export function twoDigitDatePartsToIso(day,month,year){const dd=String(day??'').replace(/\D/g,''),mm=String(month??'').replace(/\D/g,''),yy=String(year??'').replace(/\D/g,'');if(!dd||!mm||yy.length!==2)return '';const d=Number(dd),m=Number(mm),y=2000+Number(yy),probe=new Date(y,m-1,d);if(d<1||m<1||m>12||probe.getFullYear()!==y||probe.getMonth()!==m-1||probe.getDate()!==d)return '';return `${y}-${String(m).padStart(2,'0')}-${String(d).padStart(2,'0')}`}

function safeDataAttributes(data={}){
  return Object.entries(data||{}).filter(([key])=>/^[a-z0-9-]+$/i.test(key)).map(([key,value])=>` data-${key}="${esc(value??'')}"`).join('');
}

// Dependencies are supplied by the composition root; this module has no startup side effects.
export function createUiDateEditor({markCheckSeriesManual, syncCheckSeriesFromFirst, toast}){
function dateEditorMarkup(id,value='',options={}){
  const p=checkDateParts(value),role=options.role||'',seriesField=options.seriesField||'',manual=options.manual?'1':'0',label=options.label||'תאריך',isCheck=options.check===true;
  const hiddenAttrs=`${options.change?` data-change="${esc(options.change)}"`:''}${safeDataAttributes(options.data)}`;
  return `<div class="date-editor check-date-editor${esc(options.series?' check-series-date':'')}${esc(options.compact?' date-editor-compact':'')}" data-date-editor ${isCheck?'data-check-date-editor ':''}data-date-role="${esc(role)}" data-date-label="${esc(label)}"><input class="date-part check-date-part" data-date-part="day" inputmode="numeric" maxlength="2" autocomplete="off" placeholder="יום" aria-label="${esc(label)}: יום" value="${esc(p.day)}" data-focus="select-input" data-input="handle-check-date-part-input" data-blur="handle-check-date-part-blur" data-keydown="handle-check-date-part-keydown"><span class="date-sep">/</span><input class="date-part check-date-part" data-date-part="month" inputmode="numeric" maxlength="2" autocomplete="off" placeholder="חודש" aria-label="${esc(label)}: חודש" value="${esc(p.month)}" data-focus="select-input" data-input="handle-check-date-part-input" data-blur="handle-check-date-part-blur" data-keydown="handle-check-date-part-keydown"><span class="date-sep">/</span><input class="date-part check-date-part" data-date-part="year" inputmode="numeric" maxlength="2" autocomplete="off" placeholder="שנה" aria-label="${esc(label)}: שנה דו ספרתית" value="${esc(p.year)}" data-focus="select-input" data-input="handle-check-date-part-input" data-blur="handle-check-date-part-blur" data-keydown="handle-check-date-part-keydown"><button type="button" class="date-picker-btn check-date-picker-btn" title="בחירה מלוח שנה" aria-label="${esc(label)}: בחירה מלוח שנה" data-action="open-check-date-picker">▦</button><input class="date-native-picker check-date-native-picker" data-date-picker type="date" tabindex="-1" aria-hidden="true" value="${esc(value||'')}" data-change="apply-check-date-picker"><input type="hidden" ${id?`id="${esc(id)}"`:''} data-date-value ${isCheck?'data-check-date-value ':''}${seriesField?`data-series-field="${esc(seriesField)}"`:''} data-manual="${esc(manual)}" value="${esc(value||'')}"${hiddenAttrs}></div>`;
}

function checkDateEditorMarkup(id,value='',options={}){return dateEditorMarkup(id,value,{...options,check:true})}

function dateEditorValue(editor){if(!editor)return {iso:'',complete:false,empty:true,valid:true};const day=(editor.querySelector('[data-date-part="day"]')?.value||'').replace(/\D/g,''),month=(editor.querySelector('[data-date-part="month"]')?.value||'').replace(/\D/g,''),year=(editor.querySelector('[data-date-part="year"]')?.value||'').replace(/\D/g,'');const empty=!day&&!month&&!year,complete=day.length>=1&&month.length>=1&&year.length===2;if(empty)return {iso:'',complete:false,empty:true,valid:true};if(!complete)return {iso:'',complete:false,empty:false,valid:false};const iso=twoDigitDatePartsToIso(day,month,year);return {iso,complete:true,empty:false,valid:!!iso}}
function checkDateEditorValue(editor){return dateEditorValue(editor)}

function notifyDateEditor(editor,changed){const hidden=editor?.querySelector('[data-date-value]');if(!hidden)return;const role=editor.dataset.dateRole||'';if(role==='series-first')syncCheckSeriesFromFirst();else if(role==='series-manual')markCheckSeriesManual(hidden);if(changed&&hidden.dataset.change)hidden.dispatchEvent(new Event('change',{bubbles:true}))}
function notifyCheckDateEditor(editor){notifyDateEditor(editor,false)}

function commitDateEditor(editor,notify=true){if(!editor)return false;const result=dateEditorValue(editor),hidden=editor.querySelector('[data-date-value]'),picker=editor.querySelector('[data-date-picker]'),previous=hidden?.value||'';const committed=result.empty?'':result.valid?result.iso:'';if(hidden)hidden.value=committed;if(picker)picker.value=committed;editor.classList.toggle('invalid',result.complete&&!result.valid);if(notify&&(result.empty||result.valid))notifyDateEditor(editor,previous!==committed);return result.valid}
function commitCheckDateEditor(editor,notify=true){return commitDateEditor(editor,notify)}

function setDateValue(target,value,notify=false){const hidden=target?.matches?.('[data-date-value]')?target:target?.querySelector?.('[data-date-value]'),editor=hidden?.closest('[data-date-editor]');if(!hidden||!editor)return;const previous=hidden.value||'',p=checkDateParts(value);editor.querySelector('[data-date-part="day"]').value=p.day;editor.querySelector('[data-date-part="month"]').value=p.month;editor.querySelector('[data-date-part="year"]').value=p.year;hidden.value=value||'';const picker=editor.querySelector('[data-date-picker]');if(picker)picker.value=value||'';editor.classList.remove('invalid');if(notify)notifyDateEditor(editor,previous!==(value||''))}
function setCheckDateValue(target,value,notify=false){setDateValue(target,value,notify)}

function nextPart(editor,part){return part==='day'?editor.querySelector('[data-date-part="month"]'):part==='month'?editor.querySelector('[data-date-part="year"]'):null}
function previousPart(editor,part){return part==='year'?editor.querySelector('[data-date-part="month"]'):part==='month'?editor.querySelector('[data-date-part="day"]'):null}
function handleCheckDatePartInput(input){const editor=input.closest('[data-date-editor]');input.value=input.value.replace(/\D/g,'').slice(0,2);const part=input.dataset.datePart;if((part==='day'||part==='month')&&input.value.length===2){const next=nextPart(editor,part);commitDateEditor(editor,true);next?.focus();next?.select();return}commitDateEditor(editor,true)}
function handleCheckDatePartBlur(input){const editor=input.closest('[data-date-editor]');if((input.dataset.datePart==='day'||input.dataset.datePart==='month')&&input.value.length===1)input.value=input.value.padStart(2,'0');commitDateEditor(editor,true)}
function handleCheckDatePartKeydown(event,input){const editor=input.closest('[data-date-editor]'),part=input.dataset.datePart;if(['/','-','.'].includes(event.key)){const next=nextPart(editor,part);if(next){event.preventDefault();if(input.value.length===1)input.value=input.value.padStart(2,'0');commitDateEditor(editor,true);next.focus();next.select()}return}if(event.key!=='Backspace'||input.value)return;const prev=previousPart(editor,part);if(prev){event.preventDefault();prev.focus();prev.select()}}

function openCheckDatePicker(button){const editor=button.closest('[data-date-editor]'),picker=editor?.querySelector('[data-date-picker]'),hidden=editor?.querySelector('[data-date-value]');if(!picker)return;if(hidden?.value)picker.value=hidden.value;try{if(typeof picker.showPicker==='function')picker.showPicker();else picker.click()}catch(e){picker.click()}}
function applyCheckDatePicker(picker){const editor=picker.closest('[data-date-editor]');setDateValue(editor,picker.value,true)}

function normalizeCheckModalDates(){const editors=[...document.querySelectorAll('#modal [data-check-date-editor]')];for(const editor of editors){for(const part of editor.querySelectorAll('[data-date-part="day"],[data-date-part="month"]'))if(part.value.length===1)part.value=part.value.padStart(2,'0');const result=dateEditorValue(editor);commitDateEditor(editor,false);if(!result.empty&&!result.valid){editor.classList.add('invalid');const label=editor.dataset.dateLabel||'תאריך';toast(`${label}: יש להזין יום, חודש ושתי ספרות שנה תקינים`);editor.querySelector(result.complete?'[data-date-part="day"]':'[data-date-part="year"]')?.focus();return false}}return true}

return {dateEditorMarkup,dateEditorValue,commitDateEditor,setDateValue,checkDateEditorMarkup,checkDateEditorValue,notifyCheckDateEditor,commitCheckDateEditor,setCheckDateValue,handleCheckDatePartInput,handleCheckDatePartBlur,handleCheckDatePartKeydown,openCheckDatePicker,applyCheckDatePicker,normalizeCheckModalDates};
}
