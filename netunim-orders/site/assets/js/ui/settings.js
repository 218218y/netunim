import {esc} from '../core/values.js';
import {DATA_FILE, $} from '../state/constants.js';

// Dependencies are supplied by the composition root; this module has no startup side effects.
export function createUiSettings({model, supplierUi, files, session, checksSession, mountViewLayout, orderedSuppliers, orderedInventoryCategoryNames, cloudEnabled}){
function renderSettings(){$('#main').innerHTML=`<section class="hero"><div><h1>הגדרות, ענן וגיבוי</h1><p>נתוני ההזמנות והקופה נשמרים במאגרים נפרדים; רשימת הצ'קים משותפת בין המערכות, ו„מאזן קופה נטו” נקרא מנתוני הקופה בענן לצורך תצוגה וחישוב בלבד.</p></div></section><div class="settings-grid">
<div class="settings-card"><h3>Supabase — מאגר נפרד</h3><div class="notice">ניהול ההזמנות עצמו: <b>order_management_documents</b> ו־<b>order_management_document_backups</b>, דרך <b>save_order_management_document</b>.<br>טאב הצ'קים קורא ומעדכן את <b>shared_checks_documents/main</b> דרך <b>save_shared_checks_document</b>; הצ'קים אינם נשמרים עוד כעותק פעיל במסמך ההזמנות או במסמך הקופה. כרטיס „מאזן קופה נטו” קורא את נתוני הקופה ואת הצ'קים המשותפים לחישוב בלבד ואינו מוסיף כתיבה שלהם מניהול ההזמנות.</div><p>לפני החיבור הראשון יש להריץ פעם אחת את <b>supabase/setup.sql</b> של ניהול ההזמנות. לפני מעבר הייצור יש להריץ את מיגרציית <b>supabase/shared/cutover.sql</b> המצורפת, ורק אחריה להעלות את שתי גרסאות האתר החדשות.</p><div class="settings-row"><button class="btn primary" data-action="enable-cloud">הפעל ענן והעלה</button><button class="btn" data-action="open-cloud">פתח מהענן</button><button class="btn danger" data-action="logout-cloud">התנתק</button></div><p>סטטוס: ${cloudEnabled()?'פעיל':'לא פעיל'} · Revision ניהול הזמנות: <b>${session.cloudRevision>0?'r'+session.cloudRevision:'טרם נטען'}</b> · Revision צ'קים משותפים: <b>${checksSession.checksCloudRevision>0?'r'+checksSession.checksCloudRevision:'טרם נטען'}</b></p></div>
<div class="settings-card supplier-settings-card"><h3>ניהול ספקים</h3><p>הוספה, עריכה וסידור ספקים מרוכזים כאן כדי להשאיר את כרטיס הספק פנוי לעבודה השוטפת.</p><div class="settings-row"><button class="btn primary" data-action="open-supplier-modal">+ הוספת ספק</button><button class="btn" data-action="open-supplier-order-modal">סידור ספקים</button></div><div class="supplier-edit-control"><label for="settingsSupplierEdit">בחירת ספק לעריכה</label><div class="supplier-edit-picker"><select id="settingsSupplierEdit" aria-label="בחר ספק לעריכה">${orderedSuppliers().map(sp=>`<option value="${esc(sp.id)}" ${sp.id===supplierUi.currentSupplierId?'selected':''}>${esc(sp.name)}</option>`).join('')}</select><button class="btn" data-action="open-selected-supplier-editor">עריכת ספק</button></div></div><p>${esc(model.state.suppliers.length)} ספקים פעילים · הסדר בתפריט הספקים נשמר לפי הסדר שהוגדר ב„סידור ספקים”.</p></div>
<div class="settings-card"><h3>סידור רשימת המחסן</h3><p>קובע את סדר קטגוריות המלאי בתצוגת המחסן. הסדר נשמר גם בענן ובגיבויים; קטגוריות חדשות מצטרפות בלי לשבור את הסדר הקיים.</p><div class="settings-row"><button class="btn" data-action="open-inventory-category-order-modal">סידור קטגוריות במחסן</button></div><p>${orderedInventoryCategoryNames().length} קטגוריות פעילות · “אביזרים” מוגדר כברירת מחדל בסוף הרשימה.</p></div>
<div class="settings-card"><h3>שמירה מקומית וגיבויים</h3><p>כל שינוי נשמר קודם בדפדפן ומגובה גם ב־IndexedDB, והענן פועל בנפרד. תיקיית המחשב היא שכבת גיבוי נוספת בלבד: אם הדפדפן דורש אישור מחדש, העבודה והשמירה בדפדפן ובענן ממשיכות ללא תלות בה. קובץ העבודה נכתב אל <b>data/${esc(DATA_FILE)}</b>; גיבוי אוטומטי בתיקיית <b>backups</b> נוצר לכל היותר פעם ב־12 שעות ורק כשהנתונים השתנו.</p><div class="settings-row"><button class="btn" data-action="choose-folder">${files.dirHandle?'החלף תיקייה':'חבר תיקייה'}</button>${files.dirHandle&&files.dirPermission!=='granted'?'<button class="btn primary" data-action="activate-saved-folder">אשר גישה מחדש</button>':''}<button class="btn" data-action="backup-to-folder">גיבוי לתיקייה</button><button class="btn" data-action="export-json">ייצא JSON</button><button class="btn" data-action="begin-json-restore">שחזר מ־JSON</button><button class="btn" data-action="export-csv">ייצא CSV</button></div><p>${files.dirHandle?`תיקייה זכורה: ${esc(files.dirHandle.name)} · ${files.dirPermission==='granted'&&!files.folderLastError?'גישה פעילה':files.folderLastError?`שגיאת גיבוי: ${esc(files.folderLastError)}`:'ממתינה לאישור דפדפן — אין השפעה על שמירת הדפדפן והענן'}`:'לא חוברה תיקייה; השמירה בדפדפן ובענן ממשיכה כרגיל.'}</p></div></div>`;mountViewLayout({headCount:1,className:'settings-view-shell',scrollKey:'settings'})}

return { renderSettings };
}
