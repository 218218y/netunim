# Shared Checks / Supabase release — 2026-09-06

Production database upgraded and normalized after the user's instruction to finish
the upgrade. The original schema and full seven-row migration ledger remain preserved
in `audit/production-schema.json` and `audit/original-full-migration-history.json`.

| Active Production version | Change | Execution |
|---|---|---|
| 20260906200304 | production_schema_baseline | Metadata marker only; no baseline DDL executed on Production |
| 20260906203818 | capabilities_security_invoker | Applied by Supabase migration API |
| 20260906203827 | restore_group_qualified_owner_updates | Applied by Supabase migration API |
| 20260906203837 | oauth_owner_index | Applied by Supabase migration API |

The baseline ID was generated by Supabase CLI; upgrade IDs were generated by the
server and the local filenames were aligned to those returned versions. The exact
normalized file hashes are in `audit/applied-migration-manifest.json`. Active local
versions/names match `list_migrations` exactly. Original ledger SQL and all nullable
metadata fields were exported before the guarded metadata normalization; stale or
duplicate normalization attempts fail atomically in tests.

The baseline's full SQL was then stored as a **text statement in migration metadata**,
without executing that statement. All four server-recorded SQL SHA-256 hashes match
the canonical repository files; see `audit/production-migration-statement-hashes.json`.
This retains the baseline SQL for history-based reconstruction as well as CLI rebuilds.

The final Production catalog matches a clean install of all four migrations. The
only changes from the old baseline are the two reviewed function definitions/security
settings and the OAuth owner index. No checks, transactions, backup rows or sequence
current values were updated, deleted, copied or reset by this release. Function DDL
changes future calls; no business/restore RPC was exercised against Production.

The capability RPC was verified live in a read-only transaction with role
`authenticated`; it still reports ledger v3, delete intents v4, sync integrity/restore
groups/shared-check integrity v5 and finance fencing v1.

Post-upgrade Advisor evidence is `audit/security-advisor-upgraded.json`. The capability
DEFINER warning disappeared. Ten public DEFINER warnings remain intentional, with
individual justifications and actual two-user authorization regression tests in
`REVIEW.md` / `tests/supabase_authorization.py`. The three RLS/no-policy INFO entries
are service/internal tables. The separate Auth leaked-password-protection WARN is
outside this schema upgrade and remains documented; Auth settings were not changed.

Validation completed:

- Full application verification, including real browser runtime, PWA, financial,
  multitab and two-computer recovery suites; module, worker and sync-assets contracts.
- Deterministic Shared Checks race suite repeated 100 times in fresh Node processes.
- Historical baseline clean install parity and full upgraded migration-chain parity.
- Real local PostgreSQL two-user, null-UID, anon, restore-with/without-checks, restore
  replay, borrowed finance fence, expiry, takeover and wrong-epoch tests.
- Live deployment postflight executable tested against a disposable database: schema
  drift and ledger drift both block; connector evidence also rejects stale captures,
  wrong projects and different builds.
- Fresh Production catalogs and ledger compared with reviewed expectations.

Test database platform limitation: local PostgreSQL 18 uses minimal Supabase auth/role
fixtures and a pg_cron catalog stub. Application parity normalizes only the redundant
PostgreSQL 18 NOT NULL catalog representation; actual column nullability is compared.
Platform extension lifecycle and execution of scheduled retention are not simulated.

Both client applications were published to Cloudflare Pages Production / `main`:

| App | Production | Deployment |
|---|---|---|
| Orders | https://bargig-orders.pages.dev | `9b8e6350-6a67-47c9-853e-837770266f1c` |
| Kupa | https://bargig-kupa.pages.dev | `4a5a23de-885d-4d9b-b41c-b36c5f301458` |

The existing deployment engine ran all static guards and the fresh connector-backed
Supabase postflight before each upload, following the successful full repository
verification. Wrangler 4.129.0 reported success for both; a separate authenticated
Cloudflare deployment listing confirmed those exact IDs in Production. Node used the
Windows system CA trust store (`NODE_USE_SYSTEM_CA=1`); TLS verification stayed enabled.

The final race suite contains **28 tests × 100 consecutive runs**, including mutations
at the final guard continuation. It uses gates/microtasks, never sleeps. The full
browser verification passed after the final public asset changes.

Fresh Supabase postflight at **2026-09-06 20:56:57 UTC** passed schema, canonical
migration versions/names and recorded SQL hash comparisons. Static release contracts
also verify that every remaining RPC Advisor warning has reviewed authorization
coverage and that local filenames/hashes agree with Production.

**Live HTTP limitation:** direct requests for the new helper, sync/controller modules
and service workers on both Production hosts returned `418: Blocked by NetFree` from
this environment. Therefore served-byte/browser smoke verification on the live hosts
could not be certified here. Deployment completion is confirmed by Cloudflare; the
real browser/PWA/financial tests were run locally with isolated fixtures. No network
filter or TLS safety checks were disabled to work around that restriction.

## 2026-09-07 — canonical retention Cron followup

Applied **20260907035930_retention_cron_jobs.sql**, timestamp generated by Supabase
at 03:59:30 UTC. The CLI-created draft was renamed to the returned server version;
no version was fabricated and no history repair was used. Its server-recorded SQL
SHA-256 is `313c57a668cf3a7a6986382859b4ea6e75ed833980d61f58f434b71031263c98`.

The first four migrations omitted extension/scheduling metadata, even though both
prune functions and the two jobs already existed in Production. The fifth migration
makes that operational infrastructure reproducible. The postflight now verifies
pg_cron, scheduler settings, full catalog visibility, exact job definitions and
uniqueness, in addition to schema, grants and migration history/SQL hashes.
Canonical expectations are kept separately from independently captured server
evidence. See [REPRODUCIBILITY.md](REPRODUCIBILITY.md) for the reconstruction and
historical normalization audit; no SQL from this release note is an installation step.

Final authenticated read-only postflight at **2026-09-07 04:02:31 UTC: PASS**.
No application schema drift: 20 tables, 168 columns, 38 indexes, 59 functions and
39 policies, with constraints, ACLs, triggers and security modes also compared.
All five migration versions/names and server SQL hashes match canonical files.
All four previous ledger entries/hashes are unchanged.

| Job | ID | Schedule (GMT) | Active |
|---|---:|---|---|
| `netunim-document-backup-retention-daily` | 2 | `43 3 * * *` | true |
| `netunim-sync-ledger-retention-weekly` | 1 | `17 3 * * 0` | true |

Commands remain exactly `select * from netunim_internal.prune_document_backups();`
and `select * from netunim_internal.prune_sync_operation_ledgers();`, respectively.
Both run as postgres in database postgres, localhost:5432. Extension pg_cron 1.6.4
is present; scheduler enabled in GMT. There are exactly two retention jobs, with
no duplicates. **Every job field, including IDs, is identical before and after.**

No business data was changed/deleted by this release. Neither prune function was
invoked by this work. No existing migration metadata, function security mode or
grants were rewritten. The initial deployment attempt rolled back on Supabase's
extension-table lock restriction, without a ledger entry or job changes. The final
migration uses an advisory transaction lock; a new regression reproduces that
restricted postgres role and proves safe concurrent reruns without extra grants.

Validation passed:

- All 27 full verification suites, including both sites' browser/runtime, modules,
  service-worker/sync-assets, bank bridge/models, PWA/offline/recovery, multitab,
  two-computer conflicts/disjoint merges and financial scenarios. The entire suite
  was rerun after the final locking correction and before successful deployment.
- Shared Checks races: 100 consecutive deterministic runs, no test sleeps.
- Canonical manifest/version/hash contracts and historical baseline reconstruction.
- Full five-migration clean replay and live/capture postflight rejection tests,
  including final server-generated filenames/history after deployment.
- Missing/renamed/changed/disabled/duplicate jobs, wrong owner/database/host/port,
  missing extension, partial visibility and disabled/changed-timezone scheduler.
- Rerun ID preservation, concurrent reruns, in-place correction, duplicate/foreign
  ownership rejection, and traps proving installation does not call prune functions.
- All 13 reviewed RPC/helper authorizations with two users, null UID, anon,
  owner isolation and finance fences; guarded historical normalization regression.

The ten intentional SECURITY DEFINER warnings remain unchanged and covered; see
[REVIEW.md](REVIEW.md) and the [Advisor's function-execution guidance](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable).
The separate existing [Auth leaked-password protection warning](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection)
also remains outside this change. Exact fresh notices are in the audit JSON below.

Both Production sites passed the strengthened gate and were deployed on `main`:

| App | Deployment | Independent Cloudflare listing |
|---|---|---|
| Orders | `906b03a2-acfd-4c03-a236-5eb93de0a3c1` | Production/main confirmed |
| Kupa | `4e5c5aa3-f6b6-4921-8d99-0f8141a484ef` | Production/main confirmed |

No application asset edits were needed. Wrangler confirmed all 123 Orders files
and 103 Kupa files already uploaded. Direct public HTTP verification again returned
NetFree HTTP 418 for both service workers; live-host served-byte smoke is therefore
not certified. Browser/runtime verification passed against isolated local fixtures.

Clean-install scope: PostgreSQL 18 with Supabase auth/role fixtures and an explicit
cron API/catalog model. This workstation lacks native pg_cron, Docker, and working
WSL (HCS_E_SERVICE_NOT_AVAILABLE). The actual postflight rejects the model's absent
extension; synthetic positive captures exist only inside disposable tests. Real
pg_cron was verified on Production, but a new native Supabase/pg_cron environment
was not certified. The canonical chain covers the application schema and retention
jobs, given documented managed-platform prerequisites; it is not a backup of Auth
settings, secrets, platform binaries, business data or historical job runs.

Files changed/added in this followup:

- `supabase/migrations/20260907035930_retention_cron_jobs.sql`
- `supabase/retention-jobs.json`
- `supabase/schema_inventory.sql`
- `supabase/postflight-target.json`
- `supabase/canonical-migrations.json`
- `supabase/canonical-migration-manifest.json`
- `tools/supabase_postflight.py`
- `tools/supabase_migration_manifest.py`
- `tools/supabase_capture_query.py`
- `tests/supabase_retention.py`
- `tests/isolated_sync_postgres.py`
- `tests/supabase_baseline.py`
- `tests/supabase_contracts.py`
- `tests/run_all.py`
- `supabase/REPRODUCIBILITY.md`
- `supabase/REVIEW.md`
- `supabase/RELEASE.md`
- `supabase/audit/candidate-schema.json` (now includes explicit modeled operations)
- `supabase/audit/retention-production-before.json`
- `supabase/audit/retention-production-after.json`
- `supabase/audit/retention-security-advisor.json`
- `supabase/audit/retention-release-verification.json`

The final [machine-readable audit](audit/retention-release-verification.json)
contains actual jobs, versions/hashes, catalog comparison, test scope, deployment
IDs and verification limits. Historical baseline and original ledger exports remain
immutable. The latest full capture was compared against the reviewed upgraded schema and
against pre-migration catalogs; unexplained schema drift is empty.
