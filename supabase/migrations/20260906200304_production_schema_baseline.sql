-- GENERATED from audit/production-schema.json; review before use.
-- Fresh install only. Existing Production: repair history AFTER review; never replay this DDL.
-- No business data or sequence current values are included.
BEGIN;
SET LOCAL check_function_bodies = false;
CREATE SCHEMA IF NOT EXISTS extensions;
CREATE EXTENSION IF NOT EXISTS pgcrypto WITH SCHEMA extensions;
CREATE SCHEMA IF NOT EXISTS "netunim_internal";
ALTER SCHEMA "netunim_internal" OWNER TO "postgres";
CREATE SCHEMA IF NOT EXISTS "public";
ALTER SCHEMA "public" OWNER TO "pg_database_owner";
CREATE SEQUENCE "public"."shared_financial_event_seq" AS bigint;
CREATE TABLE "netunim_internal"."bank_alert_rollout_baselines" (
  "owner_id" uuid NOT NULL,
  "alert_kind" text NOT NULL,
  "baseline_at" timestamp with time zone NOT NULL
);
ALTER TABLE "netunim_internal"."bank_alert_rollout_baselines" OWNER TO "postgres";
CREATE TABLE "netunim_internal"."bank_sync_operations" (
  "owner_id" uuid NOT NULL,
  "document_name" text NOT NULL,
  "operation_id" text NOT NULL,
  "snapshot_seq" bigint NOT NULL,
  "payload_sha256" text NOT NULL,
  "created_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "netunim_internal"."bank_sync_operations" OWNER TO "postgres";
CREATE TABLE "netunim_internal"."document_sync_operations" (
  "owner_id" uuid NOT NULL,
  "domain" text NOT NULL,
  "document_name" text NOT NULL,
  "operation_id" text NOT NULL,
  "payload_sha256" text NOT NULL,
  "applied_revision" bigint NOT NULL,
  "created_at" timestamp with time zone DEFAULT now() NOT NULL,
  "client_instance_id" text,
  "app_site" text,
  "build_version" text,
  "mutation_type" text,
  "surface" text,
  "base_revision" bigint,
  "before_counts" jsonb,
  "after_counts" jsonb,
  "delete_count" integer,
  "restore_group_id" uuid,
  "audit_timestamp" timestamp with time zone
);
ALTER TABLE "netunim_internal"."document_sync_operations" OWNER TO "postgres";
CREATE TABLE "netunim_internal"."restore_operation_groups" (
  "owner_id" uuid NOT NULL,
  "restore_group_id" uuid NOT NULL,
  "app_site" text NOT NULL,
  "main_document_name" text NOT NULL,
  "main_base_revision" bigint NOT NULL,
  "checks_document_name" text DEFAULT 'main'::text NOT NULL,
  "checks_base_revision" bigint,
  "main_state" jsonb NOT NULL,
  "checks_state" jsonb,
  "main_delete_intents" jsonb DEFAULT '{}'::jsonb NOT NULL,
  "checks_delete_ids" jsonb DEFAULT '[]'::jsonb NOT NULL,
  "main_payload_sha256" text NOT NULL,
  "checks_payload_sha256" text,
  "main_operation_id" text NOT NULL,
  "checks_operation_id" text,
  "phase" text NOT NULL,
  "audit" jsonb DEFAULT '{}'::jsonb NOT NULL,
  "created_at" timestamp with time zone DEFAULT now() NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL,
  "completed_at" timestamp with time zone
);
ALTER TABLE "netunim_internal"."restore_operation_groups" OWNER TO "postgres";
CREATE TABLE "netunim_internal"."safety_snapshots" (
  "owner_id" uuid NOT NULL,
  "domain" text NOT NULL,
  "document_name" text NOT NULL,
  "operation_id" text NOT NULL,
  "restore_group_id" uuid,
  "revision" bigint NOT NULL,
  "state" jsonb NOT NULL,
  "operation_kind" text NOT NULL,
  "created_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "netunim_internal"."safety_snapshots" OWNER TO "postgres";
CREATE TABLE "public"."bank_transaction_snapshots" (
  "owner_id" uuid NOT NULL,
  "account_key" text NOT NULL,
  "account_role" text NOT NULL,
  "snapshot_at" timestamp with time zone NOT NULL,
  "coverage_from" date NOT NULL,
  "coverage_to" date NOT NULL,
  "transaction_count" integer NOT NULL,
  "transactions" jsonb NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."bank_transaction_snapshots" OWNER TO "postgres";
CREATE TABLE "public"."bank_transactions" (
  "id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "owner_id" uuid NOT NULL,
  "account_key" text NOT NULL,
  "account_role" text NOT NULL,
  "merge_key" text NOT NULL,
  "transaction_date" timestamp with time zone,
  "processed_date" timestamp with time zone,
  "amount" numeric NOT NULL,
  "currency" text DEFAULT 'ILS'::text NOT NULL,
  "description" text DEFAULT ''::text NOT NULL,
  "memo" text DEFAULT ''::text NOT NULL,
  "party_name" text DEFAULT ''::text NOT NULL,
  "party_headline" text DEFAULT ''::text NOT NULL,
  "message_headline" text DEFAULT ''::text NOT NULL,
  "message_detail" text DEFAULT ''::text NOT NULL,
  "status" text DEFAULT 'completed'::text NOT NULL,
  "balance_after" numeric,
  "bank_reference" text DEFAULT ''::text NOT NULL,
  "bank_serial" text DEFAULT ''::text NOT NULL,
  "activity_type_code" integer,
  "cheque" boolean DEFAULT false NOT NULL,
  "check_details" jsonb,
  "first_seen_at" timestamp with time zone DEFAULT now() NOT NULL,
  "last_changed_at" timestamp with time zone DEFAULT now() NOT NULL,
  "presence_state" text DEFAULT 'unknown'::text NOT NULL,
  "last_seen_at" timestamp with time zone,
  "missing_since" timestamp with time zone,
  "missing_acknowledged_at" timestamp with time zone,
  "alert_acknowledgements" jsonb DEFAULT '{}'::jsonb NOT NULL
);
ALTER TABLE "public"."bank_transactions" OWNER TO "postgres";
CREATE TABLE "public"."finance_sync_documents" (
  "owner_id" uuid NOT NULL,
  "document_name" text DEFAULT 'main'::text NOT NULL,
  "revision" bigint DEFAULT 1 NOT NULL,
  "state" jsonb DEFAULT '{"bank": null, "creditSync": null}'::jsonb NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."finance_sync_documents" OWNER TO "postgres";
CREATE TABLE "public"."finance_sync_leases" (
  "owner_id" uuid NOT NULL,
  "lease_name" text NOT NULL,
  "lease_token" text NOT NULL,
  "leased_until" timestamp with time zone NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL,
  "fence_epoch" bigint DEFAULT 1 NOT NULL
);
ALTER TABLE "public"."finance_sync_leases" OWNER TO "postgres";
CREATE TABLE "public"."google_calendar_connections" (
  "owner_id" uuid NOT NULL,
  "google_account_id" text NOT NULL,
  "refresh_token" text NOT NULL,
  "scope" text DEFAULT ''::text NOT NULL,
  "created_at" timestamp with time zone DEFAULT now() NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."google_calendar_connections" OWNER TO "postgres";
CREATE TABLE "public"."google_calendar_oauth_states" (
  "state_hash" text NOT NULL,
  "owner_id" uuid NOT NULL,
  "return_url" text NOT NULL,
  "expires_at" timestamp with time zone NOT NULL,
  "created_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."google_calendar_oauth_states" OWNER TO "postgres";
CREATE TABLE "public"."kupa_document_backups" (
  "id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "owner_id" uuid NOT NULL,
  "document_name" text NOT NULL,
  "revision" bigint NOT NULL,
  "state" jsonb NOT NULL,
  "saved_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."kupa_document_backups" OWNER TO "postgres";
CREATE TABLE "public"."kupa_documents" (
  "owner_id" uuid NOT NULL,
  "document_name" text DEFAULT 'main'::text NOT NULL,
  "revision" bigint DEFAULT 1 NOT NULL,
  "state" jsonb NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL,
  "core_updated_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."kupa_documents" OWNER TO "postgres";
CREATE TABLE "public"."kupa_periodic_backups" (
  "id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "owner_id" uuid NOT NULL,
  "document_name" text NOT NULL,
  "revision" bigint NOT NULL,
  "state" jsonb NOT NULL,
  "saved_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."kupa_periodic_backups" OWNER TO "postgres";
CREATE TABLE "public"."order_management_document_backups" (
  "id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "owner_id" uuid NOT NULL,
  "document_name" text NOT NULL,
  "revision" bigint NOT NULL,
  "state" jsonb NOT NULL,
  "saved_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."order_management_document_backups" OWNER TO "postgres";
CREATE TABLE "public"."order_management_documents" (
  "owner_id" uuid NOT NULL,
  "document_name" text DEFAULT 'suppliers'::text NOT NULL,
  "revision" bigint DEFAULT 1 NOT NULL,
  "state" jsonb NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."order_management_documents" OWNER TO "postgres";
CREATE TABLE "public"."order_management_periodic_backups" (
  "id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "owner_id" uuid NOT NULL,
  "document_name" text NOT NULL,
  "revision" bigint NOT NULL,
  "state" jsonb NOT NULL,
  "saved_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."order_management_periodic_backups" OWNER TO "postgres";
CREATE TABLE "public"."shared_checks_document_backups" (
  "id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "owner_id" uuid NOT NULL,
  "document_name" text NOT NULL,
  "revision" bigint NOT NULL,
  "state" jsonb NOT NULL,
  "saved_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."shared_checks_document_backups" OWNER TO "postgres";
CREATE TABLE "public"."shared_checks_documents" (
  "owner_id" uuid NOT NULL,
  "document_name" text DEFAULT 'main'::text NOT NULL,
  "revision" bigint DEFAULT 1 NOT NULL,
  "state" jsonb NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."shared_checks_documents" OWNER TO "postgres";
CREATE TABLE "public"."shared_checks_periodic_backups" (
  "id" bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  "owner_id" uuid NOT NULL,
  "document_name" text NOT NULL,
  "revision" bigint NOT NULL,
  "state" jsonb NOT NULL,
  "saved_at" timestamp with time zone DEFAULT now() NOT NULL
);
ALTER TABLE "public"."shared_checks_periodic_backups" OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.assert_document_invariants(p_domain text, p_state jsonb)
 RETURNS void
 LANGUAGE plpgsql
 IMMUTABLE
 SET search_path TO 'pg_catalog', 'netunim_internal'
AS $function$
declare v_path text;
begin
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then
    raise exception 'invalid_document_state' using errcode='22023',detail=p_domain;
  end if;
  if p_domain='orders' then
    if p_state?'checks' then raise exception 'orders_state_contains_checks' using errcode='22023'; end if;
    foreach v_path in array array[
      'suppliers','transactions','customerDebts','customerOrders',
      'serviceCalls','notes','inventoryItems','inventoryEvents','warehouseOrders'
    ] loop perform netunim_internal.assert_entity_array_ids(p_state,string_to_array(v_path,'.'),true);end loop;
    if jsonb_typeof(p_state->'inventoryCategoryOrder') is distinct from 'array' then raise exception 'invalid_order_configuration' using errcode='22023';end if;
  elsif p_domain='kupa' then
    if p_state?'checks' then raise exception 'kupa_state_contains_checks' using errcode='22023'; end if;
    foreach v_path in array array[
      'credits','cash','rights','notes','expenses','cards','notesSheet.rows','notesSheet.columns'
    ] loop perform netunim_internal.assert_entity_array_ids(p_state,string_to_array(v_path,'.'),true);end loop;
    if jsonb_typeof(p_state->'bank') is distinct from 'object'
       or jsonb_typeof(p_state#>'{bank,adjustments}') is distinct from 'array' then
      raise exception 'invalid_kupa_bank_state' using errcode='22023';
    end if;
  elsif p_domain='shared-checks' then
    perform netunim_internal.assert_entity_array_ids(p_state,array['checks'],true);
    if jsonb_typeof(p_state->'bankEvents') is distinct from 'array' then raise exception 'invalid_shared_checks_events' using errcode='22023';end if;
  else raise exception 'invalid_document_domain' using errcode='22023',detail=p_domain;
  end if;
end
$function$;
ALTER FUNCTION "netunim_internal"."assert_document_invariants"(p_domain text, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.assert_entity_array_ids(p_state jsonb, p_path text[], p_required boolean DEFAULT true)
 RETURNS void
 LANGUAGE plpgsql
 IMMUTABLE
 SET search_path TO 'pg_catalog'
AS $function$
declare v_rows jsonb;v_path text:=array_to_string(p_path,'.');
begin
  v_rows:=p_state#>p_path;
  if v_rows is null and not p_required then return; end if;
  if jsonb_typeof(v_rows) is distinct from 'array' then
    raise exception 'entity_collection_required' using errcode='22023',detail=v_path;
  end if;
  if exists(select 1 from jsonb_array_elements(v_rows) r(value)
    where jsonb_typeof(r.value) is distinct from 'object'
       or jsonb_typeof(r.value->'id') is distinct from 'string'
       or nullif(btrim(r.value->>'id'),'') is null
       or r.value->>'id' is distinct from btrim(r.value->>'id')) then
    raise exception 'entity_id_missing_or_blank' using errcode='22023',detail=v_path;
  end if;
  if exists(select 1 from (
    select btrim(r.value->>'id') id,count(*) n from jsonb_array_elements(v_rows) r(value)
    group by btrim(r.value->>'id') having count(*)>1
  ) duplicates) then
    raise exception 'duplicate_entity_id' using errcode='22023',detail=v_path;
  end if;
end
$function$;
ALTER FUNCTION "netunim_internal"."assert_entity_array_ids"(p_state jsonb, p_path text[], p_required boolean) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.assert_finance_sync_fence(p_lease_name text, p_lease_token text, p_fence_epoch bigint)
 RETURNS void
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare l public.finance_sync_leases%rowtype;v_owner uuid:=auth.uid();
begin
 if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
 -- Keep the row locked until the publication transaction commits. Recheck time after the lock.
 select * into l from public.finance_sync_leases where owner_id=v_owner and lease_name=p_lease_name for update;
 if not found or p_lease_token is distinct from l.lease_token or p_fence_epoch is distinct from l.fence_epoch or l.leased_until<=clock_timestamp() then
  raise exception 'stale_finance_sync_fence' using errcode='PT409';
 end if;
end $function$;
ALTER FUNCTION "netunim_internal"."assert_finance_sync_fence"(p_lease_name text, p_lease_token text, p_fence_epoch bigint) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.canonical_delete_intents(p_intents jsonb, p_allowed text[])
 RETURNS jsonb
 LANGUAGE plpgsql
 IMMUTABLE
 SET search_path TO 'pg_catalog'
AS $function$
declare
  v_key text;
  v_value jsonb;
  v_clean jsonb;
  v_out jsonb := '{}'::jsonb;
begin
  if p_intents is null then return v_out; end if;
  if jsonb_typeof(p_intents) is distinct from 'object' then
    raise exception 'invalid_delete_intents' using errcode='22023';
  end if;
  for v_key,v_value in select key,value from jsonb_each(p_intents) loop
    if not (v_key = any(p_allowed)) then
      raise exception 'invalid_delete_intent_domain' using errcode='22023', detail=v_key;
    end if;
    if jsonb_typeof(v_value) is distinct from 'array' then
      raise exception 'invalid_delete_intent_ids' using errcode='22023', detail=v_key;
    end if;
    if exists (
      select 1 from jsonb_array_elements(v_value) j(value)
      where jsonb_typeof(j.value) is distinct from 'string'
         or nullif(btrim(j.value #>> '{}'),'') is null
    ) then
      raise exception 'invalid_delete_intent_ids' using errcode='22023', detail=v_key;
    end if;
    select coalesce(jsonb_agg(id order by id),'[]'::jsonb)
      into v_clean
    from (select distinct btrim(j.value #>> '{}') id from jsonb_array_elements(v_value) j(value)) q;
    if jsonb_array_length(v_clean) <> jsonb_array_length(v_value) then
      raise exception 'duplicate_delete_intent_id' using errcode='22023', detail=v_key;
    end if;
    if jsonb_array_length(v_clean)>0 then v_out:=v_out||jsonb_build_object(v_key,v_clean); end if;
  end loop;
  return v_out;
end;
$function$;
ALTER FUNCTION "netunim_internal"."canonical_delete_intents"(p_intents jsonb, p_allowed text[]) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.capture_safety_snapshot(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_kind text, p_restore_group_id uuid DEFAULT NULL::uuid)
 RETURNS void
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_revision bigint;v_state jsonb;
begin
  if auth.uid() is null or p_owner is distinct from auth.uid() then raise exception 'snapshot_owner_mismatch' using errcode='42501';end if;
  if p_domain='orders' then select revision,state into v_revision,v_state from public.order_management_documents where owner_id=p_owner and document_name=p_document_name;
  elsif p_domain='kupa' then select revision,state into v_revision,v_state from public.kupa_documents where owner_id=p_owner and document_name=p_document_name;
  elsif p_domain='shared-checks' then select revision,state into v_revision,v_state from public.shared_checks_documents where owner_id=p_owner and document_name=p_document_name;
  else raise exception 'invalid_document_domain' using errcode='22023';end if;
  if v_revision is not null then insert into netunim_internal.safety_snapshots(owner_id,domain,document_name,operation_id,restore_group_id,revision,state,operation_kind)
    values(p_owner,p_domain,p_document_name,p_operation_id,p_restore_group_id,v_revision,v_state,p_kind) on conflict do nothing;end if;
end
$function$;
ALTER FUNCTION "netunim_internal"."capture_safety_snapshot"(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_kind text, p_restore_group_id uuid) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.document_invariant_guard()
 RETURNS trigger
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'netunim_internal'
AS $function$
declare v_domain text;
begin
  v_domain:=case tg_table_name
    when 'order_management_documents' then 'orders'
    when 'kupa_documents' then 'kupa'
    when 'shared_checks_documents' then 'shared-checks' end;
  if v_domain is null then raise exception 'unknown_invariant_table' using errcode='22023';end if;
  perform netunim_internal.assert_document_invariants(v_domain,new.state);return new;
end
$function$;
ALTER FUNCTION "netunim_internal"."document_invariant_guard"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.fenced_impl_merge_bank_transactions(p_account_key text, p_account_role text, p_transactions jsonb)
 RETURNS TABLE(inserted_count integer, updated_count integer, total_count integer)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  begin
    return query select * from netunim_internal.merge_bank_transactions(p_account_key,p_account_role,p_transactions);
  exception when lock_not_available then
    raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.';
  end;
end $function$;
ALTER FUNCTION "netunim_internal"."fenced_impl_merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.fenced_impl_save_bank_sync_snapshot(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint)
 RETURNS TABLE(finance_revision bigint, kupa_revision bigint, updated_at timestamp with time zone)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  begin
    return query select * from netunim_internal.save_bank_sync_snapshot(p_document_name,p_bank_state,p_snapshot_token,p_snapshot_seq);
  exception when lock_not_available then
    raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.';
  end;
end $function$;
ALTER FUNCTION "netunim_internal"."fenced_impl_save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.fenced_impl_save_finance_sync_document_v5(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();v_before jsonb;v_result record;v_paths text[]:=array[]::text[];
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_finance_state' using errcode='22023';end if;
  select d.state into v_before from public.finance_sync_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  select * into v_result from public.save_finance_sync_document_v3(p_document_name,p_expected_revision,p_state,p_operation_id);
  perform netunim_internal.record_operation_audit(v_owner,'finance',p_document_name,p_operation_id,coalesce(p_audit,'{}'::jsonb)||jsonb_build_object('baseRevision',p_expected_revision),coalesce(v_before,p_state),v_result.state,v_paths,null);
  return query select v_result.revision,v_result.updated_at,v_result.state,v_result.operation_replayed,v_result.operation_revision;
end
$function$;
ALTER FUNCTION "netunim_internal"."fenced_impl_save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.fenced_impl_sync_bank_transactions_snapshot(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean)
 RETURNS TABLE(inserted_count integer, updated_count integer, total_count integer, missing_count integer, active_missing_count integer, snapshot_stored boolean, baseline_created boolean)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_owner uuid:=auth.uid();
  v_inserted integer:=0;
  v_updated integer:=0;
  v_total integer:=0;
  v_previous_snapshot_at timestamptz;
  v_had_complete_snapshot boolean:=false;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if coalesce(btrim(p_account_key),'')='' or p_account_role not in ('business','home')
     or jsonb_typeof(p_transactions) is distinct from 'array'
     or p_snapshot_at is null then
    raise exception 'invalid_bank_snapshot_input' using errcode='22023';
  end if;
  if coalesce(p_complete,false) and (p_coverage_from is null or p_coverage_to is null or p_coverage_to<p_coverage_from) then
    raise exception 'invalid_bank_snapshot_coverage' using errcode='22023';
  end if;

  -- The public merge boundary owns the shared per-user advisory transaction lock both before and
  -- after core contention hardening. Transaction-scoped advisory locks remain held for this whole
  -- RPC, so merge + reconciliation + snapshot are atomic; any later exception rolls all of it back.
  select m.inserted_count,m.updated_count,m.total_count
    into v_inserted,v_updated,v_total
  from netunim_internal.fenced_impl_merge_bank_transactions(p_account_key,p_account_role,p_transactions) m;

  select s.snapshot_at into v_previous_snapshot_at
  from public.bank_transaction_snapshots s
  where s.owner_id=v_owner and s.account_key=p_account_key and s.account_role=p_account_role
  for update;
  v_had_complete_snapshot:=v_previous_snapshot_at is not null;

  -- A late client cannot mutate presence state after a newer proven-complete bank snapshot. This
  -- applies even to an incomplete read: a stale partial response must not resurrect a row that a
  -- newer complete snapshot already proved missing. Throwing rolls the merge back atomically too.
  if v_previous_snapshot_at is not null and p_snapshot_at<v_previous_snapshot_at then
    raise exception 'bank_snapshot_stale'
      using errcode='40001',hint='A newer complete bank snapshot is already stored. The stale snapshot was not applied.';
  end if;

  -- Rows actually returned by the bank are present. This is safe even for a partial read; absence
  -- is the only fact that is forbidden unless the connector proved the whole requested range.
  -- Reappearance also clears an old acknowledgement so a later disappearance becomes a new incident.
  update public.bank_transactions b set
    presence_state='present',
    last_seen_at=p_snapshot_at,
    missing_since=null,
    missing_acknowledged_at=null
  where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role
    and exists (
      select 1 from jsonb_array_elements(p_transactions) x(value)
      where x.value->>'mergeKey'=b.merge_key
    )
    and (
      b.presence_state is distinct from 'present'
      or b.last_seen_at is distinct from p_snapshot_at
      or b.missing_since is not null
      or b.missing_acknowledged_at is not null
    );

  if coalesce(p_complete,false) then
    -- Pre-upgrade archive rows start UNKNOWN. The first complete snapshot is a baseline only: it
    -- cannot manufacture warnings for old history whose prior presence was never proven. Starting
    -- with the second complete snapshot, only previously PRESENT rows inside the connector-declared
    -- coverage window can transition to MISSING.
    if v_had_complete_snapshot then
      update public.bank_transactions b set
        presence_state='missing',
        missing_since=coalesce(b.missing_since,p_snapshot_at)
      where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role
        and b.presence_state='present'
        and coalesce(b.transaction_date,b.processed_date)::date between p_coverage_from and p_coverage_to
        and (b.last_seen_at is null or b.last_seen_at<p_snapshot_at)
        and not exists (
          select 1 from jsonb_array_elements(p_transactions) x(value)
          where x.value->>'mergeKey'=b.merge_key
        );
    end if;

    insert into public.bank_transaction_snapshots(
      owner_id,account_key,account_role,snapshot_at,coverage_from,coverage_to,
      transaction_count,transactions,updated_at
    ) values (
      v_owner,p_account_key,p_account_role,p_snapshot_at,p_coverage_from,p_coverage_to,
      jsonb_array_length(p_transactions),p_transactions,now()
    )
    on conflict(owner_id,account_key,account_role) do update set
      snapshot_at=excluded.snapshot_at,
      coverage_from=excluded.coverage_from,
      coverage_to=excluded.coverage_to,
      transaction_count=excluded.transaction_count,
      transactions=excluded.transactions,
      updated_at=now()
    where public.bank_transaction_snapshots.snapshot_at<=excluded.snapshot_at;
  end if;

  select count(*)::integer into missing_count
  from public.bank_transactions b
  where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role
    and b.presence_state='missing';

  select count(*)::integer into active_missing_count
  from public.bank_transactions b
  where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role
    and b.presence_state='missing' and b.missing_acknowledged_at is null;

  inserted_count:=coalesce(v_inserted,0);
  updated_count:=coalesce(v_updated,0);
  total_count:=coalesce(v_total,0);
  snapshot_stored:=coalesce(p_complete,false);
  baseline_created:=coalesce(p_complete,false) and not v_had_complete_snapshot;
  return next;
end $function$;
ALTER FUNCTION "netunim_internal"."fenced_impl_sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.json_collection_counts(p_state jsonb, p_paths text[])
 RETURNS jsonb
 LANGUAGE plpgsql
 IMMUTABLE
 SET search_path TO 'pg_catalog'
AS $function$
declare v_path text;v_out jsonb:='{}'::jsonb;v_value jsonb;
begin
  foreach v_path in array p_paths loop
    v_value:=p_state#>string_to_array(v_path,'.');
    v_out:=v_out||jsonb_build_object(v_path,case when jsonb_typeof(v_value)='array' then jsonb_array_length(v_value) else 0 end);
  end loop;
  return v_out;
end
$function$;
ALTER FUNCTION "netunim_internal"."json_collection_counts"(p_state jsonb, p_paths text[]) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.kupa_delete_intent_guard()
 RETURNS trigger
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare
  v_v4_allowed constant text[]:=array['credits','cash','rights','notes','expenses','notesSheet.rows','notesSheet.columns'];
  v_allowed constant text[]:=array['credits','cash','rights','notes','expenses','notesSheet.rows','notesSheet.columns','cards'];
  v_key text;v_old jsonb;v_new jsonb;v_removed jsonb;v_actual jsonb:='{}'::jsonb;v_intended jsonb;v_raw text;v_cards_raw text;
begin
  if tg_op<>'UPDATE' then return new;end if;
  foreach v_key in array v_allowed loop
    if v_key='notesSheet.rows' then v_old:=case when jsonb_typeof(old.state#>'{notesSheet,rows}')='array' then old.state#>'{notesSheet,rows}' else '[]'::jsonb end;v_new:=case when jsonb_typeof(new.state#>'{notesSheet,rows}')='array' then new.state#>'{notesSheet,rows}' else '[]'::jsonb end;
    elsif v_key='notesSheet.columns' then v_old:=case when jsonb_typeof(old.state#>'{notesSheet,columns}')='array' then old.state#>'{notesSheet,columns}' else '[]'::jsonb end;v_new:=case when jsonb_typeof(new.state#>'{notesSheet,columns}')='array' then new.state#>'{notesSheet,columns}' else '[]'::jsonb end;
    else v_old:=case when jsonb_typeof(old.state->v_key)='array' then old.state->v_key else '[]'::jsonb end;v_new:=case when jsonb_typeof(new.state->v_key)='array' then new.state->v_key else '[]'::jsonb end;end if;
    select coalesce(jsonb_agg(id order by id),'[]'::jsonb) into v_removed from (
      select btrim(o.value->>'id') id from jsonb_array_elements(v_old) o(value)
      where nullif(btrim(o.value->>'id'),'') is not null
        and not exists(select 1 from jsonb_array_elements(v_new) n(value) where btrim(n.value->>'id')=btrim(o.value->>'id'))
    ) removed;
    if jsonb_array_length(v_removed)>0 then v_actual:=v_actual||jsonb_build_object(v_key,v_removed);end if;
  end loop;
  v_raw:=nullif(current_setting('app.kupa_delete_intents',true),'');
  v_cards_raw:=nullif(current_setting('app.kupa_cards_delete_intents',true),'');
  v_intended:=netunim_internal.canonical_delete_intents(coalesce(v_raw::jsonb,'{}'::jsonb),v_v4_allowed)
    ||netunim_internal.canonical_delete_intents(jsonb_build_object('cards',coalesce(v_cards_raw::jsonb,'[]'::jsonb)),array['cards']);
  if v_actual is distinct from v_intended then raise exception 'kupa_delete_intent_mismatch' using errcode='PT422';end if;
  return new;
end
$function$;
ALTER FUNCTION "netunim_internal"."kupa_delete_intent_guard"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.kupa_header_owned_state(p_state jsonb)
 RETURNS jsonb
 LANGUAGE sql
 IMMUTABLE
 SET search_path TO 'pg_catalog'
AS $function$
  select (coalesce(p_state, '{}'::jsonb) - 'bank' - 'creditSync' - 'checks')
         || jsonb_build_object(
              'bankAdjustments',
              coalesce(p_state #> '{bank,adjustments}', '[]'::jsonb)
            );
$function$;
ALTER FUNCTION "netunim_internal"."kupa_header_owned_state"(p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.mass_destructive_guard()
 RETURNS trigger
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog'
AS $function$
declare
  v_paths text[];v_path text;v_old jsonb;v_new jsonb;v_removed integer;v_old_count integer;
  v_mode text:=coalesce(current_setting('app.destructive_operation_kind',true),'');
  v_absolute_threshold constant integer:=10;
  v_percentage_min_count constant integer:=3;
  v_percentage_threshold constant numeric:=0.50;
begin
  if tg_op<>'UPDATE' or v_mode in ('bulk-delete','restore','destructive-migration') then return new;end if;
  if tg_table_name='order_management_documents' then
    v_paths:=array['suppliers','transactions','customerDebts','customerOrders','serviceCalls','notes','inventoryItems','inventoryEvents','warehouseOrders'];
  elsif tg_table_name='kupa_documents' then
    v_paths:=array['credits','cash','rights','notes','expenses','cards','notesSheet.rows','notesSheet.columns'];
  else v_paths:=array['checks'];end if;
  foreach v_path in array v_paths loop
    v_old:=case when jsonb_typeof(old.state#>string_to_array(v_path,'.'))='array' then old.state#>string_to_array(v_path,'.') else '[]'::jsonb end;
    v_new:=case when jsonb_typeof(new.state#>string_to_array(v_path,'.'))='array' then new.state#>string_to_array(v_path,'.') else '[]'::jsonb end;
    v_old_count:=jsonb_array_length(v_old);
    select count(*) into v_removed from jsonb_array_elements(v_old) o(value)
      where nullif(btrim(o.value->>'id'),'') is not null
        and not exists(select 1 from jsonb_array_elements(v_new) n(value) where btrim(n.value->>'id')=btrim(o.value->>'id'));
    if v_removed>=v_absolute_threshold
       or (v_removed>=v_percentage_min_count and v_old_count>0 and v_removed::numeric/v_old_count>=v_percentage_threshold) then
      raise exception 'mass_delete_requires_dedicated_rpc' using errcode='PT422',detail=v_path,
        hint='Use the dedicated approved bulk-delete or restore RPC.';
    end if;
  end loop;
  return new;
end
$function$;
ALTER FUNCTION "netunim_internal"."mass_destructive_guard"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.merge_bank_transactions(p_account_key text, p_account_role text, p_transactions jsonb)
 RETURNS TABLE(inserted_count integer, updated_count integer, total_count integer)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_owner uuid:=auth.uid();
  r jsonb;
  v_id bigint;
  v_inserted int:=0;
  v_updated int:=0;
  v_candidates int;
  v_candidate bigint;
  v_date timestamptz;
  v_processed timestamptz;
  v_amount numeric;
  v_serial text;
  v_reference text;
  v_description text;
  v_memo text;
  v_party_name text;
  v_status text;
  v_balance numeric;
  v_activity integer;
  v_pending_id bigint;
  v_pending_in_source boolean;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  -- Shared per-user financial writer gate: fail fast instead of filling the PostgREST pool.
  if not pg_try_advisory_xact_lock(
    hashtextextended('netunim_financial_write:' || v_owner::text, 0)
  ) then
    raise exception 'save_busy'
      using errcode = 'PT429',
            hint = 'Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout', '100ms', true);
  if coalesce(btrim(p_account_key),'')='' or p_account_role not in ('business','home') or jsonb_typeof(p_transactions) is distinct from 'array' then raise exception 'invalid_bank_merge_input' using errcode='22023'; end if;

  -- A duplicate merge key means the client could not distinguish two source rows. Fail atomically
  -- instead of silently collapsing financial movements. Fallback rows receive occurrence suffixes.
  if exists (
    select 1
    from jsonb_array_elements(p_transactions) x(value)
    group by x.value->>'mergeKey'
    having coalesce(x.value->>'mergeKey','')='' or count(*)>1
  ) then
    raise exception 'bank_archive_ambiguous_source_identity'
      using errcode='22023', hint='The bank returned rows that cannot be given unique stable identities. No archive rows were changed.';
  end if;

  for r in select value from jsonb_array_elements(p_transactions) loop
    if nullif(r->>'amount','') is null then raise exception 'invalid_bank_transaction_amount' using errcode='22023'; end if;
    v_date:=nullif(r->>'date','')::timestamptz;
    v_processed:=nullif(r->>'processedDate','')::timestamptz;
    v_amount:=(r->>'amount')::numeric;
    v_serial:=coalesce(r->>'bankSerial','');
    v_reference:=coalesce(r->>'bankReference','');
    v_description:=coalesce(r->>'description','');
    v_memo:=coalesce(r->>'memo','');
    v_party_name:=coalesce(r->>'partyName','');
    v_status:=case when r->>'status'='pending' then 'pending' else 'completed' end;
    v_balance:=nullif(r->>'balanceAfter','')::numeric;
    v_activity:=nullif(r->>'activityTypeCode','')::integer;
    v_id:=null;
    v_pending_id:=null;
    v_pending_in_source:=false;

    -- Hapoalim reference numbers are not globally unique. The bank serial is day-scoped and is
    -- the strongest stable identifier available for completed rows, so date is part of every match.
    if v_date is not null and v_serial not in ('','0') then
      select count(*),min(b.id) into v_candidates,v_candidate
      from public.bank_transactions b
      where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role
        and b.transaction_date::date=v_date::date and b.bank_serial=v_serial and b.amount=v_amount;
      if v_candidates>1 then
        raise exception 'bank_archive_existing_identity_collision'
          using errcode='40001', hint='More than one archived row matches the same bank date/serial/amount identity. No merge was committed.';
      elsif v_candidates=1 then v_id:=v_candidate; end if;
    end if;
    if v_id is null and v_date is not null and v_reference<>'' then
      select count(*),min(b.id) into v_candidates,v_candidate
      from public.bank_transactions b
      where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role
        and b.transaction_date::date=v_date::date and b.bank_reference=v_reference and b.amount=v_amount
        and b.description=v_description and b.memo=v_memo;
      if v_candidates>1 then
        raise exception 'bank_archive_existing_identity_collision'
          using errcode='40001', hint='More than one archived row matches the same bank date/reference/amount/content identity. No merge was committed.';
      elsif v_candidates=1 then v_id:=v_candidate; end if;
    end if;

    if v_id is null and coalesce(r->>'mergeKey','')<>'' then
      select count(*),min(b.id) into v_candidates,v_candidate
      from public.bank_transactions b
      where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role and b.merge_key=r->>'mergeKey';
      if v_candidates>1 then
        raise exception 'bank_archive_existing_merge_key_collision'
          using errcode='40001', hint='More than one archived row has the same merge key. No merge was committed.';
      elsif v_candidates=1 then v_id:=v_candidate; end if;
    end if;

    -- A Hapoalim pending row is a temporary state of the same movement. While pending, the bank
    -- commonly returns serial=0 and sparse beneficiary text; after settlement it assigns a serial
    -- and enriches memo/party fields. Those enriched fields must therefore never be required to
    -- identify the old placeholder. Reconcile only one UNIQUE pending candidate using facts that
    -- the current connector preserves across both states: account, amount and a tight date window,
    -- plus either a non-zero bank reference or the exact bank-provided balance/activity pair.
    -- Ambiguity deliberately leaves rows separate rather than risking a false financial merge.
    if v_status='completed' and v_date is not null then
      select count(*),min(b.id) into v_candidates,v_candidate
      from public.bank_transactions b
      where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role
        and b.status='pending' and b.amount=v_amount
        and b.transaction_date between (v_date-interval '3 days') and (v_date+interval '3 days')
        and (
          (v_reference not in ('','0') and b.bank_reference=v_reference
            and (v_activity is null or b.activity_type_code is null or b.activity_type_code=v_activity))
          or
          (v_balance is not null and v_activity is not null
            and b.balance_after is not distinct from v_balance
            and b.activity_type_code=v_activity
            and b.description=v_description)
        );
      if v_candidates=1 then
        v_pending_id:=v_candidate;

        -- Never delete a pending row that the current bank payload still exposes. The exact merge
        -- key is preferred, but connector upgrades are allowed to change derived client identities,
        -- so a conservative stable-facts fallback also counts as "still present".
        select exists(
          select 1
          from public.bank_transactions pb
          cross join lateral jsonb_array_elements(p_transactions) x(value)
          where pb.id=v_pending_id and x.value->>'status'='pending'
            and (
              x.value->>'mergeKey'=pb.merge_key
              or (
                nullif(x.value->>'amount','') is not null
                and (x.value->>'amount')::numeric=pb.amount
                and (
                  (coalesce(x.value->>'bankReference','') not in ('','0')
                    and x.value->>'bankReference'=pb.bank_reference)
                  or
                  (nullif(x.value->>'balanceAfter','') is not null
                    and nullif(x.value->>'activityTypeCode','') is not null
                    and (x.value->>'balanceAfter')::numeric is not distinct from pb.balance_after
                    and (x.value->>'activityTypeCode')::integer is not distinct from pb.activity_type_code)
                  or
                  (coalesce(x.value->>'description','')=pb.description
                    and nullif(x.value->>'date','') is not null
                    and (x.value->>'date')::timestamptz between (pb.transaction_date-interval '1 day') and (pb.transaction_date+interval '1 day'))
                )
              )
            )
        ) into v_pending_in_source;

        if not v_pending_in_source and v_id is null then
          v_id:=v_pending_id;
        end if;
      end if;
    end if;

    if v_id is null then
      insert into public.bank_transactions(owner_id,account_key,account_role,merge_key,transaction_date,processed_date,amount,currency,description,memo,party_name,party_headline,message_headline,message_detail,status,balance_after,bank_reference,bank_serial,activity_type_code,cheque,check_details)
      values(v_owner,p_account_key,p_account_role,r->>'mergeKey',v_date,v_processed,v_amount,coalesce(nullif(r->>'currency',''),'ILS'),v_description,coalesce(r->>'memo',''),coalesce(r->>'partyName',''),coalesce(r->>'partyHeadline',''),coalesce(r->>'messageHeadline',''),coalesce(r->>'messageDetail',''),v_status,v_balance,v_reference,v_serial,v_activity,coalesce((r->>'cheque')::boolean,false),r->'checkDetails');
      v_inserted:=v_inserted+1;
    else
      update public.bank_transactions b set
        merge_key=coalesce(nullif(r->>'mergeKey',''),b.merge_key),
        transaction_date=coalesce(v_date,b.transaction_date),
        processed_date=coalesce(v_processed,b.processed_date),
        currency=coalesce(nullif(r->>'currency',''),b.currency),
        description=coalesce(nullif(r->>'description',''),b.description),
        memo=coalesce(r->>'memo',b.memo),
        party_name=coalesce(r->>'partyName',b.party_name),
        party_headline=coalesce(r->>'partyHeadline',b.party_headline),
        message_headline=coalesce(r->>'messageHeadline',b.message_headline),
        message_detail=coalesce(r->>'messageDetail',b.message_detail),
        status=case when v_status='pending' and b.status='completed' then b.status else v_status end,
        balance_after=coalesce(v_balance,b.balance_after),
        bank_reference=coalesce(nullif(r->>'bankReference',''),b.bank_reference),
        bank_serial=coalesce(nullif(r->>'bankSerial',''),b.bank_serial),
        activity_type_code=coalesce(v_activity,b.activity_type_code),
        cheque=coalesce((r->>'cheque')::boolean,b.cheque),
        check_details=coalesce(r->'checkDetails',b.check_details),
        last_changed_at=now()
      where b.id=v_id and (
        b.merge_key is distinct from coalesce(nullif(r->>'mergeKey',''),b.merge_key)
        or b.transaction_date is distinct from coalesce(v_date,b.transaction_date)
        or b.processed_date is distinct from coalesce(v_processed,b.processed_date)
        or b.currency is distinct from coalesce(nullif(r->>'currency',''),b.currency)
        or b.description is distinct from coalesce(nullif(r->>'description',''),b.description)
        or b.memo is distinct from coalesce(r->>'memo',b.memo)
        or b.party_name is distinct from coalesce(r->>'partyName',b.party_name)
        or b.party_headline is distinct from coalesce(r->>'partyHeadline',b.party_headline)
        or b.message_headline is distinct from coalesce(r->>'messageHeadline',b.message_headline)
        or b.message_detail is distinct from coalesce(r->>'messageDetail',b.message_detail)
        or b.status is distinct from (case when v_status='pending' and b.status='completed' then b.status else v_status end)
        or b.balance_after is distinct from coalesce(v_balance,b.balance_after)
        or b.bank_reference is distinct from coalesce(nullif(r->>'bankReference',''),b.bank_reference)
        or b.bank_serial is distinct from coalesce(nullif(r->>'bankSerial',''),b.bank_serial)
        or b.activity_type_code is distinct from coalesce(v_activity,b.activity_type_code)
        or b.cheque is distinct from coalesce((r->>'cheque')::boolean,b.cheque)
        or b.check_details is distinct from coalesce(r->'checkDetails',b.check_details)
      );
      if found then v_updated:=v_updated+1; end if;
    end if;

    -- Heal duplicates created by the previous logic: if this completed row already existed under
    -- its final serial identity, remove only the one uniquely matched stale pending placeholder.
    -- Security-definer ownership keeps DELETE capability inside this RPC; clients are not granted
    -- direct delete permission on the bank archive table.
    if v_status='completed' and v_pending_id is not null and v_pending_id<>v_id and not v_pending_in_source then
      delete from public.bank_transactions b
      where b.id=v_pending_id and b.owner_id=v_owner and b.account_key=p_account_key
        and b.account_role=p_account_role and b.status='pending';
    end if;
  end loop;
  -- Self-verify the statement before returning. If any source row failed to become the exact
  -- archived row for its merge key, raise and let PostgreSQL roll the whole RPC statement back.
  if exists (
    select 1
    from jsonb_array_elements(p_transactions) x(value)
    where not exists (
      select 1 from public.bank_transactions b
      where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role
        and b.merge_key=x.value->>'mergeKey'
        and (nullif(x.value->>'date','') is null or b.transaction_date is not distinct from (x.value->>'date')::timestamptz)
        and (nullif(x.value->>'processedDate','') is null or b.processed_date is not distinct from (x.value->>'processedDate')::timestamptz)
        and b.amount=(x.value->>'amount')::numeric
        and b.description=coalesce(x.value->>'description','')
        and b.memo=coalesce(x.value->>'memo','')
        and (coalesce(x.value->>'bankReference','')='' or b.bank_reference=x.value->>'bankReference')
        and (coalesce(x.value->>'bankSerial','')='' or b.bank_serial=x.value->>'bankSerial')
        and (case when x.value->>'status'='pending' then b.status in ('pending','completed') else b.status='completed' end)
    )
  ) then
    raise exception 'bank_archive_merge_verification_failed'
      using errcode='40001', hint='The transaction merge did not reproduce every bank row exactly. The entire RPC was rolled back.';
  end if;

  select count(*)::int into total_count from public.bank_transactions b where b.owner_id=v_owner and b.account_key=p_account_key and b.account_role=p_account_role;
  inserted_count:=v_inserted; updated_count:=v_updated; return next;
end $function$;
ALTER FUNCTION "netunim_internal"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.order_management_delete_intent_guard()
 RETURNS trigger
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare
  v_allowed constant text[] := array['suppliers','transactions','customerDebts','customerOrders','serviceCalls','notes','inventoryItems','inventoryEvents','warehouseOrders'];
  v_key text;
  v_old jsonb;
  v_new jsonb;
  v_removed jsonb;
  v_actual jsonb := '{}'::jsonb;
  v_intended jsonb;
  v_raw text;
begin
  if tg_op <> 'UPDATE' then return new; end if;
  foreach v_key in array v_allowed loop
    v_old:=case when jsonb_typeof(old.state->v_key)='array' then old.state->v_key else '[]'::jsonb end;
    v_new:=case when jsonb_typeof(new.state->v_key)='array' then new.state->v_key else '[]'::jsonb end;
    select coalesce(jsonb_agg(id order by id),'[]'::jsonb) into v_removed
    from (
      select o.value->>'id' id
      from jsonb_array_elements(v_old) o(value)
      where nullif(btrim(o.value->>'id'),'') is not null
        and not exists (select 1 from jsonb_array_elements(v_new) n(value) where n.value->>'id'=o.value->>'id')
    ) q;
    if jsonb_array_length(v_removed)>0 then v_actual:=v_actual||jsonb_build_object(v_key,v_removed); end if;
  end loop;
  v_raw:=nullif(current_setting('app.order_management_delete_intents',true),'');
  v_intended:=netunim_internal.canonical_delete_intents(coalesce(v_raw::jsonb,'{}'::jsonb),v_allowed);
  if v_actual is distinct from v_intended then
    raise exception 'order_management_delete_intent_mismatch'
      using errcode='PT422', hint='Record deletion requires exact explicit delete intents.';
  end if;
  return new;
end;
$function$;
ALTER FUNCTION "netunim_internal"."order_management_delete_intent_guard"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.prune_document_backups()
 RETURNS TABLE(rolling_deleted bigint, periodic_deleted bigint)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog'
AS $function$
declare v_table text;v_deleted bigint;
begin
  rolling_deleted:=0;periodic_deleted:=0;
  foreach v_table in array array[
    'order_management_document_backups','kupa_document_backups','shared_checks_document_backups'
  ] loop
    execute pg_catalog.format(
      'with ranked as materialized (
         select id,row_number() over(partition by owner_id,document_name order by saved_at desc,id desc) as recency_rank
         from public.%I
       ),deleted as (
         delete from public.%I target using ranked
         where target.id=ranked.id and ranked.recency_rank>200
         returning target.id
       ) select count(*)::bigint from deleted',v_table,v_table
    ) into v_deleted;
    rolling_deleted:=rolling_deleted+v_deleted;
  end loop;
  foreach v_table in array array[
    'order_management_periodic_backups','kupa_periodic_backups','shared_checks_periodic_backups'
  ] loop
    execute pg_catalog.format(
      'with deleted as (
         delete from public.%I
         where saved_at < statement_timestamp()-interval ''365 days''
         returning id
       ) select count(*)::bigint from deleted',v_table
    ) into v_deleted;
    periodic_deleted:=periodic_deleted+v_deleted;
  end loop;
  return next;
end
$function$;
ALTER FUNCTION "netunim_internal"."prune_document_backups"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.prune_sync_operation_ledgers()
 RETURNS TABLE(document_operations_deleted bigint, bank_operations_deleted bigint)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'netunim_internal'
AS $function$
declare
  v_cutoff constant timestamptz := statement_timestamp() - interval '730 days';
  v_keep_per_document constant integer := 100;
  v_batch_size constant integer := 10000;
begin
  with ranked as materialized (
    select owner_id,
           domain,
           document_name,
           operation_id,
           created_at,
           row_number() over (
             partition by owner_id, domain, document_name
             order by created_at desc, operation_id desc
           ) as recency_rank
    from netunim_internal.document_sync_operations
  ),
  doomed as materialized (
    select owner_id, domain, document_name, operation_id
    from ranked
    where created_at < v_cutoff
      and recency_rank > v_keep_per_document
    order by created_at, owner_id, domain, document_name, operation_id
    limit v_batch_size
  ),
  deleted as (
    delete from netunim_internal.document_sync_operations target
    using doomed
    where target.owner_id = doomed.owner_id
      and target.domain = doomed.domain
      and target.document_name = doomed.document_name
      and target.operation_id = doomed.operation_id
    returning 1
  )
  select count(*)::bigint into document_operations_deleted from deleted;

  with ranked as materialized (
    select owner_id,
           document_name,
           operation_id,
           created_at,
           row_number() over (
             partition by owner_id, document_name
             order by created_at desc, operation_id desc
           ) as recency_rank
    from netunim_internal.bank_sync_operations
  ),
  doomed as materialized (
    select owner_id, document_name, operation_id
    from ranked
    where created_at < v_cutoff
      and recency_rank > v_keep_per_document
    order by created_at, owner_id, document_name, operation_id
    limit v_batch_size
  ),
  deleted as (
    delete from netunim_internal.bank_sync_operations target
    using doomed
    where target.owner_id = doomed.owner_id
      and target.document_name = doomed.document_name
      and target.operation_id = doomed.operation_id
    returning 1
  )
  select count(*)::bigint into bank_operations_deleted from deleted;

  return next;
end
$function$;
ALTER FUNCTION "netunim_internal"."prune_sync_operation_ledgers"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.record_operation_audit(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_audit jsonb, p_before jsonb, p_after jsonb, p_paths text[], p_restore_group_id uuid DEFAULT NULL::uuid)
 RETURNS void
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'netunim_internal'
AS $function$
declare v_meta jsonb:=coalesce(p_audit,'{}'::jsonb);v_delete_count integer;
begin
  if auth.uid() is null or p_owner is distinct from auth.uid() then raise exception 'audit_owner_mismatch' using errcode='42501';end if;
  select coalesce(sum(greatest(0,(b.value::text)::integer-(a.value::text)::integer)),0)::integer into v_delete_count
  from jsonb_each(netunim_internal.json_collection_counts(p_before,p_paths)) b
  join jsonb_each(netunim_internal.json_collection_counts(p_after,p_paths)) a using(key);
  update netunim_internal.document_sync_operations o set
    client_instance_id=coalesce(o.client_instance_id,left(nullif(v_meta->>'clientInstanceId',''),200)),
    app_site=coalesce(o.app_site,case when v_meta->>'app' in ('kupa','orders') then v_meta->>'app' else 'unknown' end),
    build_version=coalesce(o.build_version,left(nullif(v_meta->>'build',''),80)),
    mutation_type=coalesce(o.mutation_type,left(nullif(v_meta->>'mutationType',''),80)),
    surface=coalesce(o.surface,left(nullif(v_meta->>'surface',''),120)),
    base_revision=coalesce(o.base_revision,case when (v_meta->>'baseRevision')~'^\d+$' then (v_meta->>'baseRevision')::bigint else null end),
    before_counts=coalesce(o.before_counts,netunim_internal.json_collection_counts(p_before,p_paths)),
    after_counts=coalesce(o.after_counts,netunim_internal.json_collection_counts(p_after,p_paths)),
    delete_count=coalesce(o.delete_count,v_delete_count),
    restore_group_id=coalesce(o.restore_group_id,p_restore_group_id),
    audit_timestamp=coalesce(o.audit_timestamp,now())
  where o.owner_id=p_owner and o.domain=p_domain and o.document_name=p_document_name and o.operation_id=p_operation_id;
end
$function$;
ALTER FUNCTION "netunim_internal"."record_operation_audit"(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_audit jsonb, p_before jsonb, p_after jsonb, p_paths text[], p_restore_group_id uuid) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.save_bank_sync_snapshot(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint)
 RETURNS TABLE(finance_revision bigint, kupa_revision bigint, updated_at timestamp with time zone)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal', 'extensions'
AS $function$
declare
  v_owner uuid := auth.uid();
  v_kupa_state jsonb;
  v_kupa_bank jsonb;
  v_old_snapshot_seq bigint := 0;
  v_shared_max_seq bigint := 0;
  v_finance_state jsonb;
  v_finance_revision bigint;
  v_kupa_revision bigint;
  v_finance_updated_at timestamptz;
  v_kupa_updated_at timestamptz;
  v_operation_seq bigint;
  v_operation_hash text;
  v_payload_hash text;
  v_now timestamptz := clock_timestamp();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:' || v_owner::text, 0)) then
    raise exception 'save_busy' using errcode='PT429', hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout', '100ms', true);
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_bank_state is null or jsonb_typeof(p_bank_state) is distinct from 'object' then raise exception 'invalid_bank_sync_state' using errcode='22023'; end if;
  if coalesce(btrim(p_snapshot_token),'')='' or length(p_snapshot_token)>200 then raise exception 'invalid_bank_snapshot_token' using errcode='22023'; end if;
  if p_snapshot_seq is null or p_snapshot_seq<0 then raise exception 'invalid_bank_snapshot_sequence' using errcode='22023'; end if;
  v_payload_hash:=encode(digest(convert_to(p_bank_state::text, 'UTF8'), 'sha256'), 'hex');

  -- Canonical order remains Kupa then Finance; the shared financial advisory gate is unchanged.
  select d.state,d.revision,d.updated_at into v_kupa_state,v_kupa_revision,v_kupa_updated_at
  from public.kupa_documents d
  where d.owner_id=v_owner and d.document_name=p_document_name
  for update;
  if v_kupa_state is null then raise exception 'kupa_document_not_found' using errcode='P0002'; end if;

  select f.state,f.revision,f.updated_at into v_finance_state,v_finance_revision,v_finance_updated_at
  from public.finance_sync_documents f
  where f.owner_id=v_owner and f.document_name=p_document_name
  for update;

  select o.snapshot_seq,o.payload_sha256 into v_operation_seq,v_operation_hash
  from netunim_internal.bank_sync_operations o
  where o.owner_id=v_owner and o.document_name=p_document_name and o.operation_id=p_snapshot_token;
  if found then
    if v_operation_seq<>p_snapshot_seq or v_operation_hash<>v_payload_hash then
      raise exception 'idempotency_key_reuse' using errcode='PT422', hint='A bank snapshot token cannot be reused with a different payload.';
    end if;
    if v_finance_revision is null then raise exception 'finance_document_not_found' using errcode='P0002'; end if;
    finance_revision:=v_finance_revision;
    kupa_revision:=v_kupa_revision;
    updated_at:=greatest(v_finance_updated_at,v_kupa_updated_at);
    return next; return;
  end if;

  v_kupa_bank:=coalesce(v_kupa_state->'bank','{}'::jsonb);
  if jsonb_typeof(v_kupa_bank->'snapshotSeq')='number' then
    begin v_old_snapshot_seq:=greatest(0,(v_kupa_bank->>'snapshotSeq')::bigint);
    exception when others then raise exception 'invalid_existing_bank_snapshot_sequence' using errcode='22023'; end;
  end if;

  select coalesce(max((e.value->>'seq')::bigint),0) into v_shared_max_seq
  from public.shared_checks_documents s
  left join lateral jsonb_array_elements(case when jsonb_typeof(s.state->'bankEvents')='array' then s.state->'bankEvents' else '[]'::jsonb end) e(value) on true
  where s.owner_id=v_owner and s.document_name='main';
  v_shared_max_seq:=coalesce(v_shared_max_seq,0);
  if p_snapshot_seq<v_old_snapshot_seq then
    raise exception 'stale_bank_snapshot_watermark' using errcode='40001',hint='Refresh shared checks before saving the bank snapshot.';
  end if;
  if p_snapshot_seq>greatest(v_old_snapshot_seq,v_shared_max_seq) then
    raise exception 'bank_snapshot_watermark_ahead_of_server' using errcode='22023',hint='The requested watermark was not observed in shared check events.';
  end if;

  if v_finance_revision is null then
    insert into public.finance_sync_documents(owner_id,document_name,revision,state,updated_at)
    values(v_owner,p_document_name,1,jsonb_build_object('bank',p_bank_state,'creditSync',null),v_now)
    returning revision into v_finance_revision;
  else
    v_finance_state:=coalesce(v_finance_state,'{}'::jsonb);
    update public.finance_sync_documents f
    set revision=f.revision+1,state=jsonb_set(v_finance_state,'{bank}',p_bank_state,true),updated_at=v_now
    where f.owner_id=v_owner and f.document_name=p_document_name
    returning f.revision into v_finance_revision;
  end if;

  perform set_config('app.kupa_rpc_write','1',true);
  v_kupa_bank:=jsonb_build_object(
    'currentBalance',null,'updatedAt',null,'asOfDate',null,
    'adjustments',coalesce(v_kupa_bank->'adjustments','[]'::jsonb),
    'source',null,'sourceAccount',null,
    'snapshotToken',p_snapshot_token,'snapshotSeq',p_snapshot_seq
  );
  v_kupa_state:=(v_kupa_state-'bank'-'creditSync')||jsonb_build_object('bank',v_kupa_bank);
  update public.kupa_documents d
  set revision=d.revision+1,state=v_kupa_state,updated_at=v_now
  where d.owner_id=v_owner and d.document_name=p_document_name
  returning d.revision into v_kupa_revision;

  insert into netunim_internal.bank_sync_operations(owner_id,document_name,operation_id,snapshot_seq,payload_sha256,created_at)
  values(v_owner,p_document_name,p_snapshot_token,p_snapshot_seq,v_payload_hash,v_now);
  finance_revision:=v_finance_revision;kupa_revision:=v_kupa_revision;updated_at:=v_now;return next;
end $function$;
ALTER FUNCTION "netunim_internal"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.save_finance_sync_document(p_document_name text, p_expected_revision bigint, p_state jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare
  v_owner uuid := auth.uid();
  v_current bigint;
  v_current_updated_at timestamptz;
  v_current_state jsonb;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:' || v_owner::text, 0)) then
    raise exception 'save_busy' using errcode='PT429', hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout', '100ms', true);
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_expected_revision is null or p_expected_revision < 0 then raise exception 'invalid_expected_revision' using errcode='22023'; end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_finance_sync_state' using errcode='22023'; end if;

  select d.revision, d.updated_at, d.state
  into v_current, v_current_updated_at, v_current_state
  from public.finance_sync_documents d
  where d.owner_id=v_owner and d.document_name=p_document_name
  for update;

  if v_current is null then
    if p_expected_revision<>0 then raise exception 'revision_conflict' using errcode='PT409'; end if;
    insert into public.finance_sync_documents(owner_id,document_name,revision,state,updated_at)
    values(v_owner,p_document_name,1,p_state,now())
    returning finance_sync_documents.revision,finance_sync_documents.updated_at,finance_sync_documents.state
    into revision,updated_at,state;
    return next; return;
  end if;

  -- Lost ACK replay: equality is checked before optimistic revision conflict.
  if v_current_state = p_state then
    revision:=v_current; updated_at:=v_current_updated_at; state:=v_current_state;
    return next; return;
  end if;
  if v_current<>p_expected_revision then raise exception 'revision_conflict' using errcode='PT409'; end if;

  update public.finance_sync_documents d
  set revision=d.revision+1,state=p_state,updated_at=now()
  where d.owner_id=v_owner and d.document_name=p_document_name
  returning d.revision,d.updated_at,d.state into revision,updated_at,state;
  return next;
end $function$;
ALTER FUNCTION "netunim_internal"."save_finance_sync_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.save_kupa_document(p_document_name text, p_expected_revision bigint, p_state jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_owner uuid := auth.uid();
  v_current_revision bigint;
  v_old_state jsonb;
  v_old_updated_at timestamptz;
  v_server_state jsonb;
  v_bank jsonb;
  v_old_bank jsonb;
  v_new_token text;
  v_old_token text;
  v_requested_snapshot_num numeric;
  v_requested_snapshot_seq bigint;
  v_old_snapshot_seq bigint := 0;
  v_shared_max_seq bigint := 0;
  v_now timestamptz;
  v_new_revision bigint;
  v_new_updated_at timestamptz;
begin
  if v_owner is null then
    raise exception 'not_authenticated' using errcode = '42501';
  end if;
  if p_document_name is null or btrim(p_document_name) = '' then
    raise exception 'invalid_document_name' using errcode = '22023';
  end if;
  if p_expected_revision is null or p_expected_revision < 0 then
    raise exception 'invalid_expected_revision' using errcode = '22023';
  end if;
  if p_state is null
     or jsonb_typeof(p_state) is distinct from 'object'
     or (p_state ? 'checks')
     or jsonb_typeof(p_state->'credits') is distinct from 'array'
     or jsonb_typeof(p_state->'cash') is distinct from 'array'
     or jsonb_typeof(p_state->'expenses') is distinct from 'array'
     or jsonb_typeof(p_state->'cards') is distinct from 'array'
     or jsonb_typeof(p_state->'bank') is distinct from 'object'
     or jsonb_typeof(p_state#>'{bank,adjustments}') is distinct from 'array' then
    raise exception 'invalid_kupa_state'
      using errcode = '22023',
            hint = 'Post-cutover state must contain credits/cash/expenses/cards arrays and bank object, without checks.';
  end if;
  if exists (
    select 1 from jsonb_array_elements(p_state#>'{bank,adjustments}') a
    where a->>'type' = 'check_deposit'
  ) then
    raise exception 'legacy_check_deposit_forbidden'
      using errcode = '22023', hint = 'Check deposits are derived from shared_checks_documents after cutover.';
  end if;

  perform set_config('app.kupa_rpc_write', '1', true);

  select d.revision, d.state, d.updated_at
    into v_current_revision, v_old_state, v_old_updated_at
  from public.kupa_documents d
  where d.owner_id = v_owner and d.document_name = p_document_name
  for update;

  v_server_state := p_state;
  v_bank := p_state->'bank';
  v_old_bank := coalesce(v_old_state->'bank','{}'::jsonb);
  v_new_token := nullif(v_bank->>'snapshotToken','');
  v_old_token := nullif(v_old_bank->>'snapshotToken','');

  if v_new_token is distinct from v_old_token and v_new_token is not null then
    if jsonb_typeof(v_bank->'snapshotSeq') is distinct from 'number'
       or (v_bank->>'source' = 'manual' and jsonb_typeof(v_bank->'currentBalance') is distinct from 'number') then
      raise exception 'invalid_bank_snapshot' using errcode = '22023';
    end if;

    -- snapshotSeq הוא watermark שהלקוח באמת ראה אחרי sync של מאגר הצ'קים.
    -- אסור לשרת להקצות כאן sequence חדש: אירוע צ'ק מקביל שלא נצפה חייב להישאר אחרי ה-baseline.
    v_requested_snapshot_num := (v_bank->>'snapshotSeq')::numeric;
    if v_requested_snapshot_num < 0
       or v_requested_snapshot_num <> trunc(v_requested_snapshot_num)
       or v_requested_snapshot_num > 9223372036854775807::numeric then
      raise exception 'invalid_bank_snapshot_sequence' using errcode = '22023';
    end if;
    v_requested_snapshot_seq := v_requested_snapshot_num::bigint;

    if jsonb_typeof(v_old_bank->'snapshotSeq') = 'number' then
      begin
        v_old_snapshot_seq := greatest(0, (v_old_bank->>'snapshotSeq')::bigint);
      exception when others then
        raise exception 'invalid_existing_bank_snapshot_sequence' using errcode = '22023';
      end;
    end if;

    select coalesce(max((e.value->>'seq')::bigint), 0)
      into v_shared_max_seq
    from public.shared_checks_documents s
    left join lateral jsonb_array_elements(
      case when jsonb_typeof(s.state->'bankEvents')='array' then s.state->'bankEvents' else '[]'::jsonb end
    ) e(value) on true
    where s.owner_id = v_owner and s.document_name = 'main';
    v_shared_max_seq := coalesce(v_shared_max_seq, 0);

    if v_requested_snapshot_seq < v_old_snapshot_seq then
      raise exception 'stale_bank_snapshot_watermark'
        using errcode = 'PT409', hint = 'Refresh shared checks before saving a new bank snapshot.';
    end if;
    if v_requested_snapshot_seq > greatest(v_old_snapshot_seq, v_shared_max_seq) then
      raise exception 'bank_snapshot_watermark_ahead_of_server'
        using errcode = '22023', hint = 'The client watermark was not observed in shared check events.';
    end if;

    v_now := clock_timestamp();
    v_bank := v_bank || jsonb_build_object('snapshotSeq', v_requested_snapshot_seq);
    if v_bank->>'source' = 'manual' then
      v_bank := v_bank || jsonb_build_object('updatedAt', v_now);
    end if;
  elsif v_old_state is not null then
    -- Lightweight bank snapshot metadata is server-authoritative; ordinary Kupa saves cannot roll it back.
    v_bank := v_bank || jsonb_build_object(
      'snapshotToken', coalesce(v_old_bank->'snapshotToken','null'::jsonb),
      'snapshotSeq', coalesce(v_old_bank->'snapshotSeq','null'::jsonb),
      'updatedAt', case when v_bank->>'source' = 'manual' then coalesce(v_old_bank->'updatedAt','null'::jsonb) else 'null'::jsonb end
    );
  else
    v_bank := v_bank || jsonb_build_object('snapshotSeq', null);
  end if;

  -- Financial feeds and credit-company sync are stored in finance_sync_documents / bank_transactions,
  -- never in the Kupa document or its snapshot backups. Keep only the tiny Kupa-owned bank baseline metadata.
  v_bank := jsonb_build_object(
    'currentBalance', case when v_bank->>'source' = 'manual' then coalesce(v_bank->'currentBalance','null'::jsonb) else 'null'::jsonb end,
    'updatedAt', case when v_bank->>'source' = 'manual' then coalesce(v_bank->'updatedAt','null'::jsonb) else 'null'::jsonb end,
    'asOfDate', case when v_bank->>'source' = 'manual' then coalesce(v_bank->'asOfDate','null'::jsonb) else 'null'::jsonb end,
    'adjustments', coalesce(v_bank->'adjustments','[]'::jsonb),
    'source', case when v_bank->>'source' = 'manual' then 'manual'::text else null end,
    'sourceAccount', null,
    'snapshotToken', coalesce(v_bank->'snapshotToken','null'::jsonb),
    'snapshotSeq', coalesce(v_bank->'snapshotSeq','null'::jsonb)
  );
  v_server_state := (v_server_state - 'bank' - 'creditSync') || jsonb_build_object('bank', v_bank);

  if v_current_revision is null then
    if coalesce(p_expected_revision,0) <> 0 then
      raise exception 'revision_conflict' using errcode = 'PT409';
    end if;
    insert into public.kupa_documents(owner_id, document_name, revision, state, updated_at)
    values (v_owner, p_document_name, 1, v_server_state, now())
    returning kupa_documents.revision, kupa_documents.updated_at, kupa_documents.state
      into v_new_revision, v_new_updated_at, v_server_state;
    insert into public.kupa_periodic_backups(owner_id, document_name, revision, state, saved_at)
    values (v_owner, p_document_name, v_new_revision, v_server_state, v_new_updated_at)
    on conflict do nothing;
    return query select v_new_revision, v_new_updated_at, v_server_state;
    return;
  end if;

  -- ACK שאבד: קודם בודקים אם התוכן שכבר בשרת הוא בדיוק התוכן המבוקש.
  if v_old_state = v_server_state then
    return query select v_current_revision, v_old_updated_at, v_old_state;
    return;
  end if;
  if v_current_revision <> p_expected_revision then
    raise exception 'revision_conflict' using errcode = 'PT409';
  end if;

  insert into public.kupa_document_backups(owner_id, document_name, revision, state, saved_at)
  values (v_owner, p_document_name, v_current_revision, v_old_state, v_old_updated_at)
  on conflict do nothing;

  update public.kupa_documents d
  set revision = d.revision + 1, state = v_server_state, updated_at = now()
  where d.owner_id = v_owner and d.document_name = p_document_name
  returning d.revision, d.updated_at, d.state
    into v_new_revision, v_new_updated_at, v_server_state;

  if not exists (
    select 1 from public.kupa_periodic_backups p
    where p.owner_id = v_owner and p.document_name = p_document_name
      and p.saved_at > now() - interval '12 hours'
  ) then
    insert into public.kupa_periodic_backups(owner_id, document_name, revision, state, saved_at)
    values (v_owner, p_document_name, v_new_revision, v_server_state, v_new_updated_at)
    on conflict do nothing;
  end if;

  return query select v_new_revision, v_new_updated_at, v_server_state;
end;
$function$;
ALTER FUNCTION "netunim_internal"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.save_order_management_document(p_document_name text, p_expected_revision bigint, p_state jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_owner uuid := auth.uid();
  v_current_revision bigint;
  v_old_state jsonb;
  v_old_updated_at timestamptz;
  v_new_revision bigint;
  v_new_updated_at timestamptz;
begin
  if v_owner is null then
    raise exception 'not_authenticated' using errcode = '42501';
  end if;
  if p_document_name is null or btrim(p_document_name) = '' then
    raise exception 'invalid_document_name' using errcode = '22023';
  end if;
  if p_expected_revision is null or p_expected_revision < 0 then
    raise exception 'invalid_expected_revision' using errcode = '22023';
  end if;
  if p_state is null
     or jsonb_typeof(p_state) is distinct from 'object'
     or (p_state ? 'checks')
     or jsonb_typeof(p_state->'suppliers') is distinct from 'array'
     or jsonb_typeof(p_state->'transactions') is distinct from 'array'
     or jsonb_typeof(p_state->'customerDebts') is distinct from 'array'
     or jsonb_typeof(p_state->'customerOrders') is distinct from 'array'
     or jsonb_typeof(p_state->'serviceCalls') is distinct from 'array'
     or jsonb_typeof(p_state->'inventoryItems') is distinct from 'array'
     or jsonb_typeof(p_state->'inventoryCategoryOrder') is distinct from 'array'
     or jsonb_typeof(p_state->'inventoryEvents') is distinct from 'array'
     or jsonb_typeof(p_state->'warehouseOrders') is distinct from 'array'
     or jsonb_typeof(p_state->'notes') is distinct from 'array' then
    raise exception 'invalid_order_management_state'
      using errcode = '22023',
            hint = 'Post-cutover order state is missing required arrays or still contains checks.';
  end if;

  perform set_config('app.order_management_rpc_write', '1', true);

  -- Distributed fail-fast writer gate: do not let concurrent stale clients queue on the row lock
  -- and exhaust PostgREST's database pool. The transaction-scoped lock releases automatically.
  if not pg_try_advisory_xact_lock(
    hashtextextended('order_management:' || v_owner::text || ':' || p_document_name, 0)
  ) then
    raise exception 'revision_conflict'
      using errcode = 'PT409',
            hint = 'Another save for this document is already in progress; refresh the revision before retrying.';
  end if;

  select d.revision, d.state, d.updated_at
    into v_current_revision, v_old_state, v_old_updated_at
  from public.order_management_documents d
  where d.owner_id = v_owner and d.document_name = p_document_name
  for update;

  if v_current_revision is null then
    if coalesce(p_expected_revision,0) <> 0 then
      raise exception 'revision_conflict' using errcode = 'PT409';
    end if;
    insert into public.order_management_documents(owner_id, document_name, revision, state, updated_at)
    values (v_owner, p_document_name, 1, p_state, now())
    returning order_management_documents.revision, order_management_documents.updated_at
      into v_new_revision, v_new_updated_at;
    insert into public.order_management_periodic_backups(owner_id, document_name, revision, state, saved_at)
    values (v_owner, p_document_name, v_new_revision, p_state, v_new_updated_at)
    on conflict do nothing;
    return query select v_new_revision, v_new_updated_at;
    return;
  end if;

  -- savedAt משתנה בכל prepareState ולכן אינו חלק מהשוואת התוכן העסקי.
  -- הבדיקה קודמת ל-revision conflict כדי לתמוך ב-retry לאחר ACK שאבד.
  if (v_old_state #- '{_meta,savedAt}') = (p_state #- '{_meta,savedAt}') then
    return query select v_current_revision, v_old_updated_at;
    return;
  end if;
  if v_current_revision <> p_expected_revision then
    raise exception 'revision_conflict' using errcode = 'PT409';
  end if;

  insert into public.order_management_document_backups(owner_id, document_name, revision, state, saved_at)
  values (v_owner, p_document_name, v_current_revision, v_old_state, v_old_updated_at)
  on conflict do nothing;

  update public.order_management_documents d
  set revision=d.revision+1, state=p_state, updated_at=now()
  where d.owner_id=v_owner and d.document_name=p_document_name
  returning d.revision, d.updated_at into v_new_revision, v_new_updated_at;

  if not exists (
    select 1 from public.order_management_periodic_backups p
    where p.owner_id=v_owner and p.document_name=p_document_name
      and p.saved_at > now() - interval '12 hours'
  ) then
    insert into public.order_management_periodic_backups(owner_id, document_name, revision, state, saved_at)
    values (v_owner, p_document_name, v_new_revision, p_state, v_new_updated_at)
    on conflict do nothing;
  end if;

  return query select v_new_revision, v_new_updated_at;
end;
$function$;
ALTER FUNCTION "netunim_internal"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.save_shared_checks_document(p_document_name text, p_expected_revision bigint, p_state jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_owner uuid := auth.uid();
  v_current_revision bigint;
  v_old_state jsonb;
  v_old_updated_at timestamptz;
  v_server_state jsonb;
  v_server_checks jsonb := '[]'::jsonb;
  v_bank_events jsonb := '[]'::jsonb;
  v_check jsonb;
  v_old_check jsonb;
  v_check_id text;
  v_status text;
  v_old_status text;
  v_is_deposited boolean;
  v_was_deposited boolean;
  v_old_effect numeric;
  v_new_effect numeric;
  v_effect_delta numeric;
  v_event_seq bigint;
  v_now timestamptz;
  v_new_revision bigint;
  v_new_updated_at timestamptz;
begin
  if v_owner is null then
    raise exception 'not_authenticated' using errcode = '42501';
  end if;
  if p_document_name is null or btrim(p_document_name) = '' then
    raise exception 'invalid_document_name' using errcode = '22023';
  end if;
  if p_expected_revision is null or p_expected_revision < 0 then
    raise exception 'invalid_expected_revision' using errcode = '22023';
  end if;
  if p_state is null
     or jsonb_typeof(p_state) is distinct from 'object'
     or jsonb_typeof(p_state->'checks') is distinct from 'array' then
    raise exception 'invalid_shared_checks_state'
      using errcode = '22023', hint = 'State must be an object containing a checks array.';
  end if;
  if exists (
    select 1
    from jsonb_array_elements(p_state->'checks') c
    where jsonb_typeof(c) is distinct from 'object'
       or nullif(btrim(c->>'id'), '') is null
       or jsonb_typeof(c->'amount') is distinct from 'number'
       or jsonb_typeof(c->'dueDate') is distinct from 'string'
       or jsonb_typeof(c->'status') is distinct from 'string'
       or (c->>'status') not in ('בקופה','הופקד - במעקב','נפרע','חזר','בוטל')
  ) then
    raise exception 'invalid_shared_check_record' using errcode = '22023';
  end if;
  if exists (
    select 1
    from jsonb_array_elements(p_state->'checks') c
    group by c->>'id'
    having count(*) > 1
  ) then
    raise exception 'duplicate_shared_check_id' using errcode = '23505';
  end if;

  perform set_config('app.shared_checks_rpc_write', '1', true);

  select d.revision, d.state, d.updated_at
    into v_current_revision, v_old_state, v_old_updated_at
  from public.shared_checks_documents d
  where d.owner_id = v_owner and d.document_name = p_document_name
  for update;

  if v_old_state is not null and jsonb_typeof(v_old_state->'bankEvents') = 'array' then
    v_bank_events := v_old_state->'bankEvents';
  end if;

  -- Fail closed against a stale/bootstrap client interpreting an empty local shell as "delete every check".
  -- Normal single-record deletion remains available, including deletion of the final remaining check.
  if v_old_state is not null
     and jsonb_typeof(v_old_state->'checks') = 'array'
     and jsonb_array_length(v_old_state->'checks') > 1
     and jsonb_array_length(p_state->'checks') = 0
     and nullif(current_setting('app.shared_checks_delete_ids', true), '') is null then
    raise exception 'bulk_delete_all_shared_checks_forbidden'
      using errcode = '22023', hint = 'Refresh from shared_checks_documents before attempting a bulk deletion.';
  end if;

  -- הלקוח שולח את מצב הצ'קים בלבד. depositSeq/depositedAt ו-bankEvents הם server-authoritative.
  -- כל שינוי בהשפעה הכספית של צ'ק יוצר אירוע delta מונוטוני, כולל החזרה, שינוי סכום ומחיקה.
  for v_check in select value from jsonb_array_elements(p_state->'checks') loop
    v_check_id := v_check->>'id';
    v_status := v_check->>'status';
    v_is_deposited := v_status in ('הופקד - במעקב','נפרע');
    v_old_check := null;
    if v_old_state is not null then
      select c.value into v_old_check
      from jsonb_array_elements(v_old_state->'checks') c(value)
      where c.value->>'id' = v_check_id
      limit 1;
    end if;
    v_old_status := coalesce(v_old_check->>'status','');
    v_was_deposited := v_old_status in ('הופקד - במעקב','נפרע');
    v_old_effect := case when v_was_deposited then coalesce((v_old_check->>'amount')::numeric,0) else 0 end;
    v_new_effect := case when v_is_deposited then coalesce((v_check->>'amount')::numeric,0) else 0 end;
    v_effect_delta := v_new_effect - v_old_effect;
    v_event_seq := null;
    v_now := null;

    -- כל metadata שהלקוח החזיר מנוקה ומוחזר מהמצב השרת/מהאירוע החדש בלבד.
    v_check := v_check - 'depositSeq' - 'depositedAt' - 'bankEvents';

    if v_is_deposited and not v_was_deposited then
      v_event_seq := nextval('public.shared_financial_event_seq');
      v_now := clock_timestamp();
      v_check := v_check || jsonb_build_object('depositSeq', v_event_seq, 'depositedAt', v_now);
    elsif v_old_check is not null then
      -- retry אחרי ACK שאבד מקבל שוב בדיוק את metadata שכבר אושר בשרת.
      v_check := v_check || jsonb_build_object(
        'depositSeq', coalesce(v_old_check->'depositSeq','null'::jsonb),
        'depositedAt', coalesce(v_old_check->'depositedAt','null'::jsonb)
      );
    else
      v_check := v_check || jsonb_build_object('depositSeq', null, 'depositedAt', null);
    end if;

    if v_effect_delta <> 0 then
      if v_event_seq is null then
        v_event_seq := nextval('public.shared_financial_event_seq');
        v_now := clock_timestamp();
      end if;
      v_bank_events := v_bank_events || jsonb_build_array(jsonb_build_object(
        'seq', v_event_seq,
        'at', v_now,
        'delta', v_effect_delta,
        'kind', 'check_effect_delta',
        'checkId', v_check_id
      ));
    end if;

    v_server_checks := v_server_checks || jsonb_build_array(v_check);
  end loop;

  -- מחיקת צ'ק בעל השפעה בנקאית חייבת ליצור reversal גם לאחר שהרשומה עצמה נעלמה.
  if v_old_state is not null then
    for v_old_check in
      select c.value
      from jsonb_array_elements(v_old_state->'checks') c(value)
      where not exists (
        select 1 from jsonb_array_elements(p_state->'checks') n(value)
        where n.value->>'id' = c.value->>'id'
      )
    loop
      v_old_status := coalesce(v_old_check->>'status','');
      if v_old_status in ('הופקד - במעקב','נפרע') then
        v_old_effect := coalesce((v_old_check->>'amount')::numeric,0);
        if v_old_effect <> 0 then
          v_event_seq := nextval('public.shared_financial_event_seq');
          v_now := clock_timestamp();
          v_bank_events := v_bank_events || jsonb_build_array(jsonb_build_object(
            'seq', v_event_seq,
            'at', v_now,
            'delta', -v_old_effect,
            'kind', 'check_effect_delta',
            'checkId', v_old_check->>'id'
          ));
        end if;
      end if;
    end loop;
  end if;

  v_server_state := jsonb_build_object('version',1,'checks',v_server_checks,'bankEvents',v_bank_events);

  if v_current_revision is null then
    if coalesce(p_expected_revision, 0) <> 0 then
      raise exception 'revision_conflict' using errcode = 'PT409';
    end if;
    insert into public.shared_checks_documents(owner_id, document_name, revision, state, updated_at)
    values (v_owner, p_document_name, 1, v_server_state, now())
    returning shared_checks_documents.revision, shared_checks_documents.updated_at, shared_checks_documents.state
      into v_new_revision, v_new_updated_at, v_server_state;
    insert into public.shared_checks_periodic_backups(owner_id, document_name, revision, state, saved_at)
    values (v_owner, p_document_name, v_new_revision, v_server_state, v_new_updated_at)
    on conflict do nothing;
    return query select v_new_revision, v_new_updated_at, v_server_state;
    return;
  end if;

  -- Idempotency לפני conflict: lost ACK עם expected revision ישן אינו יוצר כשל שווא.
  if v_old_state = v_server_state then
    return query select v_current_revision, v_old_updated_at, v_old_state;
    return;
  end if;
  if v_current_revision <> p_expected_revision then
    raise exception 'revision_conflict' using errcode = 'PT409';
  end if;

  insert into public.shared_checks_document_backups(owner_id, document_name, revision, state, saved_at)
  values (v_owner, p_document_name, v_current_revision, v_old_state, v_old_updated_at)
  on conflict do nothing;

  update public.shared_checks_documents d
  set revision = d.revision + 1, state = v_server_state, updated_at = now()
  where d.owner_id = v_owner and d.document_name = p_document_name
  returning d.revision, d.updated_at, d.state
    into v_new_revision, v_new_updated_at, v_server_state;

  if not exists (
    select 1 from public.shared_checks_periodic_backups p
    where p.owner_id = v_owner and p.document_name = p_document_name
      and p.saved_at > now() - interval '12 hours'
  ) then
    insert into public.shared_checks_periodic_backups(owner_id, document_name, revision, state, saved_at)
    values (v_owner, p_document_name, v_new_revision, v_server_state, v_new_updated_at)
    on conflict do nothing;
  end if;

  return query select v_new_revision, v_new_updated_at, v_server_state;
end;
$function$;
ALTER FUNCTION "netunim_internal"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.shared_checks_delete_intent_guard()
 RETURNS trigger
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_raw text;
  v_declared_json jsonb;
  v_declared text[] := '{}'::text[];
  v_removed text[] := '{}'::text[];
begin
  if tg_op <> 'UPDATE' then return new; end if;
  if jsonb_typeof(old.state->'checks') is distinct from 'array'
     or jsonb_typeof(new.state->'checks') is distinct from 'array' then
    raise exception 'invalid_shared_checks_state' using errcode='22023';
  end if;

  select coalesce(array_agg(id order by id),'{}'::text[])
    into v_removed
  from (
    select old_check.value->>'id' as id
    from jsonb_array_elements(old.state->'checks') old_check(value)
    where nullif(btrim(old_check.value->>'id'),'') is not null
      and not exists (
        select 1
        from jsonb_array_elements(new.state->'checks') new_check(value)
        where new_check.value->>'id' = old_check.value->>'id'
      )
  ) removed;

  if cardinality(v_removed)=0 then return new; end if;

  v_raw:=nullif(current_setting('app.shared_checks_delete_ids',true),'');
  if v_raw is null then
    raise exception 'shared_checks_delete_intent_required'
      using errcode='PT422', hint='Check deletion requires an explicit deletedCheckIds list.';
  end if;

  begin
    v_declared_json:=v_raw::jsonb;
  exception when others then
    raise exception 'invalid_shared_checks_delete_intent' using errcode='22023';
  end;
  if jsonb_typeof(v_declared_json) is distinct from 'array' then
    raise exception 'invalid_shared_checks_delete_intent' using errcode='22023';
  end if;
  if exists (
    select 1 from jsonb_array_elements(v_declared_json) j(value)
    where jsonb_typeof(j.value) is distinct from 'string'
       or nullif(btrim(j.value #>> '{}'),'') is null
  ) then
    raise exception 'invalid_shared_checks_delete_intent' using errcode='22023';
  end if;

  select coalesce(array_agg(id order by id),'{}'::text[])
    into v_declared
  from (
    select distinct btrim(j.value #>> '{}') as id
    from jsonb_array_elements(v_declared_json) j(value)
  ) declared;

  if cardinality(v_declared) <> jsonb_array_length(v_declared_json) then
    raise exception 'duplicate_shared_checks_delete_intent' using errcode='22023';
  end if;
  if v_declared is distinct from v_removed then
    raise exception 'shared_checks_delete_intent_mismatch'
      using errcode='PT422', hint='deletedCheckIds must exactly match the check IDs removed by this update.';
  end if;
  return new;
end;
$function$;
ALTER FUNCTION "netunim_internal"."shared_checks_delete_intent_guard"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION netunim_internal.track_kupa_core_updated_at()
 RETURNS trigger
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
begin
  if tg_op = 'INSERT' then
    new.core_updated_at := coalesce(new.core_updated_at, new.updated_at, clock_timestamp());
  elsif netunim_internal.kupa_header_owned_state(new.state)
        is distinct from netunim_internal.kupa_header_owned_state(old.state) then
    new.core_updated_at := coalesce(new.updated_at, clock_timestamp());
  else
    -- Finance/bank bookkeeping may still advance revision + updated_at for concurrency safety.
    -- Preserve the Kupa-owned clock so the top header does not pretend user data changed.
    new.core_updated_at := old.core_updated_at;
  end if;
  return new;
end;
$function$;
ALTER FUNCTION "netunim_internal"."track_kupa_core_updated_at"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.acknowledge_bank_transaction_alert(p_transaction_id bigint, p_alert_kind text)
 RETURNS TABLE(transaction_id bigint, alert_kind text, acknowledged_at timestamp with time zone)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_owner uuid:=auth.uid();
  v_kind text:=btrim(coalesce(p_alert_kind,''));
  v_at timestamptz:=now();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_transaction_id is null or p_transaction_id<=0 then raise exception 'invalid_bank_transaction_id' using errcode='22023'; end if;
  if v_kind not in ('returned_cheque') then raise exception 'unsupported_bank_alert_kind' using errcode='22023'; end if;

  update public.bank_transactions b
     set alert_acknowledgements=coalesce(b.alert_acknowledgements,'{}'::jsonb) || jsonb_build_object(v_kind,v_at)
   where b.id=p_transaction_id and b.owner_id=v_owner
  returning b.id into transaction_id;

  if transaction_id is null then
    raise exception 'bank_alert_transaction_not_found'
      using errcode='P0002',hint='The bank transaction does not exist or is not owned by this user.';
  end if;

  alert_kind:=v_kind;
  acknowledged_at:=v_at;
  return next;
end $function$;
ALTER FUNCTION "public"."acknowledge_bank_transaction_alert"(p_transaction_id bigint, p_alert_kind text) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.acknowledge_bank_transaction_missing(p_transaction_id bigint)
 RETURNS TABLE(transaction_id bigint, acknowledged_at timestamp with time zone)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_owner uuid:=auth.uid();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_transaction_id is null or p_transaction_id<=0 then raise exception 'invalid_bank_transaction_id' using errcode='22023'; end if;

  update public.bank_transactions b
     set missing_acknowledged_at=coalesce(b.missing_acknowledged_at,now())
   where b.id=p_transaction_id and b.owner_id=v_owner and b.presence_state='missing'
  returning b.id,b.missing_acknowledged_at into transaction_id,acknowledged_at;

  if transaction_id is null then
    raise exception 'bank_missing_transaction_not_found'
      using errcode='P0002',hint='The row is not a missing bank transaction owned by this user.';
  end if;
  return next;
end $function$;
ALTER FUNCTION "public"."acknowledge_bank_transaction_missing"(p_transaction_id bigint) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.apply_restore_group_v5(p_restore_group_id uuid)
 RETURNS TABLE(restore_group_id uuid, phase text, main_revision bigint, checks_revision bigint)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();v_group netunim_internal.restore_operation_groups%rowtype;v_main record;v_checks record;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  select * into v_group from netunim_internal.restore_operation_groups g where g.owner_id=v_owner and g.restore_group_id=p_restore_group_id for update;
  if not found then raise exception 'restore_group_missing' using errcode='P0002';end if;
  if v_group.phase='completed' then
    select applied_revision into main_revision from netunim_internal.document_sync_operations where owner_id=v_owner and domain=v_group.app_site and document_name=v_group.main_document_name and operation_id=v_group.main_operation_id;
    if v_group.checks_state is not null then select applied_revision into checks_revision from netunim_internal.document_sync_operations where owner_id=v_owner and domain='shared-checks' and document_name=v_group.checks_document_name and operation_id=v_group.checks_operation_id;end if;
    restore_group_id:=v_group.restore_group_id;phase:=v_group.phase;return next;return;
  end if;
  perform set_config('app.destructive_operation_kind','restore',true);
  -- Failed applies deliberately leave the last durable phase unchanged: this entire
  -- function is atomic, so an exception also rolls back phase bookkeeping. Operators
  -- must monitor the failed RPC/database log, not infer a durable error from this row.
  update netunim_internal.restore_operation_groups set phase='main_pending',updated_at=now() where owner_id=v_owner and restore_group_id=p_restore_group_id;
  perform netunim_internal.capture_safety_snapshot(v_owner,v_group.app_site,v_group.main_document_name,v_group.main_operation_id,'restore',v_group.restore_group_id);
  if v_group.app_site='orders' then
    select * into v_main from public.save_order_management_document_v5(v_group.main_document_name,v_group.main_base_revision,v_group.main_state,v_group.main_operation_id,v_group.main_delete_intents,v_group.audit||jsonb_build_object('mutationType','restore','restoreGroupId',v_group.restore_group_id));
  else
    select * into v_main from public.save_kupa_document_v5(v_group.main_document_name,v_group.main_base_revision,v_group.main_state,v_group.main_operation_id,v_group.main_delete_intents,v_group.audit||jsonb_build_object('mutationType','restore','restoreGroupId',v_group.restore_group_id));
  end if;
  update netunim_internal.restore_operation_groups set phase='main_acked',updated_at=now() where owner_id=v_owner and restore_group_id=p_restore_group_id;
  if v_group.checks_state is not null then
    update netunim_internal.restore_operation_groups set phase='checks_pending',updated_at=now() where owner_id=v_owner and restore_group_id=p_restore_group_id;
    perform netunim_internal.capture_safety_snapshot(v_owner,'shared-checks',v_group.checks_document_name,v_group.checks_operation_id,'restore',v_group.restore_group_id);
    select * into v_checks from public.save_shared_checks_document_v5(v_group.checks_document_name,v_group.checks_base_revision,v_group.checks_state,v_group.checks_operation_id,v_group.checks_delete_ids,v_group.audit||jsonb_build_object('mutationType','restore','restoreGroupId',v_group.restore_group_id));
    update netunim_internal.restore_operation_groups set phase='checks_acked',updated_at=now() where owner_id=v_owner and restore_group_id=p_restore_group_id;
  end if;
  perform set_config('app.destructive_operation_kind','',true);
  update netunim_internal.restore_operation_groups set phase='completed',completed_at=now(),updated_at=now() where owner_id=v_owner and restore_group_id=p_restore_group_id;
  restore_group_id:=v_group.restore_group_id;phase:='completed';main_revision:=v_main.revision;checks_revision:=case when v_group.checks_state is null then null else v_checks.revision end;return next;
end
$function$;
ALTER FUNCTION "public"."apply_restore_group_v5"(p_restore_group_id uuid) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.bulk_delete_save_kupa_document_v5(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();v_result record;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  perform set_config('app.destructive_operation_kind','bulk-delete',true);
  perform netunim_internal.capture_safety_snapshot(v_owner,'kupa',p_document_name,p_operation_id,'bulk-delete');
  select * into v_result from public.save_kupa_document_v5(p_document_name,p_expected_revision,p_state,p_operation_id,p_delete_intents,coalesce(p_audit,'{}'::jsonb)||jsonb_build_object('mutationType','bulk-delete'));
  perform set_config('app.destructive_operation_kind','',true);
  return query select v_result.revision,v_result.updated_at,v_result.state,v_result.operation_replayed,v_result.operation_revision;
end
$function$;
ALTER FUNCTION "public"."bulk_delete_save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.bulk_delete_save_order_management_document_v5(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();v_result record;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  perform set_config('app.destructive_operation_kind','bulk-delete',true);
  perform netunim_internal.capture_safety_snapshot(v_owner,'orders',p_document_name,p_operation_id,'bulk-delete');
  select * into v_result from public.save_order_management_document_v5(p_document_name,p_expected_revision,p_state,p_operation_id,p_delete_intents,coalesce(p_audit,'{}'::jsonb)||jsonb_build_object('mutationType','bulk-delete'));
  perform set_config('app.destructive_operation_kind','',true);
  return query select v_result.revision,v_result.updated_at,v_result.state,v_result.operation_replayed,v_result.operation_revision;
end
$function$;
ALTER FUNCTION "public"."bulk_delete_save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.bulk_delete_save_shared_checks_document_v5(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();v_result record;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  perform set_config('app.destructive_operation_kind','bulk-delete',true);
  perform netunim_internal.capture_safety_snapshot(v_owner,'shared-checks',p_document_name,p_operation_id,'bulk-delete');
  select * into v_result from public.save_shared_checks_document_v5(p_document_name,p_expected_revision,p_state,p_operation_id,p_deleted_check_ids,coalesce(p_audit,'{}'::jsonb)||jsonb_build_object('mutationType','bulk-delete'));
  perform set_config('app.destructive_operation_kind','',true);
  return query select v_result.revision,v_result.updated_at,v_result.state,v_result.operation_replayed,v_result.operation_revision;
end
$function$;
ALTER FUNCTION "public"."bulk_delete_save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.claim_finance_sync_lease(p_lease_name text, p_lease_token text, p_ttl_seconds integer DEFAULT 1200)
 RETURNS TABLE(acquired boolean, leased_until timestamp with time zone, lease_token text, fence_epoch bigint)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare v_owner uuid:=auth.uid();v_now timestamptz:=clock_timestamp();
begin
 if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
 if p_lease_name not in ('bank','credit') or p_lease_name is null or coalesce(btrim(p_lease_token),'')='' or length(p_lease_token)>200 or p_ttl_seconds is null or p_ttl_seconds<60 or p_ttl_seconds>1800 then raise exception 'invalid_finance_sync_lease' using errcode='22023';end if;
 insert into public.finance_sync_leases as l(owner_id,lease_name,lease_token,leased_until,fence_epoch)
 values(v_owner,p_lease_name,p_lease_token,v_now+make_interval(secs=>p_ttl_seconds),1)
 on conflict(owner_id,lease_name) do update set lease_token=excluded.lease_token,
 leased_until=clock_timestamp()+make_interval(secs=>p_ttl_seconds),updated_at=clock_timestamp(),
 fence_epoch=case when l.lease_token=excluded.lease_token and l.leased_until>clock_timestamp() then l.fence_epoch else l.fence_epoch+1 end
 where l.leased_until<=clock_timestamp() or l.lease_token=excluded.lease_token
 returning true,l.leased_until,l.lease_token,l.fence_epoch into acquired,leased_until,lease_token,fence_epoch;
 if not found then select false,l.leased_until,l.lease_token,l.fence_epoch into acquired,leased_until,lease_token,fence_epoch from public.finance_sync_leases l where l.owner_id=v_owner and l.lease_name=p_lease_name;end if;
 return next;
end $function$;
ALTER FUNCTION "public"."claim_finance_sync_lease"(p_lease_name text, p_lease_token text, p_ttl_seconds integer) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.get_netunim_sync_capabilities()
 RETURNS jsonb
 LANGUAGE sql
 STABLE SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
 select jsonb_build_object(
 'documentOperationLedger',case when to_regclass('netunim_internal.document_sync_operations') is not null then 3 else 0 end,
 'syncIntegrity',case when to_regprocedure('netunim_internal.document_invariant_guard()') is not null then 5 else 0 end,
 'deleteIntents',case when (select count(*) from pg_trigger where tgname in ('order_management_delete_intent_guard','kupa_delete_intent_guard','shared_checks_delete_intent_guard') and tgenabled<>'D')=3 then 4 else 0 end,
 'massDeleteGuard',case when (select count(*) from pg_trigger where tgname in ('order_management_mass_destructive_guard','kupa_mass_destructive_guard','shared_checks_mass_destructive_guard') and tgenabled<>'D')=3 then 5 else 0 end,
 'restoreGroups',case when to_regclass('netunim_internal.restore_operation_groups') is not null and to_regprocedure('public.apply_restore_group_v5(uuid)') is not null then 5 else 0 end,
 'sharedChecksIntegrity',case when to_regprocedure('public.save_shared_checks_document_v5(text,bigint,jsonb,text,jsonb,jsonb)') is not null then 5 else 0 end,
 'financeFencing',case when to_regprocedure('public.save_bank_sync_snapshot(text,jsonb,text,bigint,text,text,bigint)') is not null and to_regprocedure('public.save_finance_sync_document_v5(text,bigint,jsonb,text,jsonb,text,text,bigint)') is not null then 1 else 0 end);
$function$;
ALTER FUNCTION "public"."get_netunim_sync_capabilities"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.kupa_guard_document_write()
 RETURNS trigger
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public'
AS $function$
begin
  if current_user = 'authenticated'
     and coalesce(current_setting('app.kupa_rpc_write', true), '') <> '1' then
    raise exception 'direct_kupa_write_forbidden'
      using errcode = '42501',
            hint = 'Use save_kupa_document RPC so revision checks and backups cannot be bypassed.';
  end if;
  if tg_op = 'UPDATE' and (
       new.owner_id is distinct from old.owner_id
       or new.document_name is distinct from old.document_name
     ) then
    raise exception 'kupa_document_identity_change_forbidden' using errcode = '22023';
  end if;
  return new;
end;
$function$;
ALTER FUNCTION "public"."kupa_guard_document_write"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.list_incomplete_restore_groups_v5()
 RETURNS TABLE(restore_group_id uuid, app_site text, phase text, created_at timestamp with time zone, updated_at timestamp with time zone)
 LANGUAGE sql
 STABLE
 SET search_path TO 'pg_catalog', 'netunim_internal'
AS $function$
  select g.restore_group_id,g.app_site,g.phase,g.created_at,g.updated_at from netunim_internal.restore_operation_groups g
  where g.owner_id=auth.uid() and g.phase<>'completed' order by g.created_at
$function$;
ALTER FUNCTION "public"."list_incomplete_restore_groups_v5"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.merge_bank_transactions(p_account_key text, p_account_role text, p_transactions jsonb, p_lease_name text DEFAULT NULL::text, p_lease_token text DEFAULT NULL::text, p_fence_epoch bigint DEFAULT NULL::bigint)
 RETURNS TABLE(inserted_count integer, updated_count integer, total_count integer)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
begin
 if p_lease_name is distinct from 'bank' then raise exception 'stale_finance_sync_fence' using errcode='PT409';end if;
 perform netunim_internal.assert_finance_sync_fence(p_lease_name,p_lease_token,p_fence_epoch);
 return query select * from netunim_internal.fenced_impl_merge_bank_transactions(p_account_key,p_account_role,p_transactions);
end $function$;
ALTER FUNCTION "public"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.order_management_guard_document_write()
 RETURNS trigger
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public'
AS $function$
begin
  if current_user = 'authenticated'
     and coalesce(current_setting('app.order_management_rpc_write', true), '') <> '1' then
    raise exception 'direct_order_management_write_forbidden'
      using errcode = '42501',
            hint = 'Use save_order_management_document RPC so revision checks and backups cannot be bypassed.';
  end if;
  if tg_op = 'UPDATE' and (
       new.owner_id is distinct from old.owner_id
       or new.document_name is distinct from old.document_name
     ) then
    raise exception 'order_management_identity_change_forbidden' using errcode = '22023';
  end if;
  return new;
end;
$function$;
ALTER FUNCTION "public"."order_management_guard_document_write"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.release_finance_sync_lease(p_lease_name text, p_lease_token text)
 RETURNS boolean
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public'
AS $function$
declare
  v_owner uuid:=auth.uid();
  v_name text:=btrim(coalesce(p_lease_name,''));
  v_token text:=btrim(coalesce(p_lease_token,''));
  v_now timestamptz:=clock_timestamp();
  v_released boolean:=false;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if v_name not in ('bank','credit') then raise exception 'invalid_finance_sync_lease_name' using errcode='22023'; end if;
  if v_token='' or length(v_token)>200 then raise exception 'invalid_finance_sync_lease_token' using errcode='22023'; end if;

  update public.finance_sync_leases lease
  set leased_until=v_now,updated_at=v_now
  where lease.owner_id=v_owner and lease.lease_name=v_name and lease.lease_token=v_token
  returning true into v_released;
  return coalesce(v_released,false);
end $function$;
ALTER FUNCTION "public"."release_finance_sync_lease"(p_lease_name text, p_lease_token text) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.rls_auto_enable()
 RETURNS event_trigger
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog'
AS $function$
DECLARE
  cmd record;
BEGIN
  FOR cmd IN
    SELECT *
    FROM pg_event_trigger_ddl_commands()
    WHERE command_tag IN ('CREATE TABLE', 'CREATE TABLE AS', 'SELECT INTO')
      AND object_type IN ('table','partitioned table')
  LOOP
     IF cmd.schema_name IS NOT NULL AND cmd.schema_name IN ('public') AND cmd.schema_name NOT IN ('pg_catalog','information_schema') AND cmd.schema_name NOT LIKE 'pg_toast%' AND cmd.schema_name NOT LIKE 'pg_temp%' THEN
      BEGIN
        EXECUTE format('alter table if exists %s enable row level security', cmd.object_identity);
        RAISE LOG 'rls_auto_enable: enabled RLS on %', cmd.object_identity;
      EXCEPTION
        WHEN OTHERS THEN
          RAISE LOG 'rls_auto_enable: failed to enable RLS on %', cmd.object_identity;
      END;
     ELSE
        RAISE LOG 'rls_auto_enable: skip % (either system schema or not in enforced list: %.)', cmd.object_identity, cmd.schema_name;
     END IF;
  END LOOP;
END;
$function$;
ALTER FUNCTION "public"."rls_auto_enable"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_bank_sync_snapshot(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint, p_lease_name text DEFAULT NULL::text, p_lease_token text DEFAULT NULL::text, p_fence_epoch bigint DEFAULT NULL::bigint)
 RETURNS TABLE(finance_revision bigint, kupa_revision bigint, updated_at timestamp with time zone)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
begin
 if p_lease_name is distinct from 'bank' then raise exception 'stale_finance_sync_fence' using errcode='PT409';end if;
 perform netunim_internal.assert_finance_sync_fence(p_lease_name,p_lease_token,p_fence_epoch);
 return query select * from netunim_internal.fenced_impl_save_bank_sync_snapshot(p_document_name,p_bank_state,p_snapshot_token,p_snapshot_seq);
end $function$;
ALTER FUNCTION "public"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint, p_lease_name text, p_lease_token text, p_fence_epoch bigint) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_finance_sync_document(p_document_name text, p_expected_revision bigint, p_state jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  begin
    return query select * from netunim_internal.save_finance_sync_document(p_document_name,p_expected_revision,p_state);
  exception when lock_not_available then
    raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.';
  end;
end $function$;
ALTER FUNCTION "public"."save_finance_sync_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_finance_sync_document_v3(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal', 'extensions'
AS $function$
declare
  v_owner uuid:=auth.uid();v_payload_hash text;v_ledger_hash text;v_applied_revision bigint;
  v_saved_revision bigint;v_saved_updated_at timestamptz;v_saved_state jsonb;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_expected_revision is null or p_expected_revision<0 then raise exception 'invalid_expected_revision' using errcode='22023'; end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_finance_sync_state' using errcode='22023'; end if;
  if coalesce(btrim(p_operation_id),'')='' or length(p_operation_id)>200 then raise exception 'invalid_operation_id' using errcode='22023'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  v_payload_hash:=encode(extensions.digest(convert_to(p_state::text,'UTF8'),'sha256'),'hex');
  select o.payload_sha256,o.applied_revision into v_ledger_hash,v_applied_revision
  from netunim_internal.document_sync_operations o
  where o.owner_id=v_owner and o.domain='finance' and o.document_name=p_document_name and o.operation_id=p_operation_id;
  if found then
    if v_ledger_hash<>v_payload_hash then raise exception 'idempotency_key_reuse' using errcode='PT422',hint='An operation id cannot be reused with a different payload.'; end if;
    select d.revision,d.updated_at,d.state into revision,updated_at,state from public.finance_sync_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
    if revision is null then raise exception 'operation_replay_document_missing' using errcode='P0002'; end if;
    operation_replayed:=true;operation_revision:=v_applied_revision;return next;return;
  end if;
  begin
    select x.revision,x.updated_at,x.state into v_saved_revision,v_saved_updated_at,v_saved_state
    from netunim_internal.save_finance_sync_document(p_document_name,p_expected_revision,p_state) x;
  exception when lock_not_available then raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.'; end;
  if v_saved_revision is null then raise exception 'save_result_missing' using errcode='P0002'; end if;
  insert into netunim_internal.document_sync_operations(owner_id,domain,document_name,operation_id,payload_sha256,applied_revision,created_at)
  values(v_owner,'finance',p_document_name,p_operation_id,v_payload_hash,v_saved_revision,coalesce(v_saved_updated_at,now()));
  select d.revision,d.updated_at,d.state into revision,updated_at,state from public.finance_sync_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  operation_replayed:=false;operation_revision:=v_saved_revision;return next;
end $function$;
ALTER FUNCTION "public"."save_finance_sync_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_finance_sync_document_v5(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb, p_lease_name text DEFAULT NULL::text, p_lease_token text DEFAULT NULL::text, p_fence_epoch bigint DEFAULT NULL::bigint)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
begin
 perform netunim_internal.assert_finance_sync_fence(p_lease_name,p_lease_token,p_fence_epoch);
 return query select * from netunim_internal.fenced_impl_save_finance_sync_document_v5(p_document_name,p_expected_revision,p_state,p_operation_id,p_audit);
end $function$;
ALTER FUNCTION "public"."save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_kupa_document(p_document_name text, p_expected_revision bigint, p_state jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  begin
    return query select * from netunim_internal.save_kupa_document(p_document_name,p_expected_revision,p_state);
  exception when lock_not_available then
    raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.';
  end;
end $function$;
ALTER FUNCTION "public"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_kupa_document_v3(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal', 'extensions'
AS $function$
declare
  v_owner uuid:=auth.uid();v_payload_hash text;v_ledger_hash text;v_applied_revision bigint;
  v_saved_revision bigint;v_saved_updated_at timestamptz;v_saved_state jsonb;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_expected_revision is null or p_expected_revision<0 then raise exception 'invalid_expected_revision' using errcode='22023'; end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_kupa_state' using errcode='22023'; end if;
  if coalesce(btrim(p_operation_id),'')='' or length(p_operation_id)>200 then raise exception 'invalid_operation_id' using errcode='22023'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  v_payload_hash:=encode(extensions.digest(convert_to(p_state::text,'UTF8'),'sha256'),'hex');
  select o.payload_sha256,o.applied_revision into v_ledger_hash,v_applied_revision
  from netunim_internal.document_sync_operations o
  where o.owner_id=v_owner and o.domain='kupa' and o.document_name=p_document_name and o.operation_id=p_operation_id;
  if found then
    if v_ledger_hash<>v_payload_hash then raise exception 'idempotency_key_reuse' using errcode='PT422',hint='An operation id cannot be reused with a different payload.'; end if;
    select d.revision,d.updated_at,d.state into revision,updated_at,state from public.kupa_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
    if revision is null then raise exception 'operation_replay_document_missing' using errcode='P0002'; end if;
    operation_replayed:=true;operation_revision:=v_applied_revision;return next;return;
  end if;
  begin
    select x.revision,x.updated_at,x.state into v_saved_revision,v_saved_updated_at,v_saved_state
    from netunim_internal.save_kupa_document(p_document_name,p_expected_revision,p_state) x;
  exception when lock_not_available then raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.'; end;
  if v_saved_revision is null then raise exception 'save_result_missing' using errcode='P0002'; end if;
  insert into netunim_internal.document_sync_operations(owner_id,domain,document_name,operation_id,payload_sha256,applied_revision,created_at)
  values(v_owner,'kupa',p_document_name,p_operation_id,v_payload_hash,v_saved_revision,coalesce(v_saved_updated_at,now()));
  select d.revision,d.updated_at,d.state into revision,updated_at,state from public.kupa_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  operation_replayed:=false;operation_revision:=v_saved_revision;return next;
end $function$;
ALTER FUNCTION "public"."save_kupa_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_kupa_document_v4(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal', 'extensions'
AS $function$
declare
  v_owner uuid:=auth.uid();
  v_allowed constant text[]:=array['credits','cash','rights','notes','expenses','notesSheet.rows','notesSheet.columns'];
  v_intents jsonb;
  v_payload_hash text;v_legacy_hash text;v_ledger_hash text;v_applied_revision bigint;
  v_saved_revision bigint;v_saved_updated_at timestamptz;v_saved_state jsonb;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_expected_revision is null or p_expected_revision<0 then raise exception 'invalid_expected_revision' using errcode='22023'; end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_kupa_state' using errcode='22023'; end if;
  if coalesce(btrim(p_operation_id),'')='' or length(p_operation_id)>200 then raise exception 'invalid_operation_id' using errcode='22023'; end if;
  v_intents:=netunim_internal.canonical_delete_intents(coalesce(p_delete_intents,'{}'::jsonb),v_allowed);
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then raise exception 'save_busy' using errcode='PT429'; end if;
  perform set_config('lock_timeout','100ms',true);
  v_payload_hash:=encode(extensions.digest(convert_to(jsonb_build_object('state',p_state,'deleteIntents',v_intents)::text,'UTF8'),'sha256'),'hex');
  v_legacy_hash:=encode(extensions.digest(convert_to(p_state::text,'UTF8'),'sha256'),'hex');
  select o.payload_sha256,o.applied_revision into v_ledger_hash,v_applied_revision from netunim_internal.document_sync_operations o
  where o.owner_id=v_owner and o.domain='kupa' and o.document_name=p_document_name and o.operation_id=p_operation_id;
  if found then
    if v_ledger_hash<>v_payload_hash and v_ledger_hash<>v_legacy_hash then raise exception 'idempotency_key_reuse' using errcode='PT422'; end if;
    select d.revision,d.updated_at,d.state into revision,updated_at,state from public.kupa_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
    if revision is null then raise exception 'operation_replay_document_missing' using errcode='P0002'; end if;
    operation_replayed:=true;operation_revision:=v_applied_revision;return next;return;
  end if;
  perform set_config('app.kupa_delete_intents',v_intents::text,true);
  begin
    select x.revision,x.updated_at,x.state into v_saved_revision,v_saved_updated_at,v_saved_state
    from netunim_internal.save_kupa_document(p_document_name,p_expected_revision,p_state) x;
  exception when lock_not_available then raise exception 'save_busy' using errcode='PT429'; end;
  if v_saved_revision is null then raise exception 'save_result_missing' using errcode='P0002'; end if;
  insert into netunim_internal.document_sync_operations(owner_id,domain,document_name,operation_id,payload_sha256,applied_revision,created_at)
  values(v_owner,'kupa',p_document_name,p_operation_id,v_payload_hash,v_saved_revision,coalesce(v_saved_updated_at,now()));
  select d.revision,d.updated_at,d.state into revision,updated_at,state from public.kupa_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  operation_replayed:=false;operation_revision:=v_saved_revision;return next;
end;
$function$;
ALTER FUNCTION "public"."save_kupa_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_kupa_document_v5(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();v_before jsonb;v_result record;v_paths text[]:=array['credits','cash','rights','notes','expenses','cards','notesSheet.rows','notesSheet.columns'];
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  perform netunim_internal.assert_document_invariants('kupa',p_state);
  select d.state into v_before from public.kupa_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  perform set_config('app.kupa_cards_delete_intents',coalesce(p_delete_intents->'cards','[]'::jsonb)::text,true);
  select * into v_result from public.save_kupa_document_v4(p_document_name,p_expected_revision,p_state,p_operation_id,coalesce(p_delete_intents,'{}'::jsonb)-'cards');
  perform set_config('app.kupa_cards_delete_intents','[]',true);
  perform netunim_internal.record_operation_audit(v_owner,'kupa',p_document_name,p_operation_id,coalesce(p_audit,'{}'::jsonb)||jsonb_build_object('baseRevision',p_expected_revision),coalesce(v_before,p_state),v_result.state,v_paths,null);
  return query select v_result.revision,v_result.updated_at,v_result.state,v_result.operation_replayed,v_result.operation_revision;
end
$function$;
ALTER FUNCTION "public"."save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_order_management_document(p_document_name text, p_expected_revision bigint, p_state jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('order_management:'||v_owner::text||':'||p_document_name,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another save is already in progress. Retry later without refreshing the revision.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  begin
    return query select * from netunim_internal.save_order_management_document(p_document_name,p_expected_revision,p_state);
  exception when lock_not_available then
    raise exception 'save_busy' using errcode='PT429',hint='The document row is temporarily locked. Retry later without refreshing the revision.';
  end;
end $function$;
ALTER FUNCTION "public"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_order_management_document_v3(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal', 'extensions'
AS $function$
declare
  v_owner uuid := auth.uid();
  v_payload_hash text;
  v_ledger_hash text;
  v_applied_revision bigint;
  v_saved_revision bigint;
  v_saved_updated_at timestamptz;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_expected_revision is null or p_expected_revision<0 then raise exception 'invalid_expected_revision' using errcode='22023'; end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_order_management_state' using errcode='22023'; end if;
  if coalesce(btrim(p_operation_id),'')='' or length(p_operation_id)>200 then raise exception 'invalid_operation_id' using errcode='22023'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('order_management:'||v_owner::text||':'||p_document_name,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another save is already in progress. Retry later without refreshing the revision.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  v_payload_hash:=encode(extensions.digest(convert_to(p_state::text,'UTF8'),'sha256'),'hex');

  select o.payload_sha256,o.applied_revision into v_ledger_hash,v_applied_revision
  from netunim_internal.document_sync_operations o
  where o.owner_id=v_owner and o.domain='orders' and o.document_name=p_document_name and o.operation_id=p_operation_id;
  if found then
    if v_ledger_hash<>v_payload_hash then
      raise exception 'idempotency_key_reuse' using errcode='PT422',hint='An operation id cannot be reused with a different payload.';
    end if;
    select d.revision,d.updated_at,d.state into revision,updated_at,state
    from public.order_management_documents d
    where d.owner_id=v_owner and d.document_name=p_document_name;
    if revision is null then raise exception 'operation_replay_document_missing' using errcode='P0002'; end if;
    operation_replayed:=true;operation_revision:=v_applied_revision;return next;return;
  end if;

  begin
    select x.revision,x.updated_at into v_saved_revision,v_saved_updated_at
    from netunim_internal.save_order_management_document(p_document_name,p_expected_revision,p_state) x;
  exception when lock_not_available then
    raise exception 'save_busy' using errcode='PT429',hint='The document row is temporarily locked. Retry later without refreshing the revision.';
  end;
  if v_saved_revision is null then raise exception 'save_result_missing' using errcode='P0002'; end if;
  insert into netunim_internal.document_sync_operations(owner_id,domain,document_name,operation_id,payload_sha256,applied_revision,created_at)
  values(v_owner,'orders',p_document_name,p_operation_id,v_payload_hash,v_saved_revision,coalesce(v_saved_updated_at,now()));
  select d.revision,d.updated_at,d.state into revision,updated_at,state
  from public.order_management_documents d
  where d.owner_id=v_owner and d.document_name=p_document_name;
  operation_replayed:=false;operation_revision:=v_saved_revision;return next;
end $function$;
ALTER FUNCTION "public"."save_order_management_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_order_management_document_v4(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal', 'extensions'
AS $function$
declare
  v_owner uuid:=auth.uid();
  v_allowed constant text[]:=array['suppliers','transactions','customerDebts','customerOrders','serviceCalls','notes','inventoryItems','inventoryEvents','warehouseOrders'];
  v_intents jsonb;
  v_payload_hash text;v_legacy_hash text;v_ledger_hash text;v_applied_revision bigint;
  v_saved_revision bigint;v_saved_updated_at timestamptz;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_expected_revision is null or p_expected_revision<0 then raise exception 'invalid_expected_revision' using errcode='22023'; end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_order_management_state' using errcode='22023'; end if;
  if coalesce(btrim(p_operation_id),'')='' or length(p_operation_id)>200 then raise exception 'invalid_operation_id' using errcode='22023'; end if;
  v_intents:=netunim_internal.canonical_delete_intents(coalesce(p_delete_intents,'{}'::jsonb),v_allowed);
  if not pg_try_advisory_xact_lock(hashtextextended('order_management:'||v_owner::text||':'||p_document_name,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another save is already in progress. Retry later without refreshing the revision.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  v_payload_hash:=encode(extensions.digest(convert_to(jsonb_build_object('state',p_state,'deleteIntents',v_intents)::text,'UTF8'),'sha256'),'hex');
  v_legacy_hash:=encode(extensions.digest(convert_to(p_state::text,'UTF8'),'sha256'),'hex');
  select o.payload_sha256,o.applied_revision into v_ledger_hash,v_applied_revision
  from netunim_internal.document_sync_operations o
  where o.owner_id=v_owner and o.domain='orders' and o.document_name=p_document_name and o.operation_id=p_operation_id;
  if found then
    if v_ledger_hash<>v_payload_hash and v_ledger_hash<>v_legacy_hash then raise exception 'idempotency_key_reuse' using errcode='PT422'; end if;
    select d.revision,d.updated_at,d.state into revision,updated_at,state from public.order_management_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
    if revision is null then raise exception 'operation_replay_document_missing' using errcode='P0002'; end if;
    operation_replayed:=true;operation_revision:=v_applied_revision;return next;return;
  end if;
  perform set_config('app.order_management_delete_intents',v_intents::text,true);
  begin
    select x.revision,x.updated_at into v_saved_revision,v_saved_updated_at
    from netunim_internal.save_order_management_document(p_document_name,p_expected_revision,p_state) x;
  exception when lock_not_available then raise exception 'save_busy' using errcode='PT429'; end;
  if v_saved_revision is null then raise exception 'save_result_missing' using errcode='P0002'; end if;
  insert into netunim_internal.document_sync_operations(owner_id,domain,document_name,operation_id,payload_sha256,applied_revision,created_at)
  values(v_owner,'orders',p_document_name,p_operation_id,v_payload_hash,v_saved_revision,coalesce(v_saved_updated_at,now()));
  select d.revision,d.updated_at,d.state into revision,updated_at,state from public.order_management_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  operation_replayed:=false;operation_revision:=v_saved_revision;return next;
end;
$function$;
ALTER FUNCTION "public"."save_order_management_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_order_management_document_v5(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();v_before jsonb;v_result record;v_paths text[]:=array['suppliers','transactions','customerDebts','customerOrders','serviceCalls','notes','inventoryItems','inventoryEvents','warehouseOrders'];
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  perform netunim_internal.assert_document_invariants('orders',p_state);
  select d.state into v_before from public.order_management_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  select * into v_result from public.save_order_management_document_v4(p_document_name,p_expected_revision,p_state,p_operation_id,p_delete_intents);
  perform netunim_internal.record_operation_audit(v_owner,'orders',p_document_name,p_operation_id,coalesce(p_audit,'{}'::jsonb)||jsonb_build_object('baseRevision',p_expected_revision),coalesce(v_before,p_state),v_result.state,v_paths,null);
  return query select v_result.revision,v_result.updated_at,v_result.state,v_result.operation_replayed,v_result.operation_revision;
end
$function$;
ALTER FUNCTION "public"."save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_shared_checks_document(p_document_name text, p_expected_revision bigint, p_state jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  begin
    return query select * from netunim_internal.save_shared_checks_document(p_document_name,p_expected_revision,p_state);
  exception when lock_not_available then
    raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.';
  end;
end $function$;
ALTER FUNCTION "public"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_shared_checks_document_v3(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal', 'extensions'
AS $function$
declare
  v_owner uuid:=auth.uid();v_payload_hash text;v_ledger_hash text;v_applied_revision bigint;
  v_saved_revision bigint;v_saved_updated_at timestamptz;v_saved_state jsonb;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_expected_revision is null or p_expected_revision<0 then raise exception 'invalid_expected_revision' using errcode='22023'; end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_shared_checks_state' using errcode='22023'; end if;
  if coalesce(btrim(p_operation_id),'')='' or length(p_operation_id)>200 then raise exception 'invalid_operation_id' using errcode='22023'; end if;
  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);
  v_payload_hash:=encode(extensions.digest(convert_to(p_state::text,'UTF8'),'sha256'),'hex');
  select o.payload_sha256,o.applied_revision into v_ledger_hash,v_applied_revision
  from netunim_internal.document_sync_operations o
  where o.owner_id=v_owner and o.domain='shared-checks' and o.document_name=p_document_name and o.operation_id=p_operation_id;
  if found then
    if v_ledger_hash<>v_payload_hash then raise exception 'idempotency_key_reuse' using errcode='PT422',hint='An operation id cannot be reused with a different payload.'; end if;
    select d.revision,d.updated_at,d.state into revision,updated_at,state from public.shared_checks_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
    if revision is null then raise exception 'operation_replay_document_missing' using errcode='P0002'; end if;
    operation_replayed:=true;operation_revision:=v_applied_revision;return next;return;
  end if;
  begin
    select x.revision,x.updated_at,x.state into v_saved_revision,v_saved_updated_at,v_saved_state
    from netunim_internal.save_shared_checks_document(p_document_name,p_expected_revision,p_state) x;
  exception when lock_not_available then raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.'; end;
  if v_saved_revision is null then raise exception 'save_result_missing' using errcode='P0002'; end if;
  insert into netunim_internal.document_sync_operations(owner_id,domain,document_name,operation_id,payload_sha256,applied_revision,created_at)
  values(v_owner,'shared-checks',p_document_name,p_operation_id,v_payload_hash,v_saved_revision,coalesce(v_saved_updated_at,now()));
  select d.revision,d.updated_at,d.state into revision,updated_at,state from public.shared_checks_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  operation_replayed:=false;operation_revision:=v_saved_revision;return next;
end $function$;
ALTER FUNCTION "public"."save_shared_checks_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_shared_checks_document_v4(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal', 'extensions'
AS $function$
declare
  v_owner uuid:=auth.uid();
  v_deleted_ids jsonb:='[]'::jsonb;
  v_payload_hash text;
  v_legacy_payload_hash text;
  v_ledger_hash text;
  v_applied_revision bigint;
  v_saved_revision bigint;
  v_saved_updated_at timestamptz;
  v_saved_state jsonb;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501'; end if;
  if p_document_name is null or btrim(p_document_name)='' then raise exception 'invalid_document_name' using errcode='22023'; end if;
  if p_expected_revision is null or p_expected_revision<0 then raise exception 'invalid_expected_revision' using errcode='22023'; end if;
  if p_state is null or jsonb_typeof(p_state) is distinct from 'object' then raise exception 'invalid_shared_checks_state' using errcode='22023'; end if;
  if coalesce(btrim(p_operation_id),'')='' or length(p_operation_id)>200 then raise exception 'invalid_operation_id' using errcode='22023'; end if;
  if p_deleted_check_ids is null or jsonb_typeof(p_deleted_check_ids) is distinct from 'array' then raise exception 'invalid_shared_checks_delete_intent' using errcode='22023'; end if;
  if exists (
    select 1 from jsonb_array_elements(p_deleted_check_ids) j(value)
    where jsonb_typeof(j.value) is distinct from 'string'
       or nullif(btrim(j.value #>> '{}'),'') is null
  ) then
    raise exception 'invalid_shared_checks_delete_intent' using errcode='22023';
  end if;

  select coalesce(jsonb_agg(id order by id),'[]'::jsonb)
    into v_deleted_ids
  from (
    select distinct btrim(j.value #>> '{}') as id
    from jsonb_array_elements(p_deleted_check_ids) j(value)
  ) declared;
  if jsonb_array_length(v_deleted_ids) <> jsonb_array_length(p_deleted_check_ids) then
    raise exception 'duplicate_shared_checks_delete_intent' using errcode='22023';
  end if;

  if not pg_try_advisory_xact_lock(hashtextextended('netunim_financial_write:'||v_owner::text,0)) then
    raise exception 'save_busy' using errcode='PT429',hint='Another financial save is already in progress. Retry later.';
  end if;
  perform set_config('lock_timeout','100ms',true);

  v_payload_hash:=encode(extensions.digest(convert_to(jsonb_build_object('state',p_state,'deletedCheckIds',v_deleted_ids)::text,'UTF8'),'sha256'),'hex');
  v_legacy_payload_hash:=encode(extensions.digest(convert_to(p_state::text,'UTF8'),'sha256'),'hex');
  select o.payload_sha256,o.applied_revision into v_ledger_hash,v_applied_revision
  from netunim_internal.document_sync_operations o
  where o.owner_id=v_owner and o.domain='shared-checks' and o.document_name=p_document_name and o.operation_id=p_operation_id;
  if found then
    if v_ledger_hash<>v_payload_hash and v_ledger_hash<>v_legacy_payload_hash then
      raise exception 'idempotency_key_reuse' using errcode='PT422',hint='An operation id cannot be reused with a different payload.';
    end if;
    select d.revision,d.updated_at,d.state into revision,updated_at,state
    from public.shared_checks_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
    if revision is null then raise exception 'operation_replay_document_missing' using errcode='P0002'; end if;
    operation_replayed:=true;operation_revision:=v_applied_revision;return next;return;
  end if;

  perform set_config('app.shared_checks_delete_ids',v_deleted_ids::text,true);
  begin
    select x.revision,x.updated_at,x.state into v_saved_revision,v_saved_updated_at,v_saved_state
    from netunim_internal.save_shared_checks_document(p_document_name,p_expected_revision,p_state) x;
  exception when lock_not_available then
    raise exception 'save_busy' using errcode='PT429',hint='A financial row is temporarily locked. Retry later.';
  end;
  if v_saved_revision is null then raise exception 'save_result_missing' using errcode='P0002'; end if;

  insert into netunim_internal.document_sync_operations(owner_id,domain,document_name,operation_id,payload_sha256,applied_revision,created_at)
  values(v_owner,'shared-checks',p_document_name,p_operation_id,v_payload_hash,v_saved_revision,coalesce(v_saved_updated_at,now()));
  select d.revision,d.updated_at,d.state into revision,updated_at,state
  from public.shared_checks_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  operation_replayed:=false;operation_revision:=v_saved_revision;return next;
end;
$function$;
ALTER FUNCTION "public"."save_shared_checks_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.save_shared_checks_document_v5(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb)
 RETURNS TABLE(revision bigint, updated_at timestamp with time zone, state jsonb, operation_replayed boolean, operation_revision bigint)
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
declare v_owner uuid:=auth.uid();v_before jsonb;v_result record;v_paths text[]:=array['checks'];
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  if jsonb_typeof(p_state->'bankEvents') is distinct from 'array' then p_state:=p_state||jsonb_build_object('bankEvents','[]'::jsonb);end if;
  perform netunim_internal.assert_document_invariants('shared-checks',p_state);
  select d.state into v_before from public.shared_checks_documents d where d.owner_id=v_owner and d.document_name=p_document_name;
  select * into v_result from public.save_shared_checks_document_v4(p_document_name,p_expected_revision,p_state,p_operation_id,p_deleted_check_ids);
  perform netunim_internal.record_operation_audit(v_owner,'shared-checks',p_document_name,p_operation_id,coalesce(p_audit,'{}'::jsonb)||jsonb_build_object('baseRevision',p_expected_revision),coalesce(v_before,p_state),v_result.state,v_paths,null);
  return query select v_result.revision,v_result.updated_at,v_result.state,v_result.operation_replayed,v_result.operation_revision;
end
$function$;
ALTER FUNCTION "public"."save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.shared_checks_guard_document_write()
 RETURNS trigger
 LANGUAGE plpgsql
 SET search_path TO 'pg_catalog', 'public'
AS $function$
begin
  if current_user = 'authenticated'
     and coalesce(current_setting('app.shared_checks_rpc_write', true), '') <> '1' then
    raise exception 'direct_shared_checks_write_forbidden'
      using errcode = '42501',
            hint = 'Use save_shared_checks_document RPC so revision checks and backups cannot be bypassed.';
  end if;
  if tg_op = 'UPDATE' and (
       new.owner_id is distinct from old.owner_id
       or new.document_name is distinct from old.document_name
     ) then
    raise exception 'shared_checks_identity_change_forbidden' using errcode = '22023';
  end if;
  return new;
end;
$function$;
ALTER FUNCTION "public"."shared_checks_guard_document_write"() OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.stage_restore_group_v5(p_restore_group_id uuid, p_app_site text, p_main_document_name text, p_main_base_revision bigint, p_main_state jsonb, p_main_delete_intents jsonb, p_checks_document_name text, p_checks_base_revision bigint, p_checks_state jsonb, p_checks_delete_ids jsonb, p_main_operation_id text, p_checks_operation_id text, p_audit jsonb)
 RETURNS TABLE(restore_group_id uuid, phase text)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'netunim_internal', 'extensions'
AS $function$
declare v_owner uuid:=auth.uid();v_main_hash text;v_checks_hash text;v_existing netunim_internal.restore_operation_groups%rowtype;
begin
  if v_owner is null then raise exception 'not_authenticated' using errcode='42501';end if;
  if p_restore_group_id is null or p_app_site not in ('kupa','orders') or p_main_base_revision<0 then raise exception 'invalid_restore_group' using errcode='22023';end if;
  perform netunim_internal.assert_document_invariants(p_app_site,p_main_state);
  if p_checks_state is not null then
    if jsonb_typeof(p_checks_state->'bankEvents') is distinct from 'array' then p_checks_state:=p_checks_state||jsonb_build_object('bankEvents','[]'::jsonb);end if;
    perform netunim_internal.assert_document_invariants('shared-checks',p_checks_state);
    if p_checks_base_revision is null or p_checks_base_revision<0 or coalesce(btrim(p_checks_operation_id),'')='' then raise exception 'invalid_restore_checks_target' using errcode='22023';end if;
  end if;
  if coalesce(btrim(p_main_operation_id),'')='' then raise exception 'invalid_restore_operation_id' using errcode='22023';end if;
  v_main_hash:=encode(extensions.digest(convert_to(jsonb_build_object('state',p_main_state,'deleteIntents',coalesce(p_main_delete_intents,'{}'::jsonb))::text,'UTF8'),'sha256'),'hex');
  v_checks_hash:=case when p_checks_state is null then null else encode(extensions.digest(convert_to(jsonb_build_object('state',p_checks_state,'deleteIds',coalesce(p_checks_delete_ids,'[]'::jsonb))::text,'UTF8'),'sha256'),'hex') end;
  insert into netunim_internal.restore_operation_groups(owner_id,restore_group_id,app_site,main_document_name,main_base_revision,checks_document_name,checks_base_revision,main_state,checks_state,main_delete_intents,checks_delete_ids,main_payload_sha256,checks_payload_sha256,main_operation_id,checks_operation_id,phase,audit)
  values(v_owner,p_restore_group_id,p_app_site,p_main_document_name,p_main_base_revision,coalesce(nullif(p_checks_document_name,''),'main'),p_checks_base_revision,p_main_state,p_checks_state,coalesce(p_main_delete_intents,'{}'::jsonb),coalesce(p_checks_delete_ids,'[]'::jsonb),v_main_hash,v_checks_hash,p_main_operation_id,p_checks_operation_id,'staged',coalesce(p_audit,'{}'::jsonb))
  on conflict do nothing;
  select * into v_existing from netunim_internal.restore_operation_groups g where g.owner_id=v_owner and g.restore_group_id=p_restore_group_id;
  if v_existing.main_payload_sha256<>v_main_hash or v_existing.checks_payload_sha256 is distinct from v_checks_hash or v_existing.app_site<>p_app_site then
    raise exception 'restore_group_id_reuse' using errcode='PT422';
  end if;
  return query select v_existing.restore_group_id,v_existing.phase;
end
$function$;
ALTER FUNCTION "public"."stage_restore_group_v5"(p_restore_group_id uuid, p_app_site text, p_main_document_name text, p_main_base_revision bigint, p_main_state jsonb, p_main_delete_intents jsonb, p_checks_document_name text, p_checks_base_revision bigint, p_checks_state jsonb, p_checks_delete_ids jsonb, p_main_operation_id text, p_checks_operation_id text, p_audit jsonb) OWNER TO "postgres";
CREATE OR REPLACE FUNCTION public.sync_bank_transactions_snapshot(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean, p_lease_name text DEFAULT NULL::text, p_lease_token text DEFAULT NULL::text, p_fence_epoch bigint DEFAULT NULL::bigint)
 RETURNS TABLE(inserted_count integer, updated_count integer, total_count integer, missing_count integer, active_missing_count integer, snapshot_stored boolean, baseline_created boolean)
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'pg_catalog', 'public', 'netunim_internal'
AS $function$
begin
 if p_lease_name is distinct from 'bank' then raise exception 'stale_finance_sync_fence' using errcode='PT409';end if;
 perform netunim_internal.assert_finance_sync_fence(p_lease_name,p_lease_token,p_fence_epoch);
 return query select * from netunim_internal.fenced_impl_sync_bank_transactions_snapshot(p_account_key,p_account_role,p_transactions,p_snapshot_at,p_coverage_from,p_coverage_to,p_complete);
end $function$;
ALTER FUNCTION "public"."sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean, p_lease_name text, p_lease_token text, p_fence_epoch bigint) OWNER TO "postgres";
ALTER TABLE "netunim_internal"."bank_alert_rollout_baselines" ADD CONSTRAINT "bank_alert_rollout_baselines_alert_kind_check" CHECK (alert_kind = 'returned_cheque'::text);
ALTER TABLE "netunim_internal"."bank_alert_rollout_baselines" ADD CONSTRAINT "bank_alert_rollout_baselines_pkey" PRIMARY KEY (owner_id, alert_kind);
ALTER TABLE "netunim_internal"."bank_sync_operations" ADD CONSTRAINT "bank_sync_operations_operation_id_check" CHECK (length(operation_id) >= 1 AND length(operation_id) <= 200);
ALTER TABLE "netunim_internal"."bank_sync_operations" ADD CONSTRAINT "bank_sync_operations_payload_sha256_check" CHECK (length(payload_sha256) = 64);
ALTER TABLE "netunim_internal"."bank_sync_operations" ADD CONSTRAINT "bank_sync_operations_pkey" PRIMARY KEY (owner_id, document_name, operation_id);
ALTER TABLE "netunim_internal"."bank_sync_operations" ADD CONSTRAINT "bank_sync_operations_snapshot_seq_check" CHECK (snapshot_seq >= 0);
ALTER TABLE "netunim_internal"."document_sync_operations" ADD CONSTRAINT "document_sync_operations_applied_revision_check" CHECK (applied_revision >= 1);
ALTER TABLE "netunim_internal"."document_sync_operations" ADD CONSTRAINT "document_sync_operations_domain_check" CHECK (domain = ANY (ARRAY['orders'::text, 'kupa'::text, 'shared-checks'::text, 'finance'::text]));
ALTER TABLE "netunim_internal"."document_sync_operations" ADD CONSTRAINT "document_sync_operations_operation_id_check" CHECK (length(operation_id) >= 1 AND length(operation_id) <= 200);
ALTER TABLE "netunim_internal"."document_sync_operations" ADD CONSTRAINT "document_sync_operations_payload_sha256_check" CHECK (length(payload_sha256) = 64);
ALTER TABLE "netunim_internal"."document_sync_operations" ADD CONSTRAINT "document_sync_operations_pkey" PRIMARY KEY (owner_id, domain, document_name, operation_id);
ALTER TABLE "netunim_internal"."restore_operation_groups" ADD CONSTRAINT "restore_operation_groups_app_site_check" CHECK (app_site = ANY (ARRAY['kupa'::text, 'orders'::text]));
ALTER TABLE "netunim_internal"."restore_operation_groups" ADD CONSTRAINT "restore_operation_groups_main_base_revision_check" CHECK (main_base_revision >= 0);
ALTER TABLE "netunim_internal"."restore_operation_groups" ADD CONSTRAINT "restore_operation_groups_main_state_check" CHECK (jsonb_typeof(main_state) = 'object'::text);
ALTER TABLE "netunim_internal"."restore_operation_groups" ADD CONSTRAINT "restore_operation_groups_phase_check" CHECK (phase = ANY (ARRAY['staged'::text, 'main_pending'::text, 'main_acked'::text, 'checks_pending'::text, 'checks_acked'::text, 'completed'::text]));
ALTER TABLE "netunim_internal"."restore_operation_groups" ADD CONSTRAINT "restore_operation_groups_pkey" PRIMARY KEY (owner_id, restore_group_id);
ALTER TABLE "netunim_internal"."safety_snapshots" ADD CONSTRAINT "safety_snapshots_operation_kind_check" CHECK (operation_kind = ANY (ARRAY['bulk-delete'::text, 'restore'::text, 'destructive-migration'::text]));
ALTER TABLE "netunim_internal"."safety_snapshots" ADD CONSTRAINT "safety_snapshots_pkey" PRIMARY KEY (owner_id, domain, document_name, operation_id);
ALTER TABLE "netunim_internal"."safety_snapshots" ADD CONSTRAINT "safety_snapshots_revision_check" CHECK (revision > 0);
ALTER TABLE "netunim_internal"."safety_snapshots" ADD CONSTRAINT "safety_snapshots_state_check" CHECK (jsonb_typeof(state) = 'object'::text);
ALTER TABLE "public"."bank_transaction_snapshots" ADD CONSTRAINT "bank_transaction_snapshots_account_role_check" CHECK (account_role = ANY (ARRAY['business'::text, 'home'::text]));
ALTER TABLE "public"."bank_transaction_snapshots" ADD CONSTRAINT "bank_transaction_snapshots_check" CHECK (coverage_to >= coverage_from);
ALTER TABLE "public"."bank_transaction_snapshots" ADD CONSTRAINT "bank_transaction_snapshots_pkey" PRIMARY KEY (owner_id, account_key, account_role);
ALTER TABLE "public"."bank_transaction_snapshots" ADD CONSTRAINT "bank_transaction_snapshots_transaction_count_check" CHECK (transaction_count >= 0);
ALTER TABLE "public"."bank_transaction_snapshots" ADD CONSTRAINT "bank_transaction_snapshots_transactions_check" CHECK (jsonb_typeof(transactions) = 'array'::text);
ALTER TABLE "public"."bank_transactions" ADD CONSTRAINT "bank_transactions_account_role_check" CHECK (account_role = ANY (ARRAY['business'::text, 'home'::text]));
ALTER TABLE "public"."bank_transactions" ADD CONSTRAINT "bank_transactions_alert_acknowledgements_object_check" CHECK (jsonb_typeof(alert_acknowledgements) = 'object'::text);
ALTER TABLE "public"."bank_transactions" ADD CONSTRAINT "bank_transactions_owner_id_account_key_merge_key_key" UNIQUE (owner_id, account_key, merge_key);
ALTER TABLE "public"."bank_transactions" ADD CONSTRAINT "bank_transactions_pkey" PRIMARY KEY (id);
ALTER TABLE "public"."bank_transactions" ADD CONSTRAINT "bank_transactions_presence_state_check" CHECK (presence_state = ANY (ARRAY['unknown'::text, 'present'::text, 'missing'::text]));
ALTER TABLE "public"."bank_transactions" ADD CONSTRAINT "bank_transactions_status_check" CHECK (status = ANY (ARRAY['pending'::text, 'completed'::text]));
ALTER TABLE "public"."finance_sync_documents" ADD CONSTRAINT "finance_sync_documents_pkey" PRIMARY KEY (owner_id, document_name);
ALTER TABLE "public"."finance_sync_documents" ADD CONSTRAINT "finance_sync_documents_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."finance_sync_documents" ADD CONSTRAINT "finance_sync_documents_state_check" CHECK (jsonb_typeof(state) = 'object'::text);
ALTER TABLE "public"."finance_sync_leases" ADD CONSTRAINT "finance_sync_leases_fence_epoch_check" CHECK (fence_epoch > 0);
ALTER TABLE "public"."finance_sync_leases" ADD CONSTRAINT "finance_sync_leases_lease_name_check" CHECK (lease_name = ANY (ARRAY['bank'::text, 'credit'::text]));
ALTER TABLE "public"."finance_sync_leases" ADD CONSTRAINT "finance_sync_leases_pkey" PRIMARY KEY (owner_id, lease_name);
ALTER TABLE "public"."google_calendar_connections" ADD CONSTRAINT "google_calendar_connections_pkey" PRIMARY KEY (owner_id);
ALTER TABLE "public"."google_calendar_oauth_states" ADD CONSTRAINT "google_calendar_oauth_states_pkey" PRIMARY KEY (state_hash);
ALTER TABLE "public"."kupa_document_backups" ADD CONSTRAINT "kupa_document_backups_pkey" PRIMARY KEY (id);
ALTER TABLE "public"."kupa_document_backups" ADD CONSTRAINT "kupa_document_backups_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."kupa_document_backups" ADD CONSTRAINT "kupa_document_backups_state_shape_check" CHECK (jsonb_typeof(state) = 'object'::text AND (NOT state ? 'checks'::text OR jsonb_typeof(state -> 'checks'::text) = 'array'::text) AND jsonb_typeof(state -> 'credits'::text) = 'array'::text AND jsonb_typeof(state -> 'cash'::text) = 'array'::text AND jsonb_typeof(state -> 'expenses'::text) = 'array'::text AND jsonb_typeof(state -> 'cards'::text) = 'array'::text);
ALTER TABLE "public"."kupa_documents" ADD CONSTRAINT "kupa_documents_pkey" PRIMARY KEY (owner_id, document_name);
ALTER TABLE "public"."kupa_documents" ADD CONSTRAINT "kupa_documents_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."kupa_documents" ADD CONSTRAINT "kupa_documents_state_shape_check" CHECK (jsonb_typeof(state) = 'object'::text AND NOT state ? 'checks'::text AND jsonb_typeof(state -> 'credits'::text) = 'array'::text AND jsonb_typeof(state -> 'cash'::text) = 'array'::text AND jsonb_typeof(state -> 'expenses'::text) = 'array'::text AND jsonb_typeof(state -> 'cards'::text) = 'array'::text AND jsonb_typeof(state -> 'bank'::text) = 'object'::text AND jsonb_typeof(state #> '{bank,adjustments}'::text[]) = 'array'::text);
ALTER TABLE "public"."kupa_periodic_backups" ADD CONSTRAINT "kupa_periodic_backups_pkey" PRIMARY KEY (id);
ALTER TABLE "public"."kupa_periodic_backups" ADD CONSTRAINT "kupa_periodic_backups_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."kupa_periodic_backups" ADD CONSTRAINT "kupa_periodic_backups_state_shape_check" CHECK (jsonb_typeof(state) = 'object'::text AND (NOT state ? 'checks'::text OR jsonb_typeof(state -> 'checks'::text) = 'array'::text) AND jsonb_typeof(state -> 'credits'::text) = 'array'::text AND jsonb_typeof(state -> 'cash'::text) = 'array'::text AND jsonb_typeof(state -> 'expenses'::text) = 'array'::text AND jsonb_typeof(state -> 'cards'::text) = 'array'::text);
ALTER TABLE "public"."order_management_document_backups" ADD CONSTRAINT "order_management_document_backups_pkey" PRIMARY KEY (id);
ALTER TABLE "public"."order_management_document_backups" ADD CONSTRAINT "order_management_document_backups_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."order_management_document_backups" ADD CONSTRAINT "order_management_document_backups_state_object_check" CHECK (jsonb_typeof(state) = 'object'::text);
ALTER TABLE "public"."order_management_documents" ADD CONSTRAINT "order_management_documents_pkey" PRIMARY KEY (owner_id, document_name);
ALTER TABLE "public"."order_management_documents" ADD CONSTRAINT "order_management_documents_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."order_management_documents" ADD CONSTRAINT "order_management_documents_state_shape_check" CHECK (jsonb_typeof(state) = 'object'::text AND NOT state ? 'checks'::text AND jsonb_typeof(state -> 'suppliers'::text) = 'array'::text AND jsonb_typeof(state -> 'transactions'::text) = 'array'::text AND jsonb_typeof(state -> 'customerDebts'::text) = 'array'::text AND jsonb_typeof(state -> 'customerOrders'::text) = 'array'::text AND jsonb_typeof(state -> 'serviceCalls'::text) = 'array'::text AND jsonb_typeof(state -> 'inventoryItems'::text) = 'array'::text AND jsonb_typeof(state -> 'inventoryCategoryOrder'::text) = 'array'::text AND jsonb_typeof(state -> 'inventoryEvents'::text) = 'array'::text AND jsonb_typeof(state -> 'warehouseOrders'::text) = 'array'::text AND jsonb_typeof(state -> 'notes'::text) = 'array'::text);
ALTER TABLE "public"."order_management_periodic_backups" ADD CONSTRAINT "order_management_periodic_backups_pkey" PRIMARY KEY (id);
ALTER TABLE "public"."order_management_periodic_backups" ADD CONSTRAINT "order_management_periodic_backups_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."order_management_periodic_backups" ADD CONSTRAINT "order_management_periodic_backups_state_object_check" CHECK (jsonb_typeof(state) = 'object'::text);
ALTER TABLE "public"."shared_checks_document_backups" ADD CONSTRAINT "shared_checks_document_backups_pkey" PRIMARY KEY (id);
ALTER TABLE "public"."shared_checks_document_backups" ADD CONSTRAINT "shared_checks_document_backups_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."shared_checks_document_backups" ADD CONSTRAINT "shared_checks_document_backups_state_shape_check" CHECK (jsonb_typeof(state) = 'object'::text AND jsonb_typeof(state -> 'checks'::text) = 'array'::text AND jsonb_typeof(state -> 'bankEvents'::text) = 'array'::text);
ALTER TABLE "public"."shared_checks_documents" ADD CONSTRAINT "shared_checks_documents_pkey" PRIMARY KEY (owner_id, document_name);
ALTER TABLE "public"."shared_checks_documents" ADD CONSTRAINT "shared_checks_documents_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."shared_checks_documents" ADD CONSTRAINT "shared_checks_documents_state_shape_check" CHECK (jsonb_typeof(state) = 'object'::text AND jsonb_typeof(state -> 'checks'::text) = 'array'::text AND jsonb_typeof(state -> 'bankEvents'::text) = 'array'::text);
ALTER TABLE "public"."shared_checks_periodic_backups" ADD CONSTRAINT "shared_checks_periodic_backups_pkey" PRIMARY KEY (id);
ALTER TABLE "public"."shared_checks_periodic_backups" ADD CONSTRAINT "shared_checks_periodic_backups_revision_check" CHECK (revision > 0);
ALTER TABLE "public"."shared_checks_periodic_backups" ADD CONSTRAINT "shared_checks_periodic_backups_state_shape_check" CHECK (jsonb_typeof(state) = 'object'::text AND jsonb_typeof(state -> 'checks'::text) = 'array'::text AND jsonb_typeof(state -> 'bankEvents'::text) = 'array'::text);
ALTER TABLE "netunim_internal"."bank_alert_rollout_baselines" ADD CONSTRAINT "bank_alert_rollout_baselines_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "netunim_internal"."bank_sync_operations" ADD CONSTRAINT "bank_sync_operations_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "netunim_internal"."document_sync_operations" ADD CONSTRAINT "document_sync_operations_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "netunim_internal"."restore_operation_groups" ADD CONSTRAINT "restore_operation_groups_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "netunim_internal"."safety_snapshots" ADD CONSTRAINT "safety_snapshots_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."bank_transaction_snapshots" ADD CONSTRAINT "bank_transaction_snapshots_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."bank_transactions" ADD CONSTRAINT "bank_transactions_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."finance_sync_documents" ADD CONSTRAINT "finance_sync_documents_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."finance_sync_leases" ADD CONSTRAINT "finance_sync_leases_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."google_calendar_connections" ADD CONSTRAINT "google_calendar_connections_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."google_calendar_oauth_states" ADD CONSTRAINT "google_calendar_oauth_states_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."kupa_document_backups" ADD CONSTRAINT "kupa_document_backups_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."kupa_documents" ADD CONSTRAINT "kupa_documents_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."kupa_periodic_backups" ADD CONSTRAINT "kupa_periodic_backups_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."order_management_document_backups" ADD CONSTRAINT "order_management_document_backups_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."order_management_documents" ADD CONSTRAINT "order_management_documents_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."order_management_periodic_backups" ADD CONSTRAINT "order_management_periodic_backups_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."shared_checks_document_backups" ADD CONSTRAINT "shared_checks_document_backups_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."shared_checks_documents" ADD CONSTRAINT "shared_checks_documents_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE "public"."shared_checks_periodic_backups" ADD CONSTRAINT "shared_checks_periodic_backups_owner_id_fkey" FOREIGN KEY (owner_id) REFERENCES auth.users(id) ON DELETE CASCADE;
CREATE INDEX bank_transactions_owner_account_date_idx ON public.bank_transactions USING btree (owner_id, account_key, transaction_date DESC);
CREATE INDEX bank_transactions_owner_missing_idx ON public.bank_transactions USING btree (owner_id, account_key, account_role, missing_since DESC) WHERE ((presence_state = 'missing'::text) AND (missing_acknowledged_at IS NULL));
CREATE INDEX bank_transactions_owner_reference_idx ON public.bank_transactions USING btree (owner_id, account_key, bank_reference) WHERE (bank_reference <> ''::text);
CREATE INDEX google_calendar_oauth_states_expires_idx ON public.google_calendar_oauth_states USING btree (expires_at);
CREATE UNIQUE INDEX kupa_document_backups_owner_doc_revision_uidx ON public.kupa_document_backups USING btree (owner_id, document_name, revision);
CREATE INDEX kupa_document_backups_owner_doc_saved_idx ON public.kupa_document_backups USING btree (owner_id, document_name, saved_at DESC);
CREATE UNIQUE INDEX kupa_periodic_backups_owner_doc_revision_uidx ON public.kupa_periodic_backups USING btree (owner_id, document_name, revision);
CREATE INDEX kupa_periodic_backups_owner_doc_saved_idx ON public.kupa_periodic_backups USING btree (owner_id, document_name, saved_at DESC);
CREATE UNIQUE INDEX order_management_backups_owner_doc_revision_uidx ON public.order_management_document_backups USING btree (owner_id, document_name, revision);
CREATE INDEX order_management_backups_owner_doc_saved_idx ON public.order_management_document_backups USING btree (owner_id, document_name, saved_at DESC);
CREATE UNIQUE INDEX order_management_periodic_backups_owner_doc_revision_uidx ON public.order_management_periodic_backups USING btree (owner_id, document_name, revision);
CREATE INDEX order_management_periodic_backups_owner_doc_saved_idx ON public.order_management_periodic_backups USING btree (owner_id, document_name, saved_at DESC);
CREATE UNIQUE INDEX shared_checks_backups_owner_doc_revision_uidx ON public.shared_checks_document_backups USING btree (owner_id, document_name, revision);
CREATE INDEX shared_checks_backups_owner_doc_saved_idx ON public.shared_checks_document_backups USING btree (owner_id, document_name, saved_at DESC);
CREATE UNIQUE INDEX shared_checks_periodic_owner_doc_revision_uidx ON public.shared_checks_periodic_backups USING btree (owner_id, document_name, revision);
CREATE INDEX shared_checks_periodic_owner_doc_saved_idx ON public.shared_checks_periodic_backups USING btree (owner_id, document_name, saved_at DESC);
ALTER SEQUENCE "public"."bank_transactions_id_seq" INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START WITH 1 CACHE 1 NO CYCLE;
REVOKE ALL ON SEQUENCE "public"."bank_transactions_id_seq" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."bank_transactions_id_seq" TO "postgres";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."bank_transactions_id_seq" TO "anon";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."bank_transactions_id_seq" TO "authenticated";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."bank_transactions_id_seq" TO "service_role";
ALTER SEQUENCE "public"."kupa_document_backups_id_seq" INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START WITH 1 CACHE 1 NO CYCLE;
REVOKE ALL ON SEQUENCE "public"."kupa_document_backups_id_seq" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."kupa_document_backups_id_seq" TO "postgres";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."kupa_document_backups_id_seq" TO "authenticated";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."kupa_document_backups_id_seq" TO "service_role";
ALTER SEQUENCE "public"."kupa_periodic_backups_id_seq" INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START WITH 1 CACHE 1 NO CYCLE;
REVOKE ALL ON SEQUENCE "public"."kupa_periodic_backups_id_seq" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."kupa_periodic_backups_id_seq" TO "postgres";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."kupa_periodic_backups_id_seq" TO "authenticated";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."kupa_periodic_backups_id_seq" TO "service_role";
ALTER SEQUENCE "public"."order_management_document_backups_id_seq" INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START WITH 1 CACHE 1 NO CYCLE;
REVOKE ALL ON SEQUENCE "public"."order_management_document_backups_id_seq" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."order_management_document_backups_id_seq" TO "postgres";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."order_management_document_backups_id_seq" TO "authenticated";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."order_management_document_backups_id_seq" TO "service_role";
ALTER SEQUENCE "public"."order_management_periodic_backups_id_seq" INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START WITH 1 CACHE 1 NO CYCLE;
REVOKE ALL ON SEQUENCE "public"."order_management_periodic_backups_id_seq" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."order_management_periodic_backups_id_seq" TO "postgres";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."order_management_periodic_backups_id_seq" TO "authenticated";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."order_management_periodic_backups_id_seq" TO "service_role";
ALTER SEQUENCE "public"."shared_checks_document_backups_id_seq" INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START WITH 1 CACHE 1 NO CYCLE;
REVOKE ALL ON SEQUENCE "public"."shared_checks_document_backups_id_seq" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."shared_checks_document_backups_id_seq" TO "postgres";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."shared_checks_document_backups_id_seq" TO "authenticated";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."shared_checks_document_backups_id_seq" TO "service_role";
ALTER SEQUENCE "public"."shared_checks_periodic_backups_id_seq" INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START WITH 1 CACHE 1 NO CYCLE;
REVOKE ALL ON SEQUENCE "public"."shared_checks_periodic_backups_id_seq" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."shared_checks_periodic_backups_id_seq" TO "postgres";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."shared_checks_periodic_backups_id_seq" TO "authenticated";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."shared_checks_periodic_backups_id_seq" TO "service_role";
ALTER SEQUENCE "public"."shared_financial_event_seq" INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START WITH 1 CACHE 1 NO CYCLE;
REVOKE ALL ON SEQUENCE "public"."shared_financial_event_seq" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."shared_financial_event_seq" TO "postgres";
GRANT SELECT, UPDATE, USAGE ON SEQUENCE "public"."shared_financial_event_seq" TO "service_role";
GRANT USAGE ON SEQUENCE "public"."shared_financial_event_seq" TO "authenticated";
CREATE TRIGGER kupa_core_updated_at_track BEFORE INSERT OR UPDATE OF state, updated_at ON kupa_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.track_kupa_core_updated_at();
ALTER TABLE "public"."kupa_documents" ENABLE TRIGGER "kupa_core_updated_at_track";
CREATE TRIGGER kupa_delete_intent_guard BEFORE UPDATE ON kupa_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.kupa_delete_intent_guard();
ALTER TABLE "public"."kupa_documents" ENABLE TRIGGER "kupa_delete_intent_guard";
CREATE TRIGGER kupa_documents_invariant_guard BEFORE INSERT OR UPDATE ON kupa_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.document_invariant_guard();
ALTER TABLE "public"."kupa_documents" ENABLE TRIGGER "kupa_documents_invariant_guard";
CREATE TRIGGER kupa_documents_write_guard BEFORE INSERT OR UPDATE ON kupa_documents FOR EACH ROW EXECUTE FUNCTION kupa_guard_document_write();
ALTER TABLE "public"."kupa_documents" ENABLE TRIGGER "kupa_documents_write_guard";
CREATE TRIGGER kupa_mass_destructive_guard BEFORE UPDATE ON kupa_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.mass_destructive_guard();
ALTER TABLE "public"."kupa_documents" ENABLE TRIGGER "kupa_mass_destructive_guard";
CREATE TRIGGER order_management_delete_intent_guard BEFORE UPDATE ON order_management_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.order_management_delete_intent_guard();
ALTER TABLE "public"."order_management_documents" ENABLE TRIGGER "order_management_delete_intent_guard";
CREATE TRIGGER order_management_documents_invariant_guard BEFORE INSERT OR UPDATE ON order_management_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.document_invariant_guard();
ALTER TABLE "public"."order_management_documents" ENABLE TRIGGER "order_management_documents_invariant_guard";
CREATE TRIGGER order_management_documents_write_guard BEFORE INSERT OR UPDATE ON order_management_documents FOR EACH ROW EXECUTE FUNCTION order_management_guard_document_write();
ALTER TABLE "public"."order_management_documents" ENABLE TRIGGER "order_management_documents_write_guard";
CREATE TRIGGER order_management_mass_destructive_guard BEFORE UPDATE ON order_management_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.mass_destructive_guard();
ALTER TABLE "public"."order_management_documents" ENABLE TRIGGER "order_management_mass_destructive_guard";
CREATE TRIGGER shared_checks_delete_intent_guard BEFORE UPDATE ON shared_checks_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.shared_checks_delete_intent_guard();
ALTER TABLE "public"."shared_checks_documents" ENABLE TRIGGER "shared_checks_delete_intent_guard";
CREATE TRIGGER shared_checks_documents_invariant_guard BEFORE INSERT OR UPDATE ON shared_checks_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.document_invariant_guard();
ALTER TABLE "public"."shared_checks_documents" ENABLE TRIGGER "shared_checks_documents_invariant_guard";
CREATE TRIGGER shared_checks_documents_write_guard BEFORE INSERT OR UPDATE ON shared_checks_documents FOR EACH ROW EXECUTE FUNCTION shared_checks_guard_document_write();
ALTER TABLE "public"."shared_checks_documents" ENABLE TRIGGER "shared_checks_documents_write_guard";
CREATE TRIGGER shared_checks_mass_destructive_guard BEFORE UPDATE ON shared_checks_documents FOR EACH ROW EXECUTE FUNCTION netunim_internal.mass_destructive_guard();
ALTER TABLE "public"."shared_checks_documents" ENABLE TRIGGER "shared_checks_mass_destructive_guard";
ALTER TABLE "netunim_internal"."bank_alert_rollout_baselines" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "netunim_internal"."bank_alert_rollout_baselines" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "netunim_internal"."bank_alert_rollout_baselines" TO "postgres";
ALTER TABLE "netunim_internal"."bank_sync_operations" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "netunim_internal"."bank_sync_operations" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "netunim_internal"."bank_sync_operations" TO "postgres";
GRANT INSERT, SELECT ON TABLE "netunim_internal"."bank_sync_operations" TO "authenticated";
ALTER TABLE "netunim_internal"."document_sync_operations" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "netunim_internal"."document_sync_operations" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "netunim_internal"."document_sync_operations" TO "postgres";
GRANT INSERT, SELECT ON TABLE "netunim_internal"."document_sync_operations" TO "authenticated";
ALTER TABLE "netunim_internal"."restore_operation_groups" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "netunim_internal"."restore_operation_groups" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "netunim_internal"."restore_operation_groups" TO "postgres";
GRANT SELECT ON TABLE "netunim_internal"."restore_operation_groups" TO "authenticated";
ALTER TABLE "netunim_internal"."safety_snapshots" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "netunim_internal"."safety_snapshots" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "netunim_internal"."safety_snapshots" TO "postgres";
GRANT SELECT ON TABLE "netunim_internal"."safety_snapshots" TO "authenticated";
ALTER TABLE "public"."bank_transaction_snapshots" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."bank_transaction_snapshots" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."bank_transaction_snapshots" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."bank_transaction_snapshots" TO "service_role";
GRANT SELECT ON TABLE "public"."bank_transaction_snapshots" TO "authenticated";
ALTER TABLE "public"."bank_transactions" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."bank_transactions" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."bank_transactions" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."bank_transactions" TO "service_role";
GRANT SELECT ON TABLE "public"."bank_transactions" TO "authenticated";
ALTER TABLE "public"."finance_sync_documents" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."finance_sync_documents" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."finance_sync_documents" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."finance_sync_documents" TO "service_role";
GRANT SELECT ON TABLE "public"."finance_sync_documents" TO "authenticated";
ALTER TABLE "public"."finance_sync_leases" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."finance_sync_leases" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."finance_sync_leases" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."finance_sync_leases" TO "service_role";
GRANT SELECT ON TABLE "public"."finance_sync_leases" TO "authenticated";
ALTER TABLE "public"."google_calendar_connections" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."google_calendar_connections" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."google_calendar_connections" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."google_calendar_connections" TO "service_role";
ALTER TABLE "public"."google_calendar_oauth_states" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."google_calendar_oauth_states" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."google_calendar_oauth_states" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."google_calendar_oauth_states" TO "service_role";
ALTER TABLE "public"."kupa_document_backups" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."kupa_document_backups" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."kupa_document_backups" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."kupa_document_backups" TO "service_role";
GRANT INSERT, SELECT ON TABLE "public"."kupa_document_backups" TO "authenticated";
ALTER TABLE "public"."kupa_documents" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."kupa_documents" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."kupa_documents" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."kupa_documents" TO "service_role";
GRANT INSERT, SELECT, UPDATE ON TABLE "public"."kupa_documents" TO "authenticated";
ALTER TABLE "public"."kupa_periodic_backups" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."kupa_periodic_backups" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."kupa_periodic_backups" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."kupa_periodic_backups" TO "service_role";
GRANT INSERT, SELECT ON TABLE "public"."kupa_periodic_backups" TO "authenticated";
ALTER TABLE "public"."order_management_document_backups" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."order_management_document_backups" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."order_management_document_backups" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."order_management_document_backups" TO "service_role";
GRANT INSERT, SELECT ON TABLE "public"."order_management_document_backups" TO "authenticated";
ALTER TABLE "public"."order_management_documents" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."order_management_documents" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."order_management_documents" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."order_management_documents" TO "service_role";
GRANT INSERT, SELECT, UPDATE ON TABLE "public"."order_management_documents" TO "authenticated";
ALTER TABLE "public"."order_management_periodic_backups" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."order_management_periodic_backups" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."order_management_periodic_backups" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."order_management_periodic_backups" TO "service_role";
GRANT INSERT, SELECT ON TABLE "public"."order_management_periodic_backups" TO "authenticated";
ALTER TABLE "public"."shared_checks_document_backups" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."shared_checks_document_backups" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."shared_checks_document_backups" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."shared_checks_document_backups" TO "service_role";
GRANT INSERT, SELECT ON TABLE "public"."shared_checks_document_backups" TO "authenticated";
ALTER TABLE "public"."shared_checks_documents" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."shared_checks_documents" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."shared_checks_documents" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."shared_checks_documents" TO "service_role";
GRANT INSERT, SELECT, UPDATE ON TABLE "public"."shared_checks_documents" TO "authenticated";
ALTER TABLE "public"."shared_checks_periodic_backups" ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE "public"."shared_checks_periodic_backups" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."shared_checks_periodic_backups" TO "postgres";
GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLE "public"."shared_checks_periodic_backups" TO "service_role";
GRANT INSERT, SELECT ON TABLE "public"."shared_checks_periodic_backups" TO "authenticated";
CREATE POLICY "bank_sync_operations_insert_own" ON "netunim_internal"."bank_sync_operations" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "bank_sync_operations_select_own" ON "netunim_internal"."bank_sync_operations" AS PERMISSIVE FOR SELECT TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "document_sync_operations_insert_own" ON "netunim_internal"."document_sync_operations" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "document_sync_operations_select_own" ON "netunim_internal"."document_sync_operations" AS PERMISSIVE FOR SELECT TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "restore_groups_select_own" ON "netunim_internal"."restore_operation_groups" AS PERMISSIVE FOR SELECT TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "safety_snapshots_select_own" ON "netunim_internal"."safety_snapshots" AS PERMISSIVE FOR SELECT TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "bank_transaction_snapshots_insert_own" ON "public"."bank_transaction_snapshots" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "bank_transaction_snapshots_select_own" ON "public"."bank_transaction_snapshots" AS PERMISSIVE FOR SELECT TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "bank_transaction_snapshots_update_own" ON "public"."bank_transaction_snapshots" AS PERMISSIVE FOR UPDATE TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id)) WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "bank_transactions_insert_own" ON "public"."bank_transactions" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "bank_transactions_select_own" ON "public"."bank_transactions" AS PERMISSIVE FOR SELECT TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "bank_transactions_update_own" ON "public"."bank_transactions" AS PERMISSIVE FOR UPDATE TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id)) WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "finance_sync_insert_own" ON "public"."finance_sync_documents" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "finance_sync_select_own" ON "public"."finance_sync_documents" AS PERMISSIVE FOR SELECT TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "finance_sync_update_own" ON "public"."finance_sync_documents" AS PERMISSIVE FOR UPDATE TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id)) WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "finance_sync_leases_insert_own" ON "public"."finance_sync_leases" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "finance_sync_leases_select_own" ON "public"."finance_sync_leases" AS PERMISSIVE FOR SELECT TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "finance_sync_leases_update_own" ON "public"."finance_sync_leases" AS PERMISSIVE FOR UPDATE TO "authenticated" USING ((( SELECT auth.uid() AS uid) = owner_id)) WITH CHECK ((( SELECT auth.uid() AS uid) = owner_id));
CREATE POLICY "kupa_backups_insert_own" ON "public"."kupa_document_backups" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "kupa_backups_select_own" ON "public"."kupa_document_backups" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "kupa_documents_insert_own" ON "public"."kupa_documents" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "kupa_documents_select_own" ON "public"."kupa_documents" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "kupa_documents_update_own" ON "public"."kupa_documents" AS PERMISSIVE FOR UPDATE TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id))) WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "kupa_periodic_backups_insert_own" ON "public"."kupa_periodic_backups" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "kupa_periodic_backups_select_own" ON "public"."kupa_periodic_backups" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "order_management_backups_insert_own" ON "public"."order_management_document_backups" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "order_management_backups_select_own" ON "public"."order_management_document_backups" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "order_management_documents_insert_own" ON "public"."order_management_documents" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "order_management_documents_select_own" ON "public"."order_management_documents" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "order_management_documents_update_own" ON "public"."order_management_documents" AS PERMISSIVE FOR UPDATE TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id))) WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "order_management_periodic_backups_insert_own" ON "public"."order_management_periodic_backups" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "order_management_periodic_backups_select_own" ON "public"."order_management_periodic_backups" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "shared_checks_backups_insert_own" ON "public"."shared_checks_document_backups" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "shared_checks_backups_select_own" ON "public"."shared_checks_document_backups" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "shared_checks_documents_insert_own" ON "public"."shared_checks_documents" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "shared_checks_documents_select_own" ON "public"."shared_checks_documents" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "shared_checks_documents_update_own" ON "public"."shared_checks_documents" AS PERMISSIVE FOR UPDATE TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id))) WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "shared_checks_periodic_insert_own" ON "public"."shared_checks_periodic_backups" AS PERMISSIVE FOR INSERT TO "authenticated" WITH CHECK (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
CREATE POLICY "shared_checks_periodic_select_own" ON "public"."shared_checks_periodic_backups" AS PERMISSIVE FOR SELECT TO "authenticated" USING (((( SELECT auth.uid() AS uid) IS NOT NULL) AND (( SELECT auth.uid() AS uid) = owner_id)));
REVOKE ALL ON FUNCTION "netunim_internal"."assert_document_invariants"(p_domain text, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."assert_document_invariants"(p_domain text, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."assert_document_invariants"(p_domain text, p_state jsonb) TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."assert_entity_array_ids"(p_state jsonb, p_path text[], p_required boolean) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."assert_entity_array_ids"(p_state jsonb, p_path text[], p_required boolean) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."assert_entity_array_ids"(p_state jsonb, p_path text[], p_required boolean) TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."assert_finance_sync_fence"(p_lease_name text, p_lease_token text, p_fence_epoch bigint) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."assert_finance_sync_fence"(p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "postgres";
REVOKE ALL ON FUNCTION "netunim_internal"."canonical_delete_intents"(p_intents jsonb, p_allowed text[]) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."canonical_delete_intents"(p_intents jsonb, p_allowed text[]) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."canonical_delete_intents"(p_intents jsonb, p_allowed text[]) TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."capture_safety_snapshot"(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_kind text, p_restore_group_id uuid) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."capture_safety_snapshot"(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_kind text, p_restore_group_id uuid) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."capture_safety_snapshot"(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_kind text, p_restore_group_id uuid) TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."document_invariant_guard"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."document_invariant_guard"() TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."document_invariant_guard"() TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."fenced_impl_merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."fenced_impl_merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."fenced_impl_merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."fenced_impl_save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."fenced_impl_save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."fenced_impl_save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."fenced_impl_save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."fenced_impl_save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."fenced_impl_save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."fenced_impl_sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."fenced_impl_sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."fenced_impl_sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."json_collection_counts"(p_state jsonb, p_paths text[]) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."json_collection_counts"(p_state jsonb, p_paths text[]) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."json_collection_counts"(p_state jsonb, p_paths text[]) TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."kupa_delete_intent_guard"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."kupa_delete_intent_guard"() TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."kupa_delete_intent_guard"() TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."kupa_header_owned_state"(p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."kupa_header_owned_state"(p_state jsonb) TO "postgres";
REVOKE ALL ON FUNCTION "netunim_internal"."mass_destructive_guard"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."mass_destructive_guard"() TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."mass_destructive_guard"() TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."order_management_delete_intent_guard"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."order_management_delete_intent_guard"() TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."order_management_delete_intent_guard"() TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."prune_document_backups"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."prune_document_backups"() TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."prune_document_backups"() TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."prune_sync_operation_ledgers"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."prune_sync_operation_ledgers"() TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."prune_sync_operation_ledgers"() TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."record_operation_audit"(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_audit jsonb, p_before jsonb, p_after jsonb, p_paths text[], p_restore_group_id uuid) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."record_operation_audit"(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_audit jsonb, p_before jsonb, p_after jsonb, p_paths text[], p_restore_group_id uuid) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."record_operation_audit"(p_owner uuid, p_domain text, p_document_name text, p_operation_id text, p_audit jsonb, p_before jsonb, p_after jsonb, p_paths text[], p_restore_group_id uuid) TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."save_finance_sync_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_finance_sync_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_finance_sync_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "netunim_internal"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "netunim_internal"."shared_checks_delete_intent_guard"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."shared_checks_delete_intent_guard"() TO "postgres";
GRANT EXECUTE ON FUNCTION "netunim_internal"."shared_checks_delete_intent_guard"() TO "authenticated";
REVOKE ALL ON FUNCTION "netunim_internal"."track_kupa_core_updated_at"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "netunim_internal"."track_kupa_core_updated_at"() TO "postgres";
REVOKE ALL ON FUNCTION "public"."acknowledge_bank_transaction_alert"(p_transaction_id bigint, p_alert_kind text) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."acknowledge_bank_transaction_alert"(p_transaction_id bigint, p_alert_kind text) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."acknowledge_bank_transaction_alert"(p_transaction_id bigint, p_alert_kind text) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."acknowledge_bank_transaction_alert"(p_transaction_id bigint, p_alert_kind text) TO "service_role";
REVOKE ALL ON FUNCTION "public"."acknowledge_bank_transaction_missing"(p_transaction_id bigint) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."acknowledge_bank_transaction_missing"(p_transaction_id bigint) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."acknowledge_bank_transaction_missing"(p_transaction_id bigint) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."acknowledge_bank_transaction_missing"(p_transaction_id bigint) TO "service_role";
REVOKE ALL ON FUNCTION "public"."apply_restore_group_v5"(p_restore_group_id uuid) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."apply_restore_group_v5"(p_restore_group_id uuid) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."apply_restore_group_v5"(p_restore_group_id uuid) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."apply_restore_group_v5"(p_restore_group_id uuid) TO "service_role";
REVOKE ALL ON FUNCTION "public"."bulk_delete_save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."bulk_delete_save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."bulk_delete_save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."bulk_delete_save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."claim_finance_sync_lease"(p_lease_name text, p_lease_token text, p_ttl_seconds integer) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."claim_finance_sync_lease"(p_lease_name text, p_lease_token text, p_ttl_seconds integer) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."claim_finance_sync_lease"(p_lease_name text, p_lease_token text, p_ttl_seconds integer) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."claim_finance_sync_lease"(p_lease_name text, p_lease_token text, p_ttl_seconds integer) TO "service_role";
REVOKE ALL ON FUNCTION "public"."get_netunim_sync_capabilities"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."get_netunim_sync_capabilities"() TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."get_netunim_sync_capabilities"() TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."get_netunim_sync_capabilities"() TO "service_role";
REVOKE ALL ON FUNCTION "public"."kupa_guard_document_write"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."kupa_guard_document_write"() TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."kupa_guard_document_write"() TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."kupa_guard_document_write"() TO "service_role";
REVOKE ALL ON FUNCTION "public"."list_incomplete_restore_groups_v5"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."list_incomplete_restore_groups_v5"() TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."list_incomplete_restore_groups_v5"() TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."list_incomplete_restore_groups_v5"() TO "service_role";
REVOKE ALL ON FUNCTION "public"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."merge_bank_transactions"(p_account_key text, p_account_role text, p_transactions jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "service_role";
REVOKE ALL ON FUNCTION "public"."order_management_guard_document_write"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."order_management_guard_document_write"() TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."order_management_guard_document_write"() TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."order_management_guard_document_write"() TO "service_role";
REVOKE ALL ON FUNCTION "public"."release_finance_sync_lease"(p_lease_name text, p_lease_token text) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."release_finance_sync_lease"(p_lease_name text, p_lease_token text) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."release_finance_sync_lease"(p_lease_name text, p_lease_token text) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."release_finance_sync_lease"(p_lease_name text, p_lease_token text) TO "service_role";
REVOKE ALL ON FUNCTION "public"."rls_auto_enable"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."rls_auto_enable"() TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."rls_auto_enable"() TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint, p_lease_name text, p_lease_token text, p_fence_epoch bigint) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_bank_sync_snapshot"(p_document_name text, p_bank_state jsonb, p_snapshot_token text, p_snapshot_seq bigint, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_finance_sync_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_finance_sync_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_finance_sync_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_finance_sync_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_finance_sync_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_finance_sync_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_finance_sync_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_audit jsonb, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_kupa_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_kupa_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_kupa_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_order_management_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_order_management_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_order_management_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_delete_intents jsonb, p_audit jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document"(p_document_name text, p_expected_revision bigint, p_state jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_shared_checks_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v3"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_shared_checks_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v4"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."save_shared_checks_document_v5"(p_document_name text, p_expected_revision bigint, p_state jsonb, p_operation_id text, p_deleted_check_ids jsonb, p_audit jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."shared_checks_guard_document_write"() FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."shared_checks_guard_document_write"() TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."shared_checks_guard_document_write"() TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."shared_checks_guard_document_write"() TO "service_role";
REVOKE ALL ON FUNCTION "public"."stage_restore_group_v5"(p_restore_group_id uuid, p_app_site text, p_main_document_name text, p_main_base_revision bigint, p_main_state jsonb, p_main_delete_intents jsonb, p_checks_document_name text, p_checks_base_revision bigint, p_checks_state jsonb, p_checks_delete_ids jsonb, p_main_operation_id text, p_checks_operation_id text, p_audit jsonb) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."stage_restore_group_v5"(p_restore_group_id uuid, p_app_site text, p_main_document_name text, p_main_base_revision bigint, p_main_state jsonb, p_main_delete_intents jsonb, p_checks_document_name text, p_checks_base_revision bigint, p_checks_state jsonb, p_checks_delete_ids jsonb, p_main_operation_id text, p_checks_operation_id text, p_audit jsonb) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."stage_restore_group_v5"(p_restore_group_id uuid, p_app_site text, p_main_document_name text, p_main_base_revision bigint, p_main_state jsonb, p_main_delete_intents jsonb, p_checks_document_name text, p_checks_base_revision bigint, p_checks_state jsonb, p_checks_delete_ids jsonb, p_main_operation_id text, p_checks_operation_id text, p_audit jsonb) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."stage_restore_group_v5"(p_restore_group_id uuid, p_app_site text, p_main_document_name text, p_main_base_revision bigint, p_main_state jsonb, p_main_delete_intents jsonb, p_checks_document_name text, p_checks_base_revision bigint, p_checks_state jsonb, p_checks_delete_ids jsonb, p_main_operation_id text, p_checks_operation_id text, p_audit jsonb) TO "service_role";
REVOKE ALL ON FUNCTION "public"."sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean, p_lease_name text, p_lease_token text, p_fence_epoch bigint) FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT EXECUTE ON FUNCTION "public"."sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "postgres";
GRANT EXECUTE ON FUNCTION "public"."sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "authenticated";
GRANT EXECUTE ON FUNCTION "public"."sync_bank_transactions_snapshot"(p_account_key text, p_account_role text, p_transactions jsonb, p_snapshot_at timestamp with time zone, p_coverage_from date, p_coverage_to date, p_complete boolean, p_lease_name text, p_lease_token text, p_fence_epoch bigint) TO "service_role";
REVOKE ALL ON SCHEMA "netunim_internal" FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
GRANT USAGE, CREATE ON SCHEMA "netunim_internal" TO "postgres";
GRANT USAGE ON SCHEMA "netunim_internal" TO "authenticated";
GRANT USAGE ON SCHEMA "netunim_internal" TO "service_role";
REVOKE ALL ON SCHEMA "public" FROM PUBLIC, "anon", "authenticated", "pg_database_owner", "postgres", "service_role";
GRANT USAGE, CREATE ON SCHEMA "public" TO "pg_database_owner";
GRANT USAGE ON SCHEMA "public" TO PUBLIC;
GRANT USAGE ON SCHEMA "public" TO "postgres";
GRANT USAGE ON SCHEMA "public" TO "anon";
GRANT USAGE ON SCHEMA "public" TO "authenticated";
GRANT USAGE ON SCHEMA "public" TO "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" REVOKE ALL ON SEQUENCES FROM PUBLIC, "anon", "authenticated", "postgres", "service_role", "supabase_admin";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT SELECT, UPDATE, USAGE ON SEQUENCES TO "postgres";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT SELECT, UPDATE, USAGE ON SEQUENCES TO "anon";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT SELECT, UPDATE, USAGE ON SEQUENCES TO "authenticated";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT SELECT, UPDATE, USAGE ON SEQUENCES TO "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" REVOKE ALL ON FUNCTIONS FROM PUBLIC, "anon", "authenticated", "postgres", "service_role", "supabase_admin";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT EXECUTE ON FUNCTIONS TO "postgres";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT EXECUTE ON FUNCTIONS TO "anon";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT EXECUTE ON FUNCTIONS TO "authenticated";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT EXECUTE ON FUNCTIONS TO "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" REVOKE ALL ON TABLES FROM PUBLIC, "anon", "authenticated", "postgres", "service_role", "supabase_admin";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLES TO "postgres";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLES TO "anon";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLES TO "authenticated";
ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin" IN SCHEMA "public" GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLES TO "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" REVOKE ALL ON SEQUENCES FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT SELECT, UPDATE, USAGE ON SEQUENCES TO "postgres";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT SELECT, UPDATE, USAGE ON SEQUENCES TO "anon";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT SELECT, UPDATE, USAGE ON SEQUENCES TO "authenticated";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT SELECT, UPDATE, USAGE ON SEQUENCES TO "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" REVOKE ALL ON FUNCTIONS FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT EXECUTE ON FUNCTIONS TO "postgres";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT EXECUTE ON FUNCTIONS TO "anon";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT EXECUTE ON FUNCTIONS TO "authenticated";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT EXECUTE ON FUNCTIONS TO "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" REVOKE ALL ON TABLES FROM PUBLIC, "anon", "authenticated", "postgres", "service_role";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLES TO "postgres";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLES TO "anon";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLES TO "authenticated";
ALTER DEFAULT PRIVILEGES FOR ROLE "postgres" IN SCHEMA "public" GRANT INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON TABLES TO "service_role";
DO $baseline$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_event_trigger WHERE evtname='ensure_rls') THEN CREATE EVENT TRIGGER "ensure_rls" ON ddl_command_end WHEN TAG IN ('CREATE TABLE', 'CREATE TABLE AS', 'SELECT INTO') EXECUTE FUNCTION "public"."rls_auto_enable"(); END IF; END $baseline$;
ALTER EVENT TRIGGER "ensure_rls" OWNER TO "postgres";
ALTER EVENT TRIGGER "ensure_rls" ENABLE;
COMMIT;
